# 任务记录

## 任务1：新增发票舞弊检测特征值

### 任务描述
新增一个特征值：发票是否舞弊，检查发票图片是否P图。一个报销单有多张发票，每张都要检查。

### 思考过程

#### 1. P图检测方案选择

**方案对比：**

1. **传统图像处理方法**
   - ELA（Error Level Analysis）：错误级别分析
   - 优点：无需AI模型，计算快速
   - 缺点：准确率较低，容易被绕过

2. **深度学习方法**
   - CNN模型：训练专门的P图检测模型
   - 优点：准确率高
   - 缺点：需要大量训练数据，模型文件大

3. **大模型视觉检测**
   - 使用GPT-4V、文心一言等大模型的视觉能力
   - 优点：无需训练，泛化能力强
   - 缺点：API调用成本高，速度慢

4. **专业AI服务**
   - 阿里云、腾讯云等提供的图像审核服务
   - 优点：专业、准确、快速
   - 缺点：需要付费

**选择方案：大模型视觉检测**

理由：
- 项目已经集成了大模型API，可以复用
- 无需额外训练模型
- 泛化能力强，能检测各种P图手法
- 可以结合OCR结果进行综合判断

**实现方式：**
- 使用智谱AI的视觉模型（GLM-4V）
- 将发票图片上传到模型
- 让模型判断是否存在P图痕迹
- 返回布尔值和置信度

#### 2. 特征函数设计

**特征名称：** invoice_fraud_detection
**特征描述：** 检测发票图片是否存在P图/篡改痕迹
**特征类型：** boolean
**特征分类：** AI特征函数

**输入参数：**
- invoice_images: 发票图片路径列表

**输出结果：**
- value: boolean（true表示存在P图，false表示正常）
- metadata: 包含每张发票的检测结果

**配置参数：**
- confidence_threshold: 置信度阈值（默认0.7）
- check_all_images: 是否检查所有图片（默认true）

#### 3. 多发票处理策略

**策略：**
- 遍历报销单的所有发票
- 对每张发票调用P图检测
- 只要有一张发票检测到P图，就返回true
- 记录每张发票的检测结果

#### 4. 性能优化

**优化措施：**
- 异步检测：多张发票可以并行检测
- 结果缓存：相同图片的检测结果缓存
- 降级策略：大模型调用失败时返回false（默认通过）

### 实现步骤

1. 创建特征函数 InvoiceFraudDetectionFunction
2. 集成智谱AI视觉模型
3. 实现多发票并行检测
4. 添加结果缓存
5. 注册特征函数
6. 创建特征记录到数据库
7. 创建规则到数据库
8. 测试验证

### 执行结果

#### 1. 特征函数开发 ✅
- 创建了 `InvoiceFraudDetectionFunction` 特征函数
- 函数名称：`invoice_fraud_detection`
- 支持多张发票并行检测
- 支持配置置信度阈值和检测提示词
- 实现了详细的检测结果记录

#### 2. 特征函数注册 ✅
- 在 `internal/server/impl.go` 中注册了新的特征函数
- 设置了日志记录器
- 特征函数已成功加载到规则引擎

#### 3. 数据库插入 ✅
- 创建了SQL脚本：`sql/insert_invoice_fraud_detection_rule.sql`
- 成功插入特征记录：
  - 特征ID：`feat-invoice-fraud-detection`
  - 特征名称：发票舞弊检测
  - 特征类型：boolean
  - 函数配置：包含置信度阈值、检查所有图片、检测提示词

- 成功插入规则记录：
  - 规则ID：`rule-invoice-fraud-detection`
  - 规则名称：发票舞弊检测规则
  - 规则优先级：100
  - 规则状态：启用

- 成功插入条件记录：
  - 条件ID：`cond-invoice-fraud-detection`
  - 特征ID：`feat-invoice-fraud-detection`
  - 操作符：eq（等于）
  - 参考值：true（检测到舞弊）

- 成功插入决策记录：
  - 决策ID：`decision-invoice-fraud-detection`
  - 决策类型：reject（驳回）
  - 决策原因：发票存在P图/篡改痕迹，不符合报销要求

#### 4. 技术实现细节

**特征函数特点：**
- 支持多张发票检测，自动遍历报销单中的所有发票
- 可配置是否检查所有图片（check_all_images）
- 可配置置信度阈值（confidence_threshold）
- 可自定义检测提示词（detection_prompt）
- 详细的检测结果记录，包括每张发票的检测详情

**检测结果格式：**
```json
{
  "value": true,
  "metadata": {
    "total_invoices": 3,
    "checked_invoices": 3,
    "fraud_invoices": 1,
    "detection_results": [
      {
        "invoice_index": 0,
        "image_path": "/uploads/invoices/xxx.png",
        "is_fraud": false,
        "confidence": 0.3,
        "reasoning": "发票图片清晰，无明显P图痕迹"
      },
      {
        "invoice_index": 1,
        "image_path": "/uploads/invoices/yyy.png",
        "is_fraud": true,
        "confidence": 0.85,
        "reasoning": "发票金额数字存在涂改痕迹，字体不一致"
      }
    ],
    "confidence_threshold": 0.7
  }
}
```

**规则执行逻辑：**
1. 规则引擎加载规则
2. 计算特征值（调用发票舞弊检测特征函数）
3. 特征函数遍历所有发票，调用视觉模型检测
4. 评估条件：特征值是否等于true
5. 生成决策：reject（驳回）

#### 5. 下一步工作
- ✅ 视觉分析API接口已实现
- ✅ 大模型视觉能力已集成（智谱AI GLM-4V）
- ✅ 服务器已成功启动，规则引擎加载了10条规则
- ⏳ 完整流程测试待进行
- ⏳ 优化检测性能（缓存、异步等）待进行

### 任务完成情况
- ✅ 特征函数开发完成
- ✅ 特征函数注册完成
- ✅ 数据库数据插入完成
- ✅ 视觉分析API接口已实现
- ✅ 大模型视觉能力已集成
- ✅ 服务器成功启动，规则引擎正常工作
- ⏳ 完整流程测试待进行
- ⏳ 性能优化待进行

### 技术总结

#### 1. 技术方案选择
- **P图检测方案**：大模型视觉检测（智谱AI GLM-4V）
- **选择理由**：
  - 项目已集成大模型API，可复用
  - 无需额外训练模型
  - 泛化能力强，能检测各种P图手法
  - 可以结合OCR结果进行综合判断

#### 2. 特征函数设计
- **函数名称**：`invoice_fraud_detection`
- **特征类型**：boolean
- **特征分类**：AI特征函数
- **支持功能**：
  - 多张发票检测
  - 可配置置信度阈值
  - 可配置检测提示词
  - 详细的检测结果记录

#### 3. API接口设计
- **接口路径**：`POST /api/v1/vision/analyze`
- **请求参数**：
  - `image_url`: 图片URL
  - `prompt`: 检测提示词
- **响应结果**：
  - `confidence`: 置信度（0-1）
  - `reasoning`: 检测理由

#### 4. 规则配置
- **规则ID**：`rule-invoice-fraud-detection`
- **规则名称**：发票舞弊检测规则
- **规则优先级**：100
- **规则条件**：特征值等于true（检测到舞弊）
- **规则决策**：reject（驳回）

#### 5. 系统集成
- **特征函数注册**：已注册到特征函数注册表
- **规则引擎加载**：规则引擎已加载10条规则
- **API路由注册**：视觉分析API已注册到路由
- **服务器启动**：服务器成功启动，监听8080端口
