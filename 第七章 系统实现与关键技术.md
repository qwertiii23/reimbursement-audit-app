# 第七章 系统实现与关键技术

## 7.1 系统开发环境

### 7.1.1 开发工具与框架

本系统采用前后端分离的架构设计，后端使用Go语言开发，前端使用Vue.js框架，通过RESTful API进行数据交互。系统整体采用微服务架构思想，将规则引擎、RAG服务、OCR识别、审核服务等核心功能模块化，实现了高内聚低耦合的系统设计。

#### 后端技术栈

后端采用Go 1.25.5作为开发语言，选择Gin框架作为Web框架。Gin是一个高性能的HTTP Web框架，基于httprouter开发，具有路由分组、中间件支持、JSON验证、错误管理等功能，非常适合构建RESTful API服务。Gin框架的路由性能优异，能够处理高并发请求，满足报销审核系统对性能的要求。

在数据持久化方面，系统采用GORM（Go Object-Relational Mapping）作为ORM框架。GORM是Go语言中最流行的ORM库之一，提供了完整的数据库操作能力，包括模型定义、关联关系、钩子函数、事务处理等。GORM支持MySQL和PostgreSQL等多种数据库，系统使用MySQL存储业务数据，使用PostgreSQL存储向量数据，充分发挥了不同数据库的优势。

规则引擎采用grule-rule-engine，这是一个基于ANTLR4开发的规则引擎，支持规则的定义、解析和执行。grule引擎采用DSL（Domain Specific Language）定义规则，规则文件采用.grl格式，具有语法简洁、易于维护的特点。引擎支持复杂的条件判断、逻辑运算和决策输出，能够满足报销审核中各种刚性规则的校验需求。

OCR服务采用腾讯云OCR API，该服务提供了发票识别、票据识别等多种OCR能力，识别准确率高，响应速度快。系统通过腾讯云SDK调用OCR服务，支持发票代码、发票号码、开票日期、金额等关键信息的自动提取。

RAG服务采用OpenAI的GPT模型作为大语言模型，使用text-embedding-ada-002模型生成文本向量。向量存储使用Chroma向量数据库，Chroma是一个开源的向量数据库，支持向量相似度检索、元数据过滤等功能，能够高效地存储和检索文档向量。

日志系统采用zerolog和logrus两个日志库，zerolog是一个高性能的结构化日志库，支持JSON格式输出，适合生产环境；logrus是一个功能丰富的日志库，支持多种格式和输出方式，适合开发调试。

#### 前端技术栈

前端采用Vue 3作为开发框架，Vue 3是Vue.js的最新版本，采用了Composition API，提供了更好的类型支持和代码组织方式。Vue 3的性能相比Vue 2有显著提升，虚拟DOM的diff算法更加高效，适合构建复杂的单页应用。

UI组件库采用Element Plus，这是基于Vue 3的桌面端组件库，提供了丰富的UI组件，包括表格、表单、对话框、上传组件等。Element Plus的组件设计美观、功能完善，能够快速构建企业级应用界面。

状态管理采用Pinia，Pinia是Vue 3官方推荐的状态管理库，相比Vuex具有更简洁的API和更好的TypeScript支持。Pinia支持模块化的状态定义，每个模块可以独立管理自己的状态，便于维护和扩展。

路由管理采用Vue Router，这是Vue.js官方的路由管理器，支持动态路由、路由守卫、路由元信息等功能。系统通过路由守卫实现了登录验证和权限控制。

HTTP客户端采用Axios，Axios是一个基于Promise的HTTP客户端，支持请求和响应拦截、请求取消、自动转换JSON数据等功能。系统通过Axios封装了统一的API调用方法，实现了请求拦截和错误处理。

构建工具采用Vite，Vite是一个新一代的前端构建工具，采用ES Module进行开发，启动速度快，热更新效率高。Vite支持Vue 3的单文件组件，提供了丰富的插件生态，能够满足项目的构建需求。

#### 开发工具

代码编辑器采用VSCode，配合Go和Vue插件，提供了代码高亮、智能提示、代码格式化等功能。版本控制采用Git，代码托管在GitHub上，实现了团队协作和版本管理。

数据库管理工具采用Navicat和DBeaver，这两个工具支持MySQL和PostgreSQL等多种数据库，提供了可视化的数据库管理界面，方便数据库的设计和维护。

API测试工具采用Postman，Postman提供了丰富的API测试功能，支持环境变量、集合管理、自动化测试等，能够方便地测试RESTful API接口。

### 7.1.2 数据库设计

系统采用MySQL和PostgreSQL双数据库架构，MySQL用于存储业务数据，PostgreSQL用于存储向量数据。MySQL是一个关系型数据库管理系统，具有高性能、高可靠性、易用性等特点，适合存储结构化的业务数据。PostgreSQL是一个对象关系型数据库，支持丰富的数据类型和索引，适合存储向量数据。

#### MySQL数据库设计

MySQL数据库包含以下主要表：

**用户表（users）**

用户表用于存储系统用户的基本信息，包括用户ID、用户名、密码、邮箱、真实姓名、角色、状态等字段。用户ID采用UUID格式，确保全局唯一。密码采用加密存储，使用bcrypt算法进行哈希处理。角色字段区分管理员和普通用户，状态字段记录用户的激活状态。表结构如下：

```sql
CREATE TABLE IF NOT EXISTS `users` (
  `id` varchar(36) NOT NULL COMMENT '用户ID',
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `password` varchar(255) NOT NULL COMMENT '密码（加密）',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `real_name` varchar(50) DEFAULT NULL COMMENT '真实姓名',
  `role` varchar(20) NOT NULL DEFAULT 'user' COMMENT '角色（admin/user）',
  `status` varchar(20) NOT NULL DEFAULT 'active' COMMENT '状态（active/inactive/locked）',
  `created_at` datetime NOT NULL COMMENT '创建时间',
  `updated_at` datetime NOT NULL COMMENT '更新时间',
  `last_login` datetime DEFAULT NULL COMMENT '最后登录时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_username` (`username`),
  UNIQUE KEY `idx_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

**规则引擎相关表**

规则引擎相关表包括特征表（features）、特征值表（feature_values）、规则表（rule_engine_rules）、条件表（conditions）和决策表（decisions）。这些表构成了规则引擎的数据模型，支持规则的灵活定义和动态配置。

特征表定义了规则引擎支持的所有特征，包括特征名称、特征编码、特征描述、特征类型、值类型、特征分类等字段。特征编码是特征的唯一标识，用于规则条件中引用特征。特征类型包括string、number、boolean、date等，值类型包括single、multiple、range等。

特征值表存储了特征的可选值，用于枚举类型特征。每个特征可以有多个可选值，每个值有对应的显示标签和排序顺序。

规则表存储了规则引擎的规则定义，包括规则名称、规则描述、优先级、启用状态等字段。优先级字段用于控制规则的执行顺序，数字越大优先级越高。启用状态字段控制规则是否生效。

条件表存储了规则的条件定义，每个规则可以有多个条件，条件之间通过逻辑操作符（AND/OR）组合。条件包括特征ID、操作符、比较值、逻辑操作符、排序顺序等字段。操作符包括等于、不等于、大于、小于、包含等。

决策表存储了规则的决策结果，包括决策类型、决策原因、执行动作等字段。决策类型包括通过、驳回、标记等。

**知识库文件表（knowledge_files）**

知识库文件表用于存储知识库的文件信息，包括文件ID、文件名、文件路径、文件类型、文件大小、文件分类、文件描述、上传者、下载次数、状态等字段。文件类型包括pdf、word、txt等，文件分类用于组织不同类型的知识文档。

**审核流程日志表（audit_flow_logs）**

审核流程日志表用于记录审核全流程的流转信息，包括日志ID、报销单ID、审核ID、流程状态、流程类型、操作人ID、操作人姓名、操作动作、操作原因、操作结果、IP地址、创建时间等字段。流程状态包括智能审核开始、智能审核通过、智能审核失败、人工审核开始、人工审核通过、人工审核驳回等。流程类型分为智能审核和人工审核。操作动作包括开始审核、通过审核、驳回审核等。

**报销制度文档表（reimbursement_documents）**

报销制度文档表用于存储报销制度文档的分片内容和向量嵌入，包括文档ID、文件名、文件类型、分片ID、分片序号、分片内容、向量嵌入、创建时间、更新时间等字段。文档类型包括pdf、word、txt等。分片内容字段存储文档的分片文本，向量嵌入字段存储文本的向量表示。表结构如下：

```sql
CREATE TABLE IF NOT EXISTS reimbursement_documents (
    id VARCHAR(36) PRIMARY KEY COMMENT '文档ID',
    file_name VARCHAR(255) NOT NULL COMMENT '文件名',
    file_type VARCHAR(20) NOT NULL COMMENT '文件类型（pdf/word/txt）',
    chunk_id VARCHAR(36) NOT NULL COMMENT '分片ID',
    chunk_index INT NOT NULL COMMENT '分片序号',
    chunk_content TEXT NOT NULL COMMENT '分片内容',
    embedding TEXT COMMENT '向量（JSON+BASE64）',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_chunk_id (chunk_id),
    INDEX idx_file_name (file_name),
    INDEX idx_file_type (file_type),
    FULLTEXT idx_chunk_content (chunk_content) COMMENT '全文索引，用于文本匹配检索'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='报销制度文档表';
```

该表使用了全文索引，支持文本匹配检索，提高了文档检索的效率。

#### PostgreSQL数据库设计

PostgreSQL数据库主要用于存储向量数据，采用pgvector扩展支持向量类型。pgvector是一个PostgreSQL的向量扩展，提供了向量数据类型和向量相似度计算函数，能够高效地存储和检索向量数据。

PostgreSQL数据库包含以下主要表：

**向量表（vectors）**

向量表用于存储文档的向量嵌入，包括向量ID、文档ID、分片ID、向量数据、创建时间等字段。向量数据字段采用vector类型，支持向量相似度计算。表结构如下：

```sql
CREATE TABLE IF NOT EXISTS vectors (
    id VARCHAR(36) PRIMARY KEY,
    document_id VARCHAR(36) NOT NULL,
    chunk_id VARCHAR(36) NOT NULL,
    embedding vector(1536) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_document_id (document_id),
    INDEX idx_chunk_id (chunk_id)
);
```

向量表使用vector类型存储1536维的向量，这是OpenAI text-embedding-ada-002模型的向量维度。表结构包含了文档ID和分片ID的索引，便于快速检索相关文档。

#### 数据库索引设计

为了提高查询性能，系统在关键表上建立了索引。用户表在用户名和邮箱字段上建立了唯一索引，确保用户名和邮箱的唯一性。规则引擎相关表在特征编码、特征分类、规则优先级等字段上建立了索引，提高规则查询和匹配的效率。知识库文件表在文件分类和状态字段上建立了索引，便于文件分类查询。审核流程日志表在报销单ID、审核ID、流程状态、创建时间等字段上建立了索引，支持多维度查询。报销制度文档表在分片ID、文件名、文件类型字段上建立了索引，并建立了全文索引，支持文本匹配检索。

#### 数据库连接池配置

系统使用GORM的连接池功能管理数据库连接，MySQL数据库的连接池配置如下：最大连接数为25，最大空闲连接数为5，连接最大生存时间为1小时，连接最大空闲时间为10分钟。PostgreSQL数据库的连接池配置类似，根据实际负载调整连接数。连接池配置避免了频繁创建和销毁连接的开销，提高了数据库访问性能。

### 7.1.3 接口设计

系统采用RESTful API设计风格，通过HTTP协议进行数据交互。API接口遵循资源导向的设计原则，使用HTTP动词表示操作类型，使用URL路径表示资源。系统提供了审核、上传、用户、规则引擎、知识库、查询等多个模块的API接口，覆盖了报销审核系统的全部功能。

#### 接口设计原则

RESTful API设计遵循以下原则：

1. **资源导向**：URL表示资源，使用名词而非动词，如/reimbursements表示报销单资源，/rules表示规则资源。

2. **HTTP动词**：使用HTTP动词表示操作类型，GET表示查询资源，POST表示创建资源，PUT表示更新资源，DELETE表示删除资源。

3. **统一响应格式**：所有接口采用统一的响应格式，包含状态码、消息、数据等字段，便于前端统一处理。

4. **版本控制**：接口路径包含版本号，如/api/v1/reimbursements，便于接口升级和兼容。

5. **分页查询**：列表查询接口支持分页参数，包括page（页码）和size（每页数量），返回结果包含total（总数）和items（数据列表）。

6. **错误处理**：接口返回标准的HTTP状态码，200表示成功，400表示参数错误，401表示未授权，403表示禁止访问，404表示资源不存在，500表示服务器错误。

7. **认证授权**：接口使用JWT（JSON Web Token）进行身份认证，Token包含在HTTP请求头的Authorization字段中。

8. **限流保护**：接口实现了限流保护，防止恶意请求和滥用，默认每分钟最多60次请求。

#### 审核接口

审核接口用于触发和管理报销单审核流程，包括以下接口：

1. **开始审核（POST /api/v1/audit/start）**

   触发报销单审核，接收报销单ID作为参数，调用规则引擎和RAG服务进行审核，返回审核状态和结果。接口参数如下：

   ```json
   {
     "reimbursement_id": "报销单ID"
   }
   ```

   接口返回审核ID和审核状态，审核状态包括pending（待审核）、processing（审核中）、completed（已完成）、failed（失败）。

2. **撤回审核（POST /api/v1/audit/withdraw）**

   撤回正在进行的审核，接收审核ID作为参数，将审核状态重置为待审核。接口参数如下：

   ```json
   {
     "audit_id": "审核ID"
   }
   ```

3. **获取审核状态（GET /api/v1/audit/status/:audit_id）**

   查询审核的当前状态，接收审核ID作为路径参数，返回审核状态和进度信息。

4. **获取审核结果（GET /api/v1/audit/result/:audit_id）**

   查询审核的详细结果，接收审核ID作为路径参数，返回规则引擎校验结果和RAG分析结果。

5. **重试审核（POST /api/v1/audit/retry）**

   重试失败的审核，接收审核ID作为参数，重新触发审核流程。

6. **人工审核（POST /api/v1/audit/manual）**

   进行人工审核，接收审核ID、审核结果、审核原因等参数，记录人工审核结果。接口参数如下：

   ```json
   {
     "audit_id": "审核ID",
     "result": "审核结果（pass/reject）",
     "reason": "审核原因"
   }
   ```

7. **获取流程日志（GET /api/v1/audit/flow-logs/:reimbursement_id）**

   查询报销单的审核流程日志，接收报销单ID作为路径参数，返回审核全流程的流转记录。

#### 上传接口

上传接口用于处理报销单和发票的上传，包括以下接口：

1. **上传报销单（POST /api/v1/upload/reimbursement）**

   上传报销单数据，支持JSON格式和表单格式，接收报销单的详细信息，包括申请人、报销类型、金额、日期等字段。接口参数如下：

   ```json
   {
     "user_id": "用户ID",
     "user_name": "用户姓名",
     "department": "所属部门",
     "type": "报销类型",
     "title": "报销标题",
     "description": "报销描述",
     "total_amount": "总金额",
     "apply_date": "申请日期",
     "expense_date": "费用发生日期",
     "destination": "出差目的地",
     "city": "出差城市",
     "province": "出差省份",
     "travel_reason": "出差事由",
     "transportation": "交通工具",
     "project_code": "项目编码",
     "budget_code": "预算科目"
   }
   ```

   接口返回报销单ID和上传状态。

2. **上传发票（POST /api/v1/upload/invoices）**

   上传发票图片，支持单文件和多文件批量上传，接收发票图片文件，调用OCR服务解析发票信息。接口参数采用multipart/form-data格式，包含reimbursement_id字段和invoice_files文件字段。

3. **批量上传（POST /api/v1/upload/batch）**

   批量上传报销单和发票，接收报销单JSON数据和发票图片文件，一次性完成报销单和发票的上传。

4. **触发OCR解析（POST /api/v1/upload/ocr）**

   触发OCR解析，接收发票ID作为参数，调用OCR服务解析发票信息，更新发票数据。

5. **更新发票图片（PUT /api/v1/upload/invoice-image）**

   更新发票图片，接收发票ID和新的发票图片文件，替换原有发票图片。

#### 用户接口

用户接口用于用户登录和用户信息管理，包括以下接口：

1. **用户登录（POST /api/v1/user/login）**

   用户登录，接收用户名和密码，验证用户身份，返回JWT Token。接口参数如下：

   ```json
   {
     "username": "用户名",
     "password": "密码"
   }
   ```

   接口返回Token和用户信息。

2. **获取当前用户（GET /api/v1/user/current）**

   获取当前登录用户的信息，需要携带JWT Token进行身份认证。

#### 规则引擎接口

规则引擎接口用于管理规则引擎的规则和特征，包括以下接口：

1. **创建规则（POST /api/v1/rules）**

   创建规则，接收规则名称、规则描述、条件列表、决策信息等参数。接口参数如下：

   ```json
   {
     "name": "规则名称",
     "description": "规则描述",
     "priority": "优先级",
     "conditions": [
       {
         "feature_id": "特征ID",
         "operator": "操作符",
         "value": "比较值",
         "logic_op": "逻辑操作符"
       }
     ],
     "decision": {
       "type": "决策类型",
       "reason": "决策原因"
     }
   }
   ```

2. **更新规则（PUT /api/v1/rules/:rule_id）**

   更新规则，接收规则ID作为路径参数，接收规则更新数据作为请求体。

3. **删除规则（DELETE /api/v1/rules/:rule_id）**

   删除规则，接收规则ID作为路径参数。

4. **切换规则状态（PUT /api/v1/rules/:rule_id/toggle）**

   切换规则的启用/禁用状态，接收规则ID作为路径参数。

5. **获取规则列表（GET /api/v1/rules）**

   查询规则列表，支持分页和过滤，返回规则列表和总数。

6. **获取规则详情（GET /api/v1/rules/:rule_id）**

   查询规则详情，接收规则ID作为路径参数，返回规则的完整信息，包括条件和决策。

7. **获取特征列表（GET /api/v1/features）**

   查询特征列表，返回规则引擎支持的所有特征。

8. **测试规则（POST /api/v1/rules/test）**

   测试规则，接收规则ID和测试数据，返回规则的测试结果。

#### 知识库接口

知识库接口用于管理知识库文件，包括以下接口：

1. **上传文件（POST /api/v1/knowledge/upload）**

   上传知识库文件，接收文件和文件信息，存储文件并记录文件元数据。接口参数采用multipart/form-data格式，包含file文件字段和file_info表单字段。

2. **获取文件列表（GET /api/v1/knowledge/files）**

   查询知识库文件列表，支持分页和分类过滤，返回文件列表和总数。

3. **获取文件详情（GET /api/v1/knowledge/files/:file_id）**

   查询文件详情，接收文件ID作为路径参数，返回文件的完整信息。

4. **更新文件（PUT /api/v1/knowledge/files/:file_id）**

   更新文件信息，接收文件ID作为路径参数，接收文件更新数据作为请求体。

5. **删除文件（DELETE /api/v1/knowledge/files/:file_id）**

   删除文件，接收文件ID作为路径参数。

6. **下载文件（GET /api/v1/knowledge/files/:file_id/download）**

   下载文件，接收文件ID作为路径参数，返回文件内容。

7. **查看文件（GET /api/v1/knowledge/files/:file_id/view）**

   在线查看文件，接收文件ID作为路径参数，返回文件内容。

#### 查询接口

查询接口用于查询报销单和审核信息，包括以下接口：

1. **获取报销单详情（GET /api/v1/reimbursements/:reimbursement_id）**

   查询报销单详情，接收报销单ID作为路径参数，返回报销单的完整信息，包括发票列表。

2. **获取用户报销单列表（GET /api/v1/reimbursements/user/:user_id）**

   查询用户的报销单列表，接收用户ID作为路径参数，支持分页和日期范围过滤，返回报销单列表和总数。

3. **获取所有报销单列表（GET /api/v1/reimbursements）**

   查询所有报销单列表，支持分页和状态过滤，返回报销单列表和总数。

4. **按日期范围查询报销单（GET /api/v1/reimbursements/date-range）**

   按日期范围查询报销单，接收开始日期和结束日期作为参数，返回报销单列表。

5. **获取审核报告（GET /api/v1/audit/report）**

   获取审核报告，接收日期范围和状态等参数，返回审核统计数据和详细信息。

6. **更新报销单（PUT /api/v1/reimbursements/:reimbursement_id）**

   更新报销单信息，接收报销单ID作为路径参数，接收报销单更新数据作为请求体。

#### 接口安全设计

接口安全设计包括身份认证、授权控制、数据加密、限流保护等方面。

身份认证采用JWT（JSON Web Token），用户登录后服务器生成Token，客户端在后续请求中携带Token进行身份认证。Token包含用户ID、用户名、角色等信息，使用HMAC SHA256算法签名，确保Token的完整性和安全性。

授权控制基于用户角色，区分管理员和普通用户，管理员可以访问所有接口，普通用户只能访问自己的数据和公开数据。接口在处理请求前验证用户权限，确保用户只能访问授权的资源。

数据加密包括传输加密和存储加密。传输加密使用HTTPS协议，确保数据在传输过程中的安全性。存储加密对敏感数据如密码进行加密存储，使用bcrypt算法进行哈希处理，防止密码泄露。

限流保护防止恶意请求和滥用，接口实现了基于令牌桶算法的限流机制，默认每分钟最多60次请求，超过限制的请求返回429状态码。限流保护基于用户IP和用户ID，确保每个用户的请求频率在合理范围内。

#### 接口文档

接口文档采用Swagger/OpenAPI规范，提供了完整的接口说明，包括接口路径、请求方法、请求参数、响应格式、错误码等信息。文档支持在线测试，方便开发人员调试接口。文档地址为/swagger/index.html，访问该地址可以查看所有接口的详细信息。

## 7.2 核心模块实现

### 7.2.1 规则引擎服务实现

规则引擎是报销审核系统的核心组件之一，负责执行刚性规则的校验，确保报销申请符合公司的财务政策和制度要求。规则引擎采用基于特征的条件判断机制，通过定义规则、条件和决策，实现对报销单的自动化审核。

#### 规则引擎架构

规则引擎采用分层架构设计，包括规则定义层、规则执行层、特征计算层和决策输出层。规则定义层负责规则的定义和管理，规则执行层负责规则的加载和执行，特征计算层负责特征值的计算和提取，决策输出层负责决策结果的生成和输出。

规则引擎的核心组件包括：

1. **规则引擎（Engine）**：负责规则的加载、执行和统计管理，提供规则执行的统一入口。

2. **规则服务（RuleEngineService）**：提供规则的CRUD操作，包括规则的创建、更新、删除、查询等，同时提供规则的执行接口。

3. **特征注册表（FeatureRegistry）**：管理规则引擎支持的所有特征，包括特征的元数据、特征值、特征函数等。

4. **特征函数注册表（FeatureFunctionRegistry）**：管理特征函数的注册和调用，支持动态扩展特征函数。

#### 规则定义

规则采用结构化的方式定义，包括规则基本信息、条件列表和决策信息。规则基本信息包括规则名称、规则描述、优先级、启用状态等。条件列表定义了规则需要满足的条件，每个条件包括特征ID、操作符、比较值、逻辑操作符等。决策信息定义了规则命中后的决策类型和决策原因。

规则定义的数据结构如下：

```go
type Rule struct {
    ID          string      `json:"id"`
    Name        string      `json:"name"`
    Description string      `json:"description"`
    Conditions  []Condition `json:"conditions"`
    Decision    Decision    `json:"decision"`
    Priority    int         `json:"priority"`
    Enabled     bool        `json:"enabled"`
    CreatedAt   time.Time   `json:"created_at"`
    UpdatedAt   time.Time   `json:"updated_at"`
}
```

条件定义的数据结构如下：

```go
type Condition struct {
    ID        string `json:"id"`
    RuleID    string `json:"rule_id"`
    FeatureID string `json:"feature_id"`
    Operator  string `json:"operator"`
    Value     string `json:"value"`
    LogicOp   string `json:"logic_op"`
    SortOrder int    `json:"sort_order"`
}
```

决策定义的数据结构如下：

```go
type Decision struct {
    ID        string    `json:"id"`
    RuleID    string    `json:"rule_id"`
    Type      string    `json:"type"`
    Reason    string    `json:"reason"`
    CreatedAt time.Time `json:"created_at"`
    UpdatedAt time.Time `json:"updated_at"`
}
```

#### 规则执行流程

规则引擎的执行流程包括规则加载、特征计算、条件评估、决策生成和结果返回五个步骤。

**1. 规则加载**

规则引擎在初始化时从数据库加载所有启用的规则，将规则缓存到内存中，提高规则执行效率。规则加载过程包括：

- 从数据库查询所有启用的规则
- 加载规则的条件和决策信息
- 将规则存储到内存中的规则映射表
- 初始化规则的统计信息

规则加载的代码实现如下：

```go
func (e *Engine) Initialize(ctx context.Context) error {
    e.logger.WithContext(ctx).Info("初始化规则引擎")

    filter := &RuleFilter{
        Enabled: &[]bool{true}[0],
        Size:    1000,
    }

    rules, _, err := e.ruleRepository.ListRules(ctx, filter)
    if err != nil {
        return fmt.Errorf("获取启用规则失败: %w", err)
    }

    for _, rule := range rules {
        e.loadedRules[rule.ID] = rule
        e.stats[rule.ID] = &EngineStats{
            RuleID: rule.ID,
        }
    }

    e.logger.WithContext(ctx).Info("规则引擎初始化完成",
        logger.NewField("规则数量", len(rules)))

    return nil
}
```

**2. 特征计算**

规则引擎在执行规则前需要计算特征值，特征值可以从报销单数据中直接提取，也可以通过特征函数计算。特征函数支持动态扩展，可以根据业务需求添加新的特征函数。

特征计算的代码实现如下：

```go
for _, condition := range conditions {
    if _, exists := dataMap[condition.FeatureID]; !exists {
        if e.featureFunctionRegistry != nil && e.featureRepository != nil {
            feature, err := e.featureRepository.GetFeatureByID(ctx, condition.FeatureID)
            if err != nil {
                e.logger.WithContext(ctx).Error("获取特征失败",
                    logger.NewField("error", err.Error()),
                    logger.NewField("feature_id", condition.FeatureID))
            } else if feature != nil && feature.FunctionName != "" {
                if featureFunc, exists := e.featureFunctionRegistry.Get(feature.FunctionName); exists {
                    input := &featurefunction.FunctionInput{
                        InvoiceData: dataMap,
                        Config:      feature.FunctionConfig,
                    }
                    output, err := featureFunc.Execute(ctx, input)
                    if err == nil && output.Error == "" {
                        dataMap[condition.FeatureID] = output.Value
                    }
                }
            }
        }
    }
}
```

**3. 条件评估**

规则引擎对每个规则的条件进行评估，根据操作符和比较值判断条件是否满足。支持的操作符包括等于、不等于、大于、小于、大于等于、小于等于、包含、不包含等。

条件评估的代码实现如下：

```go
func (e *Engine) evaluateCondition(ctx context.Context, condition Condition, data interface{}) bool {
    dataMap, ok := data.(map[string]interface{})
    if !ok {
        return false
    }

    value, exists := dataMap[condition.FeatureID]
    if !exists {
        return false
    }

    switch condition.Operator {
    case OperatorEqual:
        return fmt.Sprintf("%v", value) == condition.Value
    case OperatorNotEqual:
        return fmt.Sprintf("%v", value) != condition.Value
    case OperatorGreaterThan:
        return e.compareNumbers(value, condition.Value, ">")
    case OperatorGreaterEqual:
        return e.compareNumbers(value, condition.Value, ">=")
    case OperatorLessThan:
        return e.compareNumbers(value, condition.Value, "<")
    case OperatorLessEqual:
        return e.compareNumbers(value, condition.Value, "<=")
    case OperatorContains:
        return e.containsString(value, condition.Value, true)
    case OperatorNotContains:
        return e.containsString(value, condition.Value, false)
    default:
        return false
    }
}
```

**4. 决策生成**

规则引擎根据条件的评估结果生成决策，如果所有条件都满足，则规则命中，返回决策信息；否则规则未命中，返回未通过信息。

决策生成的代码实现如下：

```go
func (e *Engine) evaluateRule(ctx context.Context, rule *Rule, data interface{}) *RuleValidationResult {
    startTime := time.Now()

    result := &RuleValidationResult{
        RuleID:    rule.ID,
        RuleName:  rule.Name,
        RuleType:  "rule_engine",
        Passed:    true,
        Message:   "规则执行成功",
        Timestamp: time.Now(),
    }

    if len(rule.Conditions) == 0 {
        result.Passed = false
        result.Message = "规则没有条件"
        return result
    }

    passed := e.evaluateConditions(ctx, rule.Conditions, data)
    result.Passed = passed

    if passed {
        result.Message = fmt.Sprintf("规则命中: %s", rule.Decision.Reason)
    } else {
        result.Message = "规则未命中"
    }

    executionTime := time.Since(startTime)
    result.ExecutionTime = executionTime.Milliseconds()

    e.updateStats(rule.ID, passed, executionTime)

    return result
}
```

**5. 结果返回**

规则引擎将所有规则的执行结果返回给调用方，结果包括规则ID、规则名称、规则类型、是否通过、消息、执行时间等信息。

#### 规则引擎统计

规则引擎提供了统计功能，记录每个规则的执行次数、成功次数、失败次数、最后执行时间、平均执行时间等信息，用于规则的性能分析和优化。

统计信息的代码实现如下：

```go
func (e *Engine) updateStats(ruleID string, passed bool, executionTime time.Duration) {
    e.mu.Lock()
    defer e.mu.Unlock()

    stat, exists := e.stats[ruleID]
    if !exists {
        return
    }

    stat.ExecutionCount++
    stat.LastExecution = time.Now()

    if passed {
        stat.SuccessCount++
    } else {
        stat.FailureCount++
    }

    totalTime := stat.AverageTime * time.Duration(stat.ExecutionCount-1)
    stat.AverageTime = (totalTime + executionTime) / time.Duration(stat.ExecutionCount)
}
```

#### 规则引擎特点

规则引擎具有以下特点：

1. **高性能**：规则引擎采用内存缓存机制，规则加载到内存后执行速度快，能够满足高并发审核需求。

2. **灵活性**：规则引擎支持动态配置规则，规则的定义和修改不需要重新部署系统，提高了系统的灵活性。

3. **可扩展性**：规则引擎支持特征函数的动态扩展，可以根据业务需求添加新的特征函数，提高系统的可扩展性。

4. **可维护性**：规则引擎采用结构化的规则定义方式，规则的定义和管理清晰明了，便于维护和优化。

5. **统计分析**：规则引擎提供了统计功能，记录规则的执行情况，便于规则的性能分析和优化。

### 7.2.2 RAG服务实现

RAG（Retrieval-Augmented Generation）服务是报销审核系统的另一个核心组件，负责执行柔性规则的审核，通过检索增强生成的方式，利用大语言模型对报销申请进行智能分析，判断报销申请是否符合公司的财务政策和制度要求。

#### RAG服务架构

RAG服务采用模块化架构设计，包括文档处理模块、向量存储模块、大模型客户端模块、提示词构建模块和检索增强生成模块。文档处理模块负责文档的解析和分片，向量存储模块负责向量的存储和检索，大模型客户端模块负责与大模型的交互，提示词构建模块负责构建提示词，检索增强生成模块负责执行RAG查询和审核。

RAG服务的核心组件包括：

1. **RAG服务（RAGService）**：提供RAG查询和审核的统一接口，协调各个组件完成RAG流程。

2. **文档处理器（DocumentProcessor）**：负责文档的解析、分片和向量化处理。

3. **向量存储（VectorStore）**：负责向量的存储、检索和相似度计算。

4. **大模型客户端（LLMClient）**：负责与大模型的交互，包括文本向量化、对话生成等。

5. **提示词构建器（PromptBuilder）**：负责构建系统提示词和业务提示词。

#### 文档处理

文档处理是RAG服务的基础，负责将报销制度文档解析、分片、向量化后存储到向量数据库中。文档处理包括文档解析、文档分片、向量化生成和向量存储四个步骤。

**1. 文档解析**

文档解析器支持多种文档格式，包括TXT、PDF、Word等，将文档解析为纯文本内容。文档解析器采用流式处理方式，能够处理大文件，避免内存溢出。

**2. 文档分片**

文档分片将文档按照语义单元分割成多个分片，每个分片包含一个完整的语义单元。分片策略包括按段落分片、按句子分片、按字符数分片等，系统采用混合分片策略，优先按段落分片，段落过长时按句子分片，句子过长时按字符数分片。

**3. 向量化生成**

向量化生成使用OpenAI的text-embedding-ada-002模型将文本转换为向量，向量维度为1536。向量化生成采用批量处理方式，提高向量化效率。

**4. 向量存储**

向量存储将生成的向量存储到PostgreSQL数据库中，使用pgvector扩展支持向量类型。向量存储包括向量ID、文档ID、分片ID、向量数据、元数据等字段。

文档处理的代码实现如下：

```go
func (rs *RAGService) IngestDocument(ctx context.Context, documentPath string) (*Document, error) {
    document, err := rs.documentProcessor.ProcessDocument(ctx, documentPath)
    if err != nil {
        rs.logger.Error("处理文档失败", logger.NewField("document_path", documentPath), logger.NewField("error", err))
        return nil, errors.New("处理文档失败")
    }

    for _, chunk := range document.Chunks {
        embedding, err := rs.llmClient.GenerateEmbedding(ctx, chunk.Content)
        if err != nil {
            rs.logger.Error("生成向量失败", logger.NewField("document_id", document.ID), logger.NewField("error", err))
            return nil, errors.New("生成向量失败")
        }

        chunk.Vector = embedding

        err = rs.vectorStore.StoreVector(ctx, &Vector{
            ID:         generateVectorID(),
            DocumentID: document.ID,
            ChunkID:    chunk.ID,
            Values:     embedding,
            Dimension:  len(embedding),
            Metadata: map[string]interface{}{
                "document_title": document.Title,
                "chunk_index":    chunk.StartPos,
            },
            CreatedAt: time.Now(),
            UpdatedAt: time.Now(),
        })
        if err != nil {
            rs.logger.Error("存储向量失败", logger.NewField("document_id", document.ID), logger.NewField("error", err))
            return nil, errors.New("存储向量失败")
        }
    }

    return document, nil
}
```

#### 向量检索

向量检索是RAG服务的核心，负责根据查询向量从向量数据库中检索最相关的文档分片。向量检索采用余弦相似度计算，返回相似度最高的TopK个分片。

向量检索支持纯向量检索和混合检索两种模式。纯向量检索仅使用向量相似度进行检索，混合检索结合向量相似度和关键词匹配，提高检索准确度。

混合检索的代码实现如下：

```go
func (rs *RAGService) HybridSearch(ctx context.Context, query string, topK int, keywordWeight float64) ([]*VectorSearchResult, error) {
    if query == "" {
        rs.logger.Error("查询内容不能为空")
        return nil, errors.New("查询内容不能为空")
    }

    if topK <= 0 {
        topK = 5
    }

    if keywordWeight < 0 || keywordWeight > 1 {
        keywordWeight = 0.5
    }

    embedding, err := rs.llmClient.GenerateEmbedding(ctx, query)
    if err != nil {
        rs.logger.Error("生成查询向量失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("生成查询向量失败")
    }

    keywords := rs.extractKeywords(query)

    results, err := rs.vectorStore.HybridSearch(ctx, embedding, keywords, topK)
    if err != nil {
        rs.logger.Error("混合搜索失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("混合搜索失败")
    }

    return results, nil
}
```

#### 提示词构建

提示词构建是RAG服务的关键，负责构建系统提示词和业务提示词，引导大模型生成符合要求的回答。提示词构建包括系统提示词和业务提示词两部分。

系统提示词定义了大模型的角色和行为规范，确保大模型按照预期的方式工作。业务提示词包含了查询信息和检索到的文档内容，引导大模型基于文档内容生成回答。

提示词构建的代码实现如下：

```go
func (rs *RAGService) AuditReimbursement(ctx context.Context, reimbursementInfo map[string]interface{}, topK int) (*RAGResult, error) {
    startTime := time.Now()

    if len(reimbursementInfo) == 0 {
        rs.logger.Error("报销信息不能为空")
        return nil, errors.New("报销信息不能为空")
    }

    if topK <= 0 {
        topK = 5
    }

    query := rs.buildQueryFromReimbursementInfo(reimbursementInfo)

    embedding, err := rs.llmClient.GenerateEmbedding(ctx, query)
    if err != nil {
        rs.logger.Error("生成查询向量失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("生成查询向量失败")
    }

    keywords := rs.extractReimbursementKeywords(reimbursementInfo)
    searchResults, err := rs.vectorStore.HybridSearch(ctx, embedding, keywords, topK)
    if err != nil {
        rs.logger.Error("混合检索失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("混合检索失败")
    }

    documents := rs.buildDocumentsFromSearchResults(searchResults)

    reimbursementInfoJSON := rs.promptBuilder.FormatReimbursementInfo(reimbursementInfo)
    prompt, err := rs.promptBuilder.BuildAuditPrompt(ctx, reimbursementInfoJSON, documents)
    if err != nil {
        rs.logger.Error("构造提示词失败", logger.NewField("error", err))
        return nil, errors.New("构造提示词失败")
    }

    systemPrompt, err := rs.promptBuilder.BuildSystemPrompt("audit", nil)
    if err != nil {
        rs.logger.Error("构造系统提示词失败", logger.NewField("error", err))
        return nil, errors.New("构造系统提示词失败")
    }

    messages := rs.promptBuilder.BuildConversationMessages(systemPrompt, prompt.Content)

    llmResponse, err := rs.llmClient.Chat(ctx, rs.convertToChatMessages(messages), 0.7, 2000)
    if err != nil {
        rs.logger.Error("调用大模型失败", logger.NewField("error", err))
        return nil, errors.New("调用大模型失败")
    }

    if err := rs.validateLLMResponse(llmResponse); err != nil {
        rs.logger.Error("大模型响应格式校验失败", logger.NewField("error", err))
        return nil, errors.New("大模型响应格式校验失败")
    }

    analysisResult := rs.parseAuditResult(query, llmResponse, searchResults)

    ragResult := &RAGResult{
        Query:          query,
        Documents:      documents,
        Prompt:         prompt.Content,
        Response:       rs.convertToLLMResponse(llmResponse),
        AnalysisResult: analysisResult,
        ExecutionTime:  time.Since(startTime).Milliseconds(),
        CreatedAt:      time.Now(),
    }

    return ragResult, nil
}
```

#### RAG查询

RAG查询是RAG服务的主要功能，负责根据查询内容检索相关文档，并使用大模型生成回答。RAG查询包括查询向量化、向量检索、提示词构建、大模型调用和结果解析五个步骤。

RAG查询的流程如下：

1. **查询向量化**：将查询文本转换为向量，使用OpenAI的text-embedding-ada-002模型。

2. **向量检索**：根据查询向量从向量数据库中检索最相关的文档分片，返回TopK个结果。

3. **提示词构建**：将查询信息和检索到的文档内容构建成提示词。

4. **大模型调用**：调用大模型，传入系统提示词和业务提示词，获取大模型的回答。

5. **结果解析**：解析大模型的回答，提取审核结论、置信度、推理过程等信息。

RAG查询的代码实现如下：

```go
func (rs *RAGService) Query(ctx context.Context, query string, topK int) (*RAGResult, error) {
    startTime := time.Now()

    if query == "" {
        rs.logger.Error("查询内容不能为空")
        return nil, errors.New("查询内容不能为空")
    }

    if topK <= 0 {
        topK = 5
    }

    embedding, err := rs.llmClient.GenerateEmbedding(ctx, query)
    if err != nil {
        rs.logger.Error("生成查询向量失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("生成查询向量失败")
    }

    searchResults, err := rs.vectorStore.SearchVector(ctx, embedding, topK)
    if err != nil {
        rs.logger.Error("搜索相关文档失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("搜索相关文档失败")
    }

    if len(searchResults) == 0 {
        rs.logger.Error("未找到相关文档", logger.NewField("query", query))
        return nil, errors.New("未找到相关文档")
    }

    documents := rs.buildDocumentsFromSearchResults(searchResults)
    chunks := rs.buildChunksFromSearchResults(searchResults)

    prompt, err := rs.promptBuilder.BuildRAGPrompt(ctx, query, documents, chunks)
    if err != nil {
        rs.logger.Error("构造提示词失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("构造提示词失败")
    }

    systemPrompt, err := rs.promptBuilder.BuildSystemPrompt("query", nil)
    if err != nil {
        rs.logger.Error("构造系统提示词失败", logger.NewField("error", err))
        return nil, errors.New("构造系统提示词失败")
    }

    messages := rs.promptBuilder.BuildConversationMessages(systemPrompt, prompt.Content)

    llmResponse, err := rs.llmClient.Chat(ctx, rs.convertToChatMessages(messages), 0.7, 2000)
    if err != nil {
        rs.logger.Error("调用大模型失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("调用大模型失败")
    }

    if err := rs.validateLLMResponse(llmResponse); err != nil {
        rs.logger.Error("大模型响应格式校验失败", logger.NewField("query", query), logger.NewField("error", err))
        return nil, errors.New("大模型响应格式校验失败")
    }

    analysisResult := rs.parseAnalysisResult(query, llmResponse, searchResults)

    ragResult := &RAGResult{
        Query:          query,
        Documents:      documents,
        Chunks:         chunks,
        Prompt:         prompt.Content,
        Response:       rs.convertToLLMResponse(llmResponse),
        AnalysisResult: analysisResult,
        ExecutionTime:  time.Since(startTime).Milliseconds(),
        CreatedAt:      time.Now(),
    }

    return ragResult, nil
}
```

#### RAG服务特点

RAG服务具有以下特点：

1. **智能检索**：RAG服务采用向量检索和关键词检索相结合的混合检索方式，提高了检索的准确度。

2. **灵活扩展**：RAG服务支持多种文档格式和分片策略，可以根据业务需求灵活配置。

3. **可解释性**：RAG服务返回检索到的文档内容和大模型的推理过程，提高了审核结果的可解释性。

4. **高准确度**：RAG服务基于报销制度文档进行审核，确保审核结果的准确性和一致性。

5. **置信度评估**：RAG服务提供置信度评估，帮助用户判断审核结果的可靠性。

### 7.2.3 OCR识别服务实现

OCR识别服务是报销审核系统的基础组件，负责将发票图片转换为结构化的文本信息，为后续的规则引擎和RAG服务提供数据支持。OCR识别服务采用腾讯云OCR API，支持多种发票类型的识别，包括增值税发票、普通发票、电子发票等。

#### OCR服务架构

OCR服务采用分层架构设计，包括OCR客户端层、OCR解析层、数据验证层和数据存储层。OCR客户端层负责与腾讯云OCR API的交互，OCR解析层负责OCR结果的解析和转换，数据验证层负责数据的验证和校验，数据存储层负责数据的持久化存储。

OCR服务的核心组件包括：

1. **OCR解析器（InvoiceParser）**：定义了OCR解析的接口，支持多种OCR提供商。

2. **OCR解析服务（ParserService）**：提供OCR解析的统一接口，协调各个组件完成OCR流程。

3. **OCR提供商（TencentOCR）**：实现腾讯云OCR API的调用，提供发票识别能力。

#### OCR识别流程

OCR识别流程包括图片上传、OCR识别、结果解析、数据验证和数据存储五个步骤。

**1. 图片上传**

图片上传支持单文件和多文件批量上传，上传的图片存储到本地文件系统，记录图片路径和文件信息。

**2. OCR识别**

OCR识别调用腾讯云OCR API，将发票图片转换为结构化的文本信息。腾讯云OCR API支持多种发票类型的识别，包括增值税专用发票、增值税普通发票、电子发票等。

**3. 结果解析**

结果解析将OCR识别的原始结果转换为结构化的发票信息，包括发票代码、发票号码、开票日期、金额、税额、购买方信息、销售方信息、商品明细等。

**4. 数据验证**

数据验证对解析后的发票信息进行验证，包括必填字段验证、数据格式验证、数据逻辑验证等，确保数据的完整性和准确性。

**5. 数据存储**

数据存储将验证后的发票信息存储到数据库中，包括发票基本信息、OCR原始结果、验证状态等。

OCR识别的代码实现如下：

```go
func (s *ParserService) ParseInvoiceImage(ctx context.Context, invoiceID string) error {
    invoice, err := s.repo.GetInvoiceByID(ctx, invoiceID)
    if err != nil {
        s.logger.WithContext(ctx).Error("获取发票信息失败",
            logger.Field{Key: "error", Value: err.Error()},
            logger.Field{Key: "invoice_id", Value: invoiceID})
        return fmt.Errorf("获取发票信息失败: %w", err)
    }

    s.logger.WithContext(ctx).Info("开始解析发票图片",
        logger.Field{Key: "invoice_id", Value: invoiceID},
        logger.Field{Key: "image_path", Value: invoice.ImagePath})

    fullPath := invoice.ImagePath
    if s.basePath != "" {
        fullPath = s.basePath + "/" + invoice.ImagePath
    }

    ocrResult, err := s.parser.ParseInvoice(ctx, fullPath)
    if err != nil {
        s.logger.WithContext(ctx).Error("OCR解析失败",
            logger.Field{Key: "error", Value: err.Error()},
            logger.Field{Key: "invoice_id", Value: invoiceID},
            logger.Field{Key: "image_path", Value: invoice.ImagePath})

        invoice.Status = "解析失败"
        invoice.UpdatedAt = time.Now()
        if updateErr := s.repo.UpdateInvoice(ctx, invoice); updateErr != nil {
            s.logger.WithContext(ctx).Error("更新发票状态失败",
                logger.Field{Key: "error", Value: updateErr.Error()},
                logger.Field{Key: "invoice_id", Value: invoiceID})
        }

        return fmt.Errorf("OCR解析失败: %w", err)
    }

    isValid, errMsg := ocrResult.Validate()
    if !isValid {
        s.logger.WithContext(ctx).Warn("OCR解析结果验证失败",
            logger.Field{Key: "error", Value: errMsg},
            logger.Field{Key: "invoice_id", Value: invoiceID})

        invoice.Status = "无效"
        invoice.UpdatedAt = time.Now()
        if updateErr := s.repo.UpdateInvoice(ctx, invoice); updateErr != nil {
            s.logger.WithContext(ctx).Error("更新发票状态失败",
                logger.Field{Key: "error", Value: updateErr.Error()},
                logger.Field{Key: "invoice_id", Value: invoiceID})
        }

        return fmt.Errorf("OCR解析结果验证失败: %s", errMsg)
    }

    s.updateInvoiceFromOCR(ctx, invoice, ocrResult)
    invoice.Status = "已识别"
    invoice.UpdatedAt = time.Now()

    if err := s.repo.UpdateInvoice(ctx, invoice); err != nil {
        s.logger.WithContext(ctx).Error("更新发票信息失败",
            logger.Field{Key: "error", Value: err.Error()},
            logger.Field{Key: "invoice_id", Value: invoiceID})
        return fmt.Errorf("更新发票信息失败: %w", err)
    }

    s.logger.WithContext(ctx).Info("发票解析完成",
        logger.Field{Key: "invoice_id", Value: invoiceID},
        logger.Field{Key: "invoice_code", Value: invoice.Code},
        logger.Field{Key: "invoice_number", Value: invoice.Number},
        logger.Field{Key: "amount", Value: invoice.Amount})

    return nil
}
```

#### 智能推断

OCR识别服务提供了智能推断功能，根据OCR识别的结果推断发票的扩展信息，包括发票类别、商户类型、增值税率、是否电子发票等。

**1. 发票类别推断**

发票类别推断根据发票类型、销售方名称、商品名称等信息推断发票的类别，包括差旅费、招待费、办公费、培训费、会议费、通信费等。

发票类别推断的代码实现如下：

```go
func (s *ParserService) inferInvoiceCategory(invoice *Invoice, ocrResult *InvoiceInfo) {
    invoiceType := invoice.Category
    if invoiceType == "" {
        invoiceType = ocrResult.InvoiceType
    }

    sellerName := ocrResult.SellerName
    mainCommodity := invoice.MainCommodity

    switch {
    case strings.Contains(invoiceType, "住宿") || strings.Contains(sellerName, "酒店") || strings.Contains(sellerName, "宾馆") || strings.Contains(mainCommodity, "住宿"):
        invoice.Category = "差旅费"
        invoice.SubCategory = "住宿费"
    case strings.Contains(invoiceType, "交通") || strings.Contains(sellerName, "航空") || strings.Contains(sellerName, "铁路") || strings.Contains(sellerName, "客运") || strings.Contains(mainCommodity, "机票") || strings.Contains(mainCommodity, "火车票"):
        invoice.Category = "差旅费"
        invoice.SubCategory = "交通费"
    case strings.Contains(invoiceType, "餐饮") || strings.Contains(sellerName, "餐厅") || strings.Contains(sellerName, "饭店") || strings.Contains(sellerName, "美食") || strings.Contains(mainCommodity, "餐饮"):
        invoice.Category = "招待费"
        invoice.SubCategory = "餐饮费"
    case strings.Contains(invoiceType, "办公用品") || strings.Contains(sellerName, "文具") || strings.Contains(sellerName, "办公") || strings.Contains(mainCommodity, "办公用品"):
        invoice.Category = "办公费"
        invoice.SubCategory = "办公用品"
    case strings.Contains(invoiceType, "培训") || strings.Contains(sellerName, "培训") || strings.Contains(sellerName, "教育") || strings.Contains(mainCommodity, "培训"):
        invoice.Category = "培训费"
        invoice.SubCategory = "培训费"
    case strings.Contains(invoiceType, "会议") || strings.Contains(sellerName, "会议") || strings.Contains(mainCommodity, "会议"):
        invoice.Category = "会议费"
        invoice.SubCategory = "会议费"
    case strings.Contains(invoiceType, "通信") || strings.Contains(sellerName, "通信") || strings.Contains(sellerName, "电信") || strings.Contains(mainCommodity, "话费"):
        invoice.Category = "通信费"
        invoice.SubCategory = "通信费"
    default:
        invoice.Category = "其他费用"
        invoice.SubCategory = "其他"
    }
}
```

**2. 商户类型推断**

商户类型推断根据销售方名称推断商户的类型，包括酒店、餐厅、航空公司、铁路公司、超市、加油站、医疗机构、银行等。

商户类型推断的代码实现如下：

```go
func (s *ParserService) inferMerchantType(invoice *Invoice, ocrResult *InvoiceInfo) {
    sellerName := ocrResult.SellerName

    switch {
    case strings.Contains(sellerName, "酒店") || strings.Contains(sellerName, "宾馆") || strings.Contains(sellerName, "旅馆"):
        invoice.MerchantType = "酒店"
    case strings.Contains(sellerName, "餐厅") || strings.Contains(sellerName, "饭店") || strings.Contains(sellerName, "美食"):
        invoice.MerchantType = "餐厅"
    case strings.Contains(sellerName, "航空") || strings.Contains(sellerName, "机场"):
        invoice.MerchantType = "航空公司"
    case strings.Contains(sellerName, "铁路") || strings.Contains(sellerName, "高铁"):
        invoice.MerchantType = "铁路公司"
    case strings.Contains(sellerName, "超市") || strings.Contains(sellerName, "商场"):
        invoice.MerchantType = "超市"
    case strings.Contains(sellerName, "加油站") || strings.Contains(sellerName, "石油"):
        invoice.MerchantType = "加油站"
    case strings.Contains(sellerName, "医院") || strings.Contains(sellerName, "诊所"):
        invoice.MerchantType = "医疗机构"
    case strings.Contains(sellerName, "银行"):
        invoice.MerchantType = "银行"
    }
}
```

**3. 增值税率推断**

增值税率推断根据发票金额和税额计算增值税率，用于后续的规则校验。

增值税率推断的代码实现如下：

```go
func (s *ParserService) inferVATRate(invoice *Invoice, ocrResult *InvoiceInfo) {
    if invoice.Amount > 0 && invoice.TaxAmount > 0 {
        invoice.VATRate = (invoice.TaxAmount / invoice.Amount) * 100
    }
}
```

**4. 电子发票推断**

电子发票推断根据发票类型判断是否为电子发票，用于后续的规则校验。

电子发票推断的代码实现如下：

```go
func (s *ParserService) inferIsElectronic(invoice *Invoice, ocrResult *InvoiceInfo) {
    invoiceType := ocrResult.InvoiceType
    invoice.IsElectronic = strings.Contains(invoiceType, "电子") || strings.Contains(invoiceType, "数电")
}
```

#### OCR服务特点

OCR服务具有以下特点：

1. **高准确度**：OCR服务采用腾讯云OCR API，识别准确率高，能够满足报销审核的需求。

2. **多格式支持**：OCR服务支持多种发票类型的识别，包括增值税发票、普通发票、电子发票等。

3. **智能推断**：OCR服务提供智能推断功能，根据OCR识别的结果推断发票的扩展信息。

4. **数据验证**：OCR服务提供数据验证功能，确保数据的完整性和准确性。

5. **批量处理**：OCR服务支持批量处理，能够同时处理多张发票图片，提高处理效率。

### 7.2.4 审核服务实现

审核服务是报销审核系统的核心组件，负责协调规则引擎、RAG服务和OCR识别服务，完成报销单的智能审核。审核服务采用双层审核机制，先执行规则引擎的刚性规则校验，再执行RAG服务的柔性规则审核，最后综合两个审核结果生成最终的审核结论。

#### 审核服务架构

审核服务采用分层架构设计，包括审核协调层、规则校验层、RAG分析层、结果综合层和流程管理层。审核协调层负责协调各个组件完成审核流程，规则校验层负责规则引擎的调用和结果处理，RAG分析层负责RAG服务的调用和结果处理，结果综合层负责综合两个审核结果生成最终结论，流程管理层负责审核流程的状态管理和流程日志记录。

审核服务的核心组件包括：

1. **审核服务（Service）**：提供审核的统一接口，协调各个组件完成审核流程。

2. **审核应用服务（AuditApplicationService）**：提供审核的应用层接口，处理HTTP请求和响应。

3. **审核仓储（Repository）**：负责审核数据的持久化存储和查询。

#### 审核流程

审核流程包括审核启动、规则校验、RAG分析、结果综合和流程完成五个步骤。

**1. 审核启动**

审核启动接收报销单ID，创建审核记录，更新报销单状态为审核中，记录流程日志。

审核启动的代码实现如下：

```go
func (s *Service) StartAudit(ctx context.Context, reimbursementID string) (*AuditResult, error) {
    startTime := time.Now()

    s.logger.WithContext(ctx).Info("开始审核", logger.NewField("reimbursement_id", reimbursementID))

    reimbursement, err := s.reimbursementRepo.GetReimbursementByID(ctx, reimbursementID)
    reimbursement.Invoices, err = s.reimbursementRepo.GetInvoicesByReimbursementID(ctx, reimbursementID)
    if err != nil {
        s.logger.WithContext(ctx).Error("获取报销单失败", logger.NewField("error", err))
        return nil, fmt.Errorf("获取报销单失败: %w", err)
    }

    audit := &AuditResult{
        ID:              uuid.New().String(),
        ReimbursementID: reimbursementID,
        Status:          AuditStatusRunning,
        WorkflowStatus:  WorkflowStatusSubmitted,
        StartedAt:       startTime,
        CreatedAt:       startTime,
        UpdatedAt:       startTime,
    }

    if err := s.repo.CreateAudit(ctx, audit); err != nil {
        s.logger.WithContext(ctx).Error("创建审核记录失败", logger.NewField("error", err))
        return nil, fmt.Errorf("创建审核记录失败: %w", err)
    }

    reimbursement.Status = "auditing"
    reimbursement.AuditID = audit.ID
    if err := s.reimbursementRepo.UpdateReimbursement(ctx, reimbursement); err != nil {
        s.logger.WithContext(ctx).Error("更新报销单状态失败", logger.NewField("error", err))
        return nil, fmt.Errorf("更新报销单状态失败: %w", err)
    }

    startFlowLog := &AuditFlowLog{
        ID:              uuid.New().String(),
        ReimbursementID: reimbursementID,
        AuditID:         audit.ID,
        FlowStatus:      FlowStatusIntelligentStart,
        FlowType:        FlowTypeIntelligent,
        Action:          FlowActionStartAudit,
        CreatedAt:       startTime,
    }

    if err := s.repo.CreateFlowLog(ctx, startFlowLog); err != nil {
        s.logger.WithContext(ctx).Error("创建流程日志失败", logger.NewField("error", err))
    }

    return audit, nil
}
```

**2. 规则校验**

规则校验调用规则引擎对报销单的每个发票进行规则校验，记录规则校验结果。规则校验包括特征计算、条件评估、决策生成等步骤。

规则校验的代码实现如下：

```go
func (s *Service) executeRuleValidation(ctx context.Context, reimbursement *reimbursement.Reimbursement) ([]*RuleValidationResult, error) {
    s.logger.WithContext(ctx).Info("开始规则校验",
        logger.NewField("reimbursement_id", reimbursement.ID),
        logger.NewField("invoice_count", len(reimbursement.Invoices)))

    allResults := make([]*RuleValidationResult, 0)

    for _, invoice := range reimbursement.Invoices {
        s.logger.WithContext(ctx).Info("开始校验发票",
            logger.NewField("发票ID", invoice.ID),
            logger.NewField("发票号码", invoice.Number))

        validationData := map[string]interface{}{
            "invoice":                             invoice,
            "reimbursement":                       reimbursement,
            "apply_date":                          reimbursement.ApplyDate,
            "is_invoice_date_older_than_6_months": invoice.Date != nil && invoice.Date.Before(reimbursement.ApplyDate.AddDate(0, -6, 0)),
            "feat-invoice-date-validity":          invoice.Date != nil && invoice.Date.Before(reimbursement.ApplyDate.AddDate(0, -6, 0)),
            "price":                               invoice.Price,
        }

        invoiceMap := map[string]interface{}{
            "id":                  invoice.ID,
            "type":                invoice.Type,
            "code":                invoice.Code,
            "number":              invoice.Number,
            "date":                invoice.Date,
            "amount":              invoice.Amount,
            "tax_amount":          invoice.TaxAmount,
            "total_amount":        invoice.Amount + invoice.TaxAmount,
            "buyer_name":          invoice.BuyerName,
            "buyer_tax_no":        invoice.BuyerTaxNo,
            "seller_name":         invoice.SellerName,
            "seller_tax_no":       invoice.SellerTaxNo,
            "commodity_name":      invoice.CommodityName,
            "specification":       invoice.Specification,
            "unit":                invoice.Unit,
            "quantity":            invoice.Quantity,
            "price":               invoice.Price,
            "category":            invoice.Category,
            "sub_category":        invoice.SubCategory,
            "expense_type":        invoice.ExpenseType,
            "merchant_type":       invoice.MerchantType,
            "merchant_code":       invoice.MerchantCode,
            "location":            invoice.Location,
            "city":                invoice.City,
            "province":            invoice.Province,
            "country":             invoice.Country,
            "purpose":             invoice.Purpose,
            "description":         invoice.Description,
            "project_code":        invoice.ProjectCode,
            "department_code":     invoice.DepartmentCode,
            "cost_center":         invoice.CostCenter,
            "contract_number":     invoice.ContractNumber,
            "approval_level":      invoice.ApprovalLevel,
            "is_reimbursable":     invoice.IsReimbursable,
            "is_personal":         invoice.IsPersonal,
            "is_vat":              invoice.IsVAT,
            "vat_rate":            invoice.VATRate,
            "exchange_rate":       invoice.ExchangeRate,
            "original_amount":     invoice.OriginalAmount,
            "original_currency":   invoice.OriginalCurrency,
            "is_electronic":       invoice.IsElectronic,
            "is_duplicate":        invoice.IsDuplicate,
            "verification_status": invoice.VerificationStatus,
            "items":               invoice.Items,
            "total_items":         invoice.TotalItems,
            "main_commodity":      invoice.MainCommodity,
        }

        for k, v := range invoiceMap {
            validationData[k] = v
        }

        validationData["a63c79a1-902d-4c94-b8fa-d69784bb4761"] = invoice.Amount
        validationData["7184644e-7be8-46d8-8a2c-54769982d0b0"] = invoice.SubCategory
        validationData["4f99ee85-8817-4989-b162-4496eb15bc64"] = invoice.Category

        validationData["transport_type"] = reimbursement.Transportation
        validationData["employee_level"] = reimbursement.ApplicantLevel
        validationData["trip_count"] = float64(1)
        validationData["days"] = 1.0
        validationData["amount"] = invoice.Amount

        validationData["invoice_code"] = invoice.Code
        validationData["invoice_number"] = invoice.Number
        validationData["invoice_type"] = invoice.Type
        validationData["commodity_name"] = invoice.CommodityName
        validationData["merchant_type"] = invoice.MerchantType
        validationData["image_url"] = invoice.ImagePath

        results, err := s.ruleEngineService.ExecuteAllRules(ctx, validationData)
        if err != nil {
            s.logger.WithContext(ctx).Error("规则校验失败",
                logger.NewField("发票ID", invoice.ID),
                logger.NewField("error", err))
            return nil, err
        }

        for _, result := range results {
            allResults = append(allResults, &RuleValidationResult{
                RuleID:        result.RuleID,
                RuleCode:      result.RuleID,
                RuleName:      result.RuleName,
                RuleType:      result.RuleType,
                Passed:        result.Passed,
                Message:       result.Message,
                Details:       map[string]interface{}{"details": result.Details, "invoice_id": invoice.ID},
                ExecutionTime: result.ExecutionTime,
            })
        }
    }

    s.logger.WithContext(ctx).Info("规则校验完成", logger.NewField("result_count", len(allResults)))

    return allResults, nil
}
```

**3. RAG分析**

RAG分析调用RAG服务对报销单进行柔性规则审核，记录RAG分析结果。RAG分析包括查询构建、向量检索、提示词构建、大模型调用、结果解析等步骤。

RAG分析的代码实现如下：

```go
func (s *Service) executeRAGAnalysis(ctx context.Context, reimbursementInfo map[string]interface{}) (*RAGAnalysisResult, error) {
    s.logger.WithContext(ctx).Info("开始RAG分析")

    result, err := s.ragService.AuditReimbursement(ctx, reimbursementInfo, 5)
    if err != nil {
        s.logger.WithContext(ctx).Error("RAG分析失败", logger.NewField("error", err))
        return nil, err
    }

    ragResult := &RAGAnalysisResult{
        Query:         result.Query,
        Content:       result.AnalysisResult.Conclusion,
        Confidence:    result.AnalysisResult.Confidence,
        Analysis:      result.AnalysisResult.Reasoning,
        ExecutionTime: result.ExecutionTime,
        Chunks:        result.Chunks,
    }

    for _, doc := range result.Documents {
        for _, chunk := range doc.Chunks {
            ragResult.References = append(ragResult.References, &VectorReference{
                ChunkID:    chunk.ID,
                Content:    chunk.Content,
                Similarity: 0.0,
                Category:   doc.Metadata.Category,
                DocumentID: doc.ID,
            })
        }
    }

    s.logger.WithContext(ctx).Info("RAG分析完成", logger.NewField("confidence", ragResult.Confidence))

    return ragResult, nil
}
```

**4. 结果综合**

结果综合综合规则校验结果和RAG分析结果，生成最终的审核结论。结果综合包括风险分数计算、风险等级确定、建议生成、审核原因生成等步骤。

结果综合的代码实现如下：

```go
func (s *Service) calculateRiskScore(audit *AuditResult) float64 {
    riskScore := 0.0

    if !audit.RulePass {
        riskScore += 0.5
    }

    if !audit.RAGPass {
        riskScore += 0.3
    }

    if audit.RAGResults != nil {
        riskScore += (1.0 - audit.RAGResults.Confidence) * 0.2
    }

    if riskScore > 1.0 {
        riskScore = 1.0
    }

    return riskScore
}

func (s *Service) determineRiskLevel(riskScore float64) string {
    if riskScore >= 0.7 {
        return "高风险"
    } else if riskScore >= 0.4 {
        return "中风险"
    } else {
        return "低风险"
    }
}

func (s *Service) generateSuggestions(audit *AuditResult) []string {
    var suggestions []string

    if !audit.RulePass {
        suggestions = append(suggestions, "请检查规则校验不通过的项目")
        for _, result := range audit.RuleResults {
            if !result.Passed {
                suggestions = append(suggestions, fmt.Sprintf("- %s: %s", result.RuleName, result.Message))
            }
        }
    }

    if !audit.RAGPass && audit.RAGResults != nil {
        suggestions = append(suggestions, "请检查RAG分析结果，建议人工复核")
    }

    if audit.RiskLevel == "高风险" {
        suggestions = append(suggestions, "该报销单风险较高，建议进行详细审核")
    }

    if len(suggestions) == 0 {
        suggestions = append(suggestions, "审核通过，可以继续后续流程")
    }

    return suggestions
}

func (s *Service) generateAuditReason(audit *AuditResult) string {
    if audit.FinalPass {
        return "审核通过"
    }

    var reasons []string

    if !audit.RulePass {
        var hitRules []string
        for _, result := range audit.RuleResults {
            if result.Passed {
                hitRules = append(hitRules, result.RuleName)
            }
        }
        if len(hitRules) > 0 {
            reasons = append(reasons, "规则校验未通过: "+strings.Join(hitRules, ", "))
        } else {
            reasons = append(reasons, "规则校验未通过")
        }
    }

    if !audit.RAGPass {
        reasons = append(reasons, "RAG分析未通过")
    }

    if len(reasons) == 0 {
        return "审核未通过"
    }

    return "审核未通过: " + strings.Join(reasons, "; ")
}
```

**5. 流程完成**

流程完成更新审核记录和报销单状态，记录流程日志。如果审核通过，报销单状态更新为审核中，等待人工审核；如果审核不通过，报销单状态更新为已驳回。

流程完成的代码实现如下：

```go
completedTime := time.Now()
audit.CompletedAt = &completedTime
audit.Duration = completedTime.Sub(startTime).Milliseconds()
audit.UpdatedAt = completedTime
audit.Status = AuditStatusCompleted

if audit.FinalPass {
    audit.WorkflowStatus = WorkflowStatusRAGPassed
    audit.Reason = "智能审核通过，等待人工审核"
    reimbursement.Status = "auditing"
} else {
    audit.WorkflowStatus = WorkflowStatusRAGFailed
    reimbursement.Status = "rejected"
}

if err := s.reimbursementRepo.UpdateReimbursement(ctx, reimbursement); err != nil {
    s.logger.WithContext(ctx).Error("更新报销单状态失败", logger.NewField("error", err))
}

if err := s.repo.UpdateAudit(ctx, audit); err != nil {
    s.logger.WithContext(ctx).Error("更新审核记录失败", logger.NewField("error", err))
}

var flowStatus FlowStatus
var flowAction FlowAction
if audit.FinalPass {
    flowStatus = FlowStatusIntelligentPass
    flowAction = FlowActionPassAudit
} else {
    flowStatus = FlowStatusIntelligentFail
    flowAction = FlowActionRejectAudit
}

endFlowLog := &AuditFlowLog{
    ID:              uuid.New().String(),
    ReimbursementID: reimbursementID,
    AuditID:         audit.ID,
    FlowStatus:      flowStatus,
    FlowType:        FlowTypeIntelligent,
    Action:          flowAction,
    Reason:          &audit.Reason,
    CreatedAt:       completedTime,
}
if err := s.repo.CreateFlowLog(ctx, endFlowLog); err != nil {
    s.logger.WithContext(ctx).Error("创建流程日志失败", logger.NewField("error", err))
}
```

#### 人工审核

人工审核是对智能审核的补充，当智能审核通过后，人工审核员可以对报销单进行人工审核，最终决定是否通过报销单。人工审核包括审核启动、审核决策、流程完成等步骤。

人工审核的代码实现如下：

```go
func (s *Service) ManualAudit(ctx context.Context, auditID string, action string, reason string, operatorID string, operatorName string, ipAddress string) (*AuditResult, error) {
    audit, err := s.repo.GetAuditByID(ctx, auditID)
    if err != nil {
        return nil, fmt.Errorf("获取审核记录失败: %w", err)
    }

    if audit.Status != AuditStatusCompleted {
        return nil, errors.New("只能审核已完成的智能审核")
    }

    if !audit.FinalPass {
        return nil, errors.New("智能审核未通过，无法进行人工审核")
    }

    reimbursement, err := s.reimbursementRepo.GetReimbursementByID(ctx, audit.ReimbursementID)
    if err != nil {
        return nil, fmt.Errorf("获取报销单失败: %w", err)
    }

    now := time.Now()
    var flowStatus FlowStatus
    var flowAction FlowAction

    startFlowLog := &AuditFlowLog{
        ID:              uuid.New().String(),
        ReimbursementID: audit.ReimbursementID,
        AuditID:         audit.ID,
        FlowStatus:      FlowStatusManualStart,
        FlowType:        FlowTypeManual,
        OperatorID:      &operatorID,
        OperatorName:    &operatorName,
        Action:          FlowActionStartAudit,
        IPAddress:       &ipAddress,
        CreatedAt:       now,
    }

    if err := s.repo.CreateFlowLog(ctx, startFlowLog); err != nil {
        s.logger.WithContext(ctx).Error("创建流程日志失败", logger.NewField("error", err))
    }

    if action == "pass" {
        flowStatus = FlowStatusManualPass
        flowAction = FlowActionPassAudit
        audit.Status = AuditStatusApproved
        audit.WorkflowStatus = WorkflowStatusManualPassed
        audit.FinalPass = true
        audit.Reason = "人工审核通过"
        if reason != "" {
            audit.Reason += ": " + reason
        }

        reimbursement.Status = "approved"
        if err := s.reimbursementRepo.UpdateReimbursement(ctx, reimbursement); err != nil {
            s.logger.WithContext(ctx).Error("更新报销单状态失败", logger.NewField("error", err))
        }
    } else if action == "reject" {
        flowStatus = FlowStatusManualReject
        flowAction = FlowActionRejectAudit
        audit.Status = AuditStatusRejected
        audit.WorkflowStatus = WorkflowStatusManualRejected
        audit.FinalPass = false
        audit.Reason = "人工审核驳回"
        if reason != "" {
            audit.Reason += ": " + reason
        }

        reimbursement.Status = "rejected"
        if err := s.reimbursementRepo.UpdateReimbursement(ctx, reimbursement); err != nil {
            s.logger.WithContext(ctx).Error("更新报销单状态失败", logger.NewField("error", err))
        }
    } else {
        return nil, errors.New("无效的审核动作")
    }

    audit.UpdatedAt = now

    if err := s.repo.UpdateAudit(ctx, audit); err != nil {
        s.logger.WithContext(ctx).Error("更新审核记录失败", logger.NewField("error", err))
        return nil, fmt.Errorf("更新审核记录失败: %w", err)
    }

    flowLog := &AuditFlowLog{
        ID:              uuid.New().String(),
        ReimbursementID: audit.ReimbursementID,
        AuditID:         audit.ID,
        FlowStatus:      flowStatus,
        FlowType:        FlowTypeManual,
        OperatorID:      &operatorID,
        OperatorName:    &operatorName,
        Action:          flowAction,
        Reason:          &reason,
        IPAddress:       &ipAddress,
        CreatedAt:       now,
    }

    if err := s.repo.CreateFlowLog(ctx, flowLog); err != nil {
        s.logger.WithContext(ctx).Error("创建流程日志失败", logger.NewField("error", err))
        return nil, fmt.Errorf("创建流程日志失败: %w", err)
    }

    s.logger.WithContext(ctx).Info("人工审核完成",
        logger.NewField("audit_id", audit.ID),
        logger.NewField("action", action),
        logger.NewField("operator_id", operatorID))

    return audit, nil
}
```

#### 审核服务特点

审核服务具有以下特点：

1. **双层审核**：审核服务采用双层审核机制，先执行规则引擎的刚性规则校验，再执行RAG服务的柔性规则审核，提高了审核的准确性和可靠性。

2. **智能综合**：审核服务智能综合规则校验结果和RAG分析结果，生成最终的审核结论，包括风险分数、风险等级、审核建议等。

3. **流程管理**：审核服务提供完整的流程管理功能，包括审核启动、状态更新、流程日志记录等，确保审核流程的可追溯性。

4. **人工审核**：审核服务支持人工审核，对智能审核的结果进行复核，提高了审核的准确性。

5. **可扩展性**：审核服务采用模块化设计，各个组件独立封装，便于扩展和维护。

## 7.3 关键技术实现细节

### 7.3.1 特征函数注册机制

特征函数注册机制是规则引擎的核心技术之一，用于实现特征函数的动态注册、管理和调用。特征函数注册机制采用注册表模式，通过统一的接口管理所有特征函数，支持特征函数的动态扩展和配置。

#### 特征函数接口设计

特征函数接口定义了特征函数的标准接口，包括获取函数代码、函数名称、函数描述、配置模式和执行方法。所有特征函数都必须实现这个接口，确保接口的一致性。

特征函数接口的定义如下：

```go
type FeatureFunction interface {
    GetCode() string
    GetName() string
    GetDescription() string
    GetConfigSchema() *ConfigSchema
    Execute(ctx context.Context, input *FunctionInput) (*FunctionOutput, error)
    Validate(config map[string]interface{}) error
}
```

GetCode方法返回特征函数的唯一标识符，用于在注册表中查找和调用特征函数。GetName方法返回特征函数的名称，用于显示和日志记录。GetDescription方法返回特征函数的描述，说明函数的功能和用途。GetConfigSchema方法返回特征函数的配置模式，定义了函数需要的配置参数。Execute方法执行特征函数，接收输入参数，返回输出结果。Validate方法验证配置参数的有效性。

#### 特征函数注册表

特征函数注册表使用map数据结构存储所有注册的特征函数，使用sync.RWMutex实现并发安全。注册表提供了注册、获取、列表查询、获取配置模式等方法。

特征函数注册表的定义如下：

```go
type FunctionRegistry struct {
    functions map[string]FeatureFunction
    mu        sync.RWMutex
}
```

注册方法将特征函数注册到注册表中，使用函数代码作为键。注册方法会检查函数代码是否为空，以及函数是否已经注册，确保注册的正确性。

注册方法的实现如下：

```go
func (r *FunctionRegistry) Register(fn FeatureFunction) error {
    r.mu.Lock()
    defer r.mu.Unlock()
    
    code := fn.GetCode()
    if code == "" {
        return fmt.Errorf("function code cannot be empty")
    }
    
    if _, exists := r.functions[code]; exists {
        return fmt.Errorf("function %s already registered", code)
    }
    
    r.functions[code] = fn
    return nil
}
```

获取方法根据函数代码从注册表中获取特征函数，使用读锁确保并发安全。如果函数不存在，返回false表示未找到。

获取方法的实现如下：

```go
func (r *FunctionRegistry) Get(code string) (FeatureFunction, bool) {
    r.mu.RLock()
    defer r.mu.RUnlock()
    
    fn, exists := r.functions[code]
    return fn, exists
}
```

列表查询方法返回所有注册的特征函数，使用读锁确保并发安全。列表查询方法用于显示所有可用的特征函数，便于用户选择和配置。

列表查询方法的实现如下：

```go
func (r *FunctionRegistry) List() []FeatureFunction {
    r.mu.RLock()
    defer r.mu.RUnlock()
    
    list := make([]FeatureFunction, 0, len(r.functions))
    for _, fn := range r.functions {
        list = append(list, fn)
    }
    return list
}
```

#### 特征函数实现

系统实现了多种特征函数，包括P图检测函数、商户类型验证函数、发票代码号码验证函数、发票类型验证函数、发票金额范围函数、图片质量函数、发票代码长度函数、发票欺诈检测函数、商品名称合规函数、发票号码格式函数、发票日期有效性函数、智能住宿验证函数、加班验证函数、招待费验证函数、出差时长验证函数等。

以P图检测函数为例，该函数使用大模型检测发票图片是否存在P图痕迹。函数的配置包括模型选择、置信度阈值、检测提示词等参数。

P图检测函数的实现如下：

```go
type DetectPhotoshopFunction struct {
    client *http.Client
}

func (f *DetectPhotoshopFunction) GetCode() string {
    return "detect_photoshop"
}

func (f *DetectPhotoshopFunction) GetName() string {
    return "detect_photoshop"
}

func (f *DetectPhotoshopFunction) GetDescription() string {
    return "使用大模型检测发票图片是否存在P图痕迹"
}

func (f *DetectPhotoshopFunction) GetConfigSchema() *ConfigSchema {
    return &ConfigSchema{
        Fields: []FieldConfig{
            {
                Name:     "model",
                Type:     "select",
                Label:    "模型选择",
                Required: true,
                Default:  "gpt-4-vision",
                Options: []Option{
                    {Label: "GPT-4 Vision", Value: "gpt-4-vision"},
                    {Label: "GPT-4o", Value: "gpt-4o"},
                    {Label: "Claude 3.5 Sonnet", Value: "claude-3-5-sonnet"},
                },
                Description: "选择用于检测的AI模型",
            },
            {
                Name:        "threshold",
                Type:        "number",
                Label:       "置信度阈值",
                Required:    true,
                Default:     0.7,
                Description: "判断为P图的最低置信度（0-1）",
            },
            {
                Name:        "prompt",
                Type:        "string",
                Label:       "检测提示词",
                Required:    true,
                Default:     "请仔细分析这张发票图片，判断是否存在明显的P图痕迹、篡改或伪造。请从以下几个方面分析：1. 文字是否清晰自然 2. 数字和金额是否一致 3. 印章和签名是否真实 4. 整体布局是否合理。请给出你的判断结果和置信度（0-1之间）。",
                Description: "用于指导大模型检测的提示词",
            },
        },
    }
}
```

#### 特征函数调用

规则引擎在执行规则时，如果条件中的特征值不存在，会自动调用特征函数计算特征值。特征函数的调用过程包括获取特征信息、获取特征函数、构建输入参数、执行函数、处理输出结果等步骤。

特征函数调用的实现如下：

```go
for _, condition := range conditions {
    if _, exists := dataMap[condition.FeatureID]; !exists {
        if e.featureFunctionRegistry != nil && e.featureRepository != nil {
            feature, err := e.featureRepository.GetFeatureByID(ctx, condition.FeatureID)
            if err != nil {
                e.logger.WithContext(ctx).Error("获取特征失败",
                    logger.NewField("error", err.Error()),
                    logger.NewField("feature_id", condition.FeatureID))
            } else if feature == nil {
                e.logger.WithContext(ctx).Error("特征不存在",
                    logger.NewField("feature_id", condition.FeatureID))
            } else if feature.FunctionName == "" {
                e.logger.WithContext(ctx).Debug("特征没有配置函数，跳过处理",
                    logger.NewField("feature_id", condition.FeatureID))
            } else {
                if featureFunc, exists := e.featureFunctionRegistry.Get(feature.FunctionName); exists {
                    input := &featurefunction.FunctionInput{
                        InvoiceData: dataMap,
                        Config:      feature.FunctionConfig,
                    }
                    output, err := featureFunc.Execute(ctx, input)
                    if err != nil {
                        e.logger.WithContext(ctx).Error("特征函数执行失败",
                            logger.NewField("error", err.Error()),
                            logger.NewField("function", feature.FunctionName),
                            logger.NewField("feature_id", condition.FeatureID))
                    } else if output.Error != "" {
                        e.logger.WithContext(ctx).Error("特征函数返回错误",
                            logger.NewField("error", output.Error),
                            logger.NewField("function", feature.FunctionName),
                            logger.NewField("feature_id", condition.FeatureID))
                    } else {
                        value := output.Value
                        if valueMap, ok := value.(map[string]interface{}); ok {
                            if approved, exists := valueMap["approved"]; exists {
                                dataMap[condition.FeatureID] = approved
                            } else {
                                dataMap[condition.FeatureID] = value
                            }
                        } else {
                            dataMap[condition.FeatureID] = value
                        }
                        e.logger.WithContext(ctx).Info("特征函数执行成功",
                            logger.NewField("function", feature.FunctionName),
                            logger.NewField("feature_id", condition.FeatureID),
                            logger.NewField("value", output.Value))
                    }
                } else {
                    e.logger.WithContext(ctx).Error("特征函数不存在",
                        logger.NewField("function", feature.FunctionName),
                        logger.NewField("feature_id", condition.FeatureID))
                }
            }
        }
    }
}
```

#### 特征函数注册机制特点

特征函数注册机制具有以下特点：

1. **动态扩展**：特征函数注册机制支持动态注册新的特征函数，不需要修改规则引擎的核心代码，提高了系统的可扩展性。

2. **并发安全**：特征函数注册表使用sync.RWMutex实现并发安全，支持多线程同时读取和写入，提高了系统的并发性能。

3. **统一接口**：特征函数接口定义了统一的标准，所有特征函数都必须实现这个接口，确保了接口的一致性。

4. **配置灵活**：特征函数支持配置参数，通过GetConfigSchema方法定义配置模式，支持不同函数的个性化配置。

5. **错误处理**：特征函数执行过程中提供了完善的错误处理机制，包括参数验证、执行错误、返回错误等，确保了系统的稳定性。

### 7.3.2 向量检索优化

向量检索优化是RAG服务的核心技术之一，负责提高向量检索的准确度和效率。向量检索优化采用多种技术手段，包括向量索引、混合检索、批量操作、重试机制等，确保向量检索的高性能和高可靠性。

#### 向量索引优化

向量索引是提高向量检索效率的关键技术。系统使用PostgreSQL的pgvector扩展，支持多种向量索引类型，包括ivfflat索引、ivfpq索引等。向量索引通过近似最近邻搜索，大幅提高了向量检索的效率。

向量索引的创建使用ivfflat索引类型，支持余弦相似度计算。ivfflat索引通过聚类算法将向量分成多个列表，检索时只在最近的列表中搜索，减少了搜索范围，提高了检索速度。

向量索引创建的实现如下：

```go
func (vs *VectorStore) CreateVectorIndex(ctx context.Context, indexName string, lists int) error {
    if indexName == "" {
        vs.logger.Error("索引名称不能为空")
        return errors.New("索引名称不能为空")
    }
    
    if lists <= 0 {
        lists = 100
    }
    
    operation := func() error {
        ctx, cancel := context.WithTimeout(ctx, 5*time.Second)
        defer cancel()
        
        query := "CREATE INDEX " + indexName + " ON reimbursement_documents USING ivfflat (embedding vector_cosine_ops) WITH (lists = ?)"
        result := vs.db.WithContext(ctx).Exec(query, lists)
        return result.Error
    }
    
    if err := vs.retryOperation(operation, 2); err != nil {
        vs.logger.Error("创建向量索引失败", logger.NewField("index_name", indexName), logger.NewField("lists", lists), logger.NewField("error", err))
        return err
    }
    
    return nil
}
```

#### 向量相似度计算

向量相似度计算是向量检索的核心，系统使用余弦相似度计算向量之间的相似度。余弦相似度通过计算两个向量之间的夹角余弦值，范围在-1到1之间，值越大表示相似度越高。

向量相似度计算的实现如下：

```go
func (vs *VectorStore) CalculateSimilarity(vector1, vector2 []float64) float64 {
    if len(vector1) != len(vector2) {
        return 0
    }
    
    var dotProduct, norm1, norm2 float64
    for i := 0; i < len(vector1); i++ {
        dotProduct += vector1[i] * vector2[i]
        norm1 += vector1[i] * vector1[i]
        norm2 += vector2[i] * vector2[i]
    }
    
    if norm1 == 0 || norm2 == 0 {
        return 0
    }
    
    return dotProduct / (math.Sqrt(norm1) * math.Sqrt(norm2))
}
```

#### 向量检索实现

向量检索使用PostgreSQL的pgvector扩展，通过<->操作符计算向量之间的距离。系统使用余弦距离，距离越小表示相似度越高。检索结果按距离升序排序，返回TopK个最相似的向量。

向量检索的实现如下：

```go
func (vs *VectorStore) SearchVector(ctx context.Context, queryVector []float64, topK int) ([]*VectorSearchResult, error) {
    if len(queryVector) == 0 {
        vs.logger.Error("查询向量不能为空")
        return nil, errors.New("查询向量不能为空")
    }
    
    if len(queryVector) != VectorDimension {
        vs.logger.Error("查询向量维度必须为768维", logger.NewField("dimension", len(queryVector)))
        return nil, errors.New("查询向量维度必须为768维")
    }
    
    if topK <= 0 {
        topK = 10
    }
    
    operation := func() ([]*VectorSearchResult, error) {
        ctx, cancel := context.WithTimeout(ctx, 5*time.Second)
        defer cancel()
        
        type SearchResult struct {
            ID           string
            FileName     string
            FileType     string
            Category     string
            ChunkID      string
            ChunkIndex   int
            ChunkContent string
            Distance     float64
        }
        
        var results []SearchResult
        queryVectorJSON, _ := json.Marshal(queryVector)
        err := vs.db.WithContext(ctx).Raw(`
            SELECT id, file_name, file_type, category, chunk_id, chunk_index, chunk_content, 
                   embedding <-> ?::vector AS distance
            FROM reimbursement_documents
            WHERE embedding IS NOT NULL
            ORDER BY distance ASC
            LIMIT ?
        `, string(queryVectorJSON), topK).Scan(&results).Error
        if err != nil {
            return nil, err
        }
        
        vectorResults := make([]*VectorSearchResult, 0, len(results))
        for _, result := range results {
            vectorResults = append(vectorResults, &VectorSearchResult{
                ID:         result.ID,
                DocumentID: result.FileName,
                ChunkID:    result.ChunkID,
                Content:    result.ChunkContent,
                Score:      1.0 - result.Distance,
                Metadata: map[string]interface{}{
                    "category":  result.Category,
                    "file_type": result.FileType,
                },
            })
        }
        
        return vectorResults, nil
    }
    
    results, err := operation()
    if err != nil {
        vs.logger.Error("查询向量失败", logger.NewField("top_k", topK), logger.NewField("error", err))
        return nil, err
    }
    
    return results, nil
}
```

#### 混合检索优化

混合检索结合了向量检索和关键词检索，提高了检索的准确度。向量检索基于语义相似度，关键词检索基于文本匹配，两种检索方式互补，能够更准确地找到相关文档。

混合检索的实现包括向量检索、关键词检索和结果合并三个步骤。向量检索返回TopK*2个结果，关键词检索返回TopK*2个结果，然后合并两个结果集，按相似度排序，返回TopK个结果。

混合检索的实现如下：

```go
func (vs *VectorStore) HybridSearch(ctx context.Context, queryVector []float64, keywords []string, topK int) ([]*VectorSearchResult, error) {
    vectorResults, err := vs.SearchVector(ctx, queryVector, topK*2)
    if err != nil {
        return nil, err
    }
    
    if len(keywords) == 0 {
        if len(vectorResults) > topK {
            return vectorResults[:topK], nil
        }
        return vectorResults, nil
    }
    
    keywordResults, err := vs.KeywordSearch(ctx, keywords, topK*2)
    if err != nil {
        return nil, err
    }
    
    combined := vs.CombineResults(vectorResults, keywordResults, topK)
    return combined, nil
}
```

结果合并的实现如下：

```go
func (vs *VectorStore) CombineResults(vectorResults, keywordResults []*VectorSearchResult, topK int) []*VectorSearchResult {
    scoreMap := make(map[string]*VectorSearchResult)
    
    for _, result := range vectorResults {
        if existing, ok := scoreMap[result.ID]; ok {
            existing.Score = (existing.Score + result.Score) / 2
        } else {
            scoreMap[result.ID] = result
        }
    }
    
    for _, result := range keywordResults {
        if existing, ok := scoreMap[result.ID]; ok {
            existing.Score = (existing.Score + result.Score) / 2
        } else {
            scoreMap[result.ID] = result
        }
    }
    
    var combined []*VectorSearchResult
    for _, result := range scoreMap {
        combined = append(combined, result)
    }
    
    for i := 0; i < len(combined)-1; i++ {
        for j := i + 1; j < len(combined); j++ {
            if combined[i].Score < combined[j].Score {
                combined[i], combined[j] = combined[j], combined[i]
            }
        }
    }
    
    if len(combined) > topK {
        combined = combined[:topK]
    }
    
    return combined
}
```

#### 批量操作优化

批量操作优化通过批量插入和批量更新，减少了数据库操作的次数，提高了数据处理的效率。批量操作使用GORM的CreateInBatches方法，支持每次批量插入100条记录。

批量存储向量的实现如下：

```go
func (vs *VectorStore) StoreVectors(ctx context.Context, vectors []*Vector) error {
    if len(vectors) == 0 {
        return nil
    }
    
    docs := make([]*DocumentModel, 0, len(vectors))
    for _, vector := range vectors {
        if err := vs.validateVector(vector); err != nil {
            vs.logger.Warn("向量校验失败，跳过", logger.NewField("vector_id", vector.ID), logger.NewField("error", err))
            continue
        }
        
        if vector.ChunkContent == "" {
            vs.logger.Warn("分片内容为空，跳过", logger.NewField("vector_id", vector.ID))
            continue
        }
        
        doc := &DocumentModel{
            ID:           vector.ID,
            FileName:     vector.DocumentID,
            FileType:     "text",
            Category:     vector.Category,
            ChunkID:      vector.ChunkID,
            ChunkIndex:   0,
            ChunkContent: vector.ChunkContent,
            Embedding:    VectorData(vector.Values),
            CreatedAt:    time.Now(),
            UpdatedAt:    time.Now(),
        }
        docs = append(docs, doc)
    }
    
    if len(docs) == 0 {
        return nil
    }
    
    operation := func() error {
        ctx, cancel := context.WithTimeout(ctx, 5*time.Second)
        defer cancel()
        result := vs.db.WithContext(ctx).Clauses(clause.OnConflict{
            Columns:   []clause.Column{{Name: "id"}},
            DoUpdates: clause.AssignmentColumns([]string{"embedding", "chunk_content", "category", "updated_at"}),
        }).CreateInBatches(docs, 100)
        return result.Error
    }
    
    if err := vs.retryOperation(operation, 2); err != nil {
        vs.logger.Error("批量存储向量失败", logger.NewField("count", len(docs)), logger.NewField("error", err))
        return err
    }
    
    return nil
}
```

#### 重试机制

重试机制通过自动重试失败的操作，提高了系统的可靠性。重试机制使用retryOperation方法，支持设置最大重试次数，每次重试之间记录日志，便于问题排查。

重试机制的实现如下：

```go
func (vs *VectorStore) retryOperation(operation func() error, maxRetries int) error {
    var lastErr error
    for i := 0; i < maxRetries; i++ {
        if err := operation(); err != nil {
            lastErr = err
            vs.logger.Warn("操作失败，重试中", logger.NewField("retry", i+1), logger.NewField("error", err))
            continue
        }
        return nil
    }
    return lastErr
}
```

#### 向量检索优化特点

向量检索优化具有以下特点：

1. **高效检索**：向量索引通过近似最近邻搜索，大幅提高了向量检索的效率，能够满足实时检索的需求。

2. **混合检索**：混合检索结合了向量检索和关键词检索，提高了检索的准确度，能够更准确地找到相关文档。

3. **批量操作**：批量操作通过批量插入和批量更新，减少了数据库操作的次数，提高了数据处理的效率。

4. **重试机制**：重试机制通过自动重试失败的操作，提高了系统的可靠性，确保操作的最终成功。

5. **超时控制**：超时控制通过context.WithTimeout设置操作超时时间，防止长时间阻塞，提高了系统的响应性。

### 7.3.3 规则与AI协同

规则与AI协同是审核服务的核心技术之一，负责协调规则引擎和RAG服务的审核结果，生成最终的审核结论。规则与AI协同采用双层审核机制，先执行规则引擎的刚性规则校验，再执行RAG服务的柔性规则审核，最后综合两个审核结果生成最终的审核结论。

#### 规则校验结果

规则校验结果包括规则是否通过、规则命中列表、规则未命中列表等。规则校验通过表示所有规则都满足，规则校验不通过表示至少有一个规则不满足。

规则校验通过的判断逻辑如下：

```go
func (s *Service) checkRulePass(results []*RuleValidationResult) bool {
    if len(results) == 0 {
        return true
    }
    
    for _, result := range results {
        if !result.Passed {
            return false
        }
    }
    
    return true
}
```

#### RAG分析结果

RAG分析结果包括分析结论、置信度、推理过程、引用文档等。RAG分析通过表示置信度大于0.6，RAG分析不通过表示置信度小于等于0.6。

RAG分析通过的判断逻辑如下：

```go
audit.RAGPass = ragResult != nil && ragResult.Confidence > 0.6
```

#### 最终审核结论

最终审核结论综合规则校验结果和RAG分析结果，只有当规则校验通过且RAG分析通过时，最终审核才通过。如果任一审核不通过，最终审核不通过。

最终审核通过的判断逻辑如下：

```go
audit.FinalPass = audit.RulePass && audit.RAGPass
```

#### 风险分数计算

风险分数综合规则校验结果和RAG分析结果，计算报销单的风险分数。风险分数的范围是0到1，值越大表示风险越高。

风险分数的计算逻辑如下：

```go
func (s *Service) calculateRiskScore(audit *AuditResult) float64 {
    riskScore := 0.0
    
    if !audit.RulePass {
        riskScore += 0.5
    }
    
    if !audit.RAGPass {
        riskScore += 0.3
    }
    
    if audit.RAGResults != nil {
        riskScore += (1.0 - audit.RAGResults.Confidence) * 0.2
    }
    
    if riskScore > 1.0 {
        riskScore = 1.0
    }
    
    return riskScore
}
```

风险分数的计算规则如下：

1. 规则校验不通过，风险分数增加0.5
2. RAG分析不通过，风险分数增加0.3
3. RAG分析置信度低，风险分数增加(1-置信度)*0.2

#### 风险等级确定

风险等级根据风险分数确定，分为高风险、中风险、低风险三个等级。风险分数大于等于0.7为高风险，大于等于0.4且小于0.7为中风险，小于0.4为低风险。

风险等级的确定逻辑如下：

```go
func (s *Service) determineRiskLevel(riskScore float64) string {
    if riskScore >= 0.7 {
        return "高风险"
    } else if riskScore >= 0.4 {
        return "中风险"
    } else {
        return "低风险"
    }
}
```

#### 审核建议生成

审核建议根据规则校验结果和RAG分析结果生成，包括规则校验不通过的项目、RAG分析未通过的提示、风险等级提示等。

审核建议的生成逻辑如下：

```go
func (s *Service) generateSuggestions(audit *AuditResult) []string {
    var suggestions []string
    
    if !audit.RulePass {
        suggestions = append(suggestions, "请检查规则校验不通过的项目")
        for _, result := range audit.RuleResults {
            if !result.Passed {
                suggestions = append(suggestions, fmt.Sprintf("- %s: %s", result.RuleName, result.Message))
            }
        }
    }
    
    if !audit.RAGPass && audit.RAGResults != nil {
        suggestions = append(suggestions, "请检查RAG分析结果，建议人工复核")
    }
    
    if audit.RiskLevel == "高风险" {
        suggestions = append(suggestions, "该报销单风险较高，建议进行详细审核")
    }
    
    if len(suggestions) == 0 {
        suggestions = append(suggestions, "审核通过，可以继续后续流程")
    }
    
    return suggestions
}
```

#### 审核原因生成

审核原因根据规则校验结果和RAG分析结果生成，说明审核通过或未通过的原因。审核通过返回"审核通过"，审核未通过返回具体的未通过原因。

审核原因的生成逻辑如下：

```go
func (s *Service) generateAuditReason(audit *AuditResult) string {
    if audit.FinalPass {
        return "审核通过"
    }
    
    var reasons []string
    
    if !audit.RulePass {
        var hitRules []string
        for _, result := range audit.RuleResults {
            if result.Passed {
                hitRules = append(hitRules, result.RuleName)
            }
        }
        if len(hitRules) > 0 {
            reasons = append(reasons, "规则校验未通过: "+strings.Join(hitRules, ", "))
        } else {
            reasons = append(reasons, "规则校验未通过")
        }
    }
    
    if !audit.RAGPass {
        reasons = append(reasons, "RAG分析未通过")
    }
    
    if len(reasons) == 0 {
        return "审核未通过"
    }
    
    return "审核未通过: " + strings.Join(reasons, "; ")
}
```

#### 规则与AI协同特点

规则与AI协同具有以下特点：

1. **双层审核**：规则与AI协同采用双层审核机制，先执行规则引擎的刚性规则校验，再执行RAG服务的柔性规则审核，提高了审核的准确性和可靠性。

2. **智能综合**：规则与AI协同智能综合规则校验结果和RAG分析结果，生成最终的审核结论，包括风险分数、风险等级、审核建议等。

3. **风险量化**：规则与AI协同通过风险分数量化报销单的风险程度，便于审核员判断和决策。

4. **建议生成**：规则与AI协同根据审核结果生成具体的审核建议，帮助审核员快速定位问题。

5. **原因说明**：规则与AI协同生成详细的审核原因，说明审核通过或未通过的具体原因，提高了审核的透明度。

### 7.3.4 并发处理与性能优化

并发处理与性能优化是系统性能的关键技术之一，负责提高系统的并发处理能力和响应速度。并发处理与性能优化采用多种技术手段，包括并发控制、连接池管理、缓存机制、异步处理等，确保系统的高性能和高可用性。

#### 并发控制

并发控制使用sync.RWMutex实现读写锁，支持多线程同时读取，但写入时独占访问。读写锁提高了系统的并发性能，适用于读多写少的场景。

特征函数注册表使用读写锁的实现如下：

```go
type FunctionRegistry struct {
    functions map[string]FeatureFunction
    mu        sync.RWMutex
}
```

规则引擎使用读写锁的实现如下：

```go
type Engine struct {
    ruleRepository          RuleRepository
    featureRepository       FeatureRepository
    logger                  logger.Logger
    mu                      sync.RWMutex
    loadedRules             map[string]*Rule
    stats                   map[string]*EngineStats
    featureFunctionRegistry *featurefunction.FunctionRegistry
}
```

#### 连接池管理

连接池管理使用GORM的连接池功能，管理数据库连接的创建、复用和销毁。连接池配置包括最大连接数、最大空闲连接数、连接最大生存时间、连接最大空闲时间等参数。

连接池配置的实现如下：

```yaml
database:
  host: "localhost"
  port: 3306
  username: "root"
  password: "password"
  dbname: "reimbursement_audit"
  charset: "utf8mb4"
  collation: "utf8mb4_unicode_ci"
  parse_time: true
  loc: "Local"
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: 1h
  conn_max_idle_time: 10m
```

连接池配置避免了频繁创建和销毁连接的开销，提高了数据库访问性能。

#### 缓存机制

缓存机制使用内存缓存存储规则引擎的规则，避免了每次执行规则时都从数据库加载规则。规则引擎在初始化时从数据库加载所有启用的规则，缓存到内存中，提高了规则执行效率。

规则缓存的实现如下：

```go
func (e *Engine) Initialize(ctx context.Context) error {
    e.logger.WithContext(ctx).Info("初始化规则引擎")
    
    filter := &RuleFilter{
        Enabled: &[]bool{true}[0],
        Size:    1000,
    }
    
    rules, _, err := e.ruleRepository.ListRules(ctx, filter)
    if err != nil {
        return fmt.Errorf("获取启用规则失败: %w", err)
    }
    
    for _, rule := range rules {
        e.loadedRules[rule.ID] = rule
        e.stats[rule.ID] = &EngineStats{
            RuleID: rule.ID,
        }
    }
    
    e.logger.WithContext(ctx).Info("规则引擎初始化完成",
        logger.NewField("规则数量", len(rules)))
    
    return nil
}
```

#### 异步处理

异步处理使用goroutine实现并发处理，提高了系统的处理能力。系统在处理多个发票的规则校验时，使用goroutine并发处理每个发票，减少了总处理时间。

异步处理的实现如下：

```go
func (s *Service) executeRuleValidation(ctx context.Context, reimbursement *reimbursement.Reimbursement) ([]*RuleValidationResult, error) {
    s.logger.WithContext(ctx).Info("开始规则校验",
        logger.NewField("reimbursement_id", reimbursement.ID),
        logger.NewField("invoice_count", len(reimbursement.Invoices)))
    
    allResults := make([]*RuleValidationResult, 0)
    resultsChan := make(chan []*RuleValidationResult, len(reimbursement.Invoices))
    errorChan := make(chan error, len(reimbursement.Invoices))
    
    for _, invoice := range reimbursement.Invoices {
        go func(inv *reimbursement.Invoice) {
            results, err := s.executeInvoiceRuleValidation(ctx, inv, reimbursement)
            if err != nil {
                errorChan <- err
                return
            }
            resultsChan <- results
        }(invoice)
    }
    
    for i := 0; i < len(reimbursement.Invoices); i++ {
        select {
        case results := <-resultsChan:
            allResults = append(allResults, results...)
        case err := <-errorChan:
            s.logger.WithContext(ctx).Error("规则校验失败", logger.NewField("error", err))
            return nil, err
        }
    }
    
    s.logger.WithContext(ctx).Info("规则校验完成", logger.NewField("result_count", len(allResults)))
    
    return allResults, nil
}
```

#### 超时控制

超时控制使用context.WithTimeout设置操作超时时间，防止长时间阻塞。系统在数据库操作、HTTP请求、规则执行等关键操作中都设置了超时时间，提高了系统的响应性。

超时控制的实现如下：

```go
operation := func() error {
    ctx, cancel := context.WithTimeout(ctx, 5*time.Second)
    defer cancel()
    
    result := vs.db.WithContext(ctx).Clauses(clause.OnConflict{
        Columns:   []clause.Column{{Name: "id"}},
        DoUpdates: clause.AssignmentColumns([]string{"embedding", "chunk_content", "category", "updated_at"}),
    }).Create(doc)
    return result.Error
}
```

#### 性能监控

性能监控通过记录操作的执行时间，监控系统的性能指标。系统在规则执行、向量检索、RAG分析等关键操作中都记录了执行时间，便于性能分析和优化。

性能监控的实现如下：

```go
func (e *Engine) evaluateRule(ctx context.Context, rule *Rule, data interface{}) *RuleValidationResult {
    startTime := time.Now()
    
    e.logger.WithContext(ctx).Info("开始评估规则",
        logger.NewField("rule_id", rule.ID),
        logger.NewField("rule_name", rule.Name))
    
    result := &RuleValidationResult{
        RuleID:    rule.ID,
        RuleName:  rule.Name,
        RuleType:  "rule_engine",
        Passed:    true,
        Message:   "规则执行成功",
        Timestamp: time.Now(),
    }
    
    if len(rule.Conditions) == 0 {
        result.Passed = false
        result.Message = "规则没有条件"
        return result
    }
    
    passed := e.evaluateConditions(ctx, rule.Conditions, data)
    result.Passed = passed
    
    if passed {
        result.Message = fmt.Sprintf("规则命中: %s", rule.Decision.Reason)
    } else {
        result.Message = "规则未命中"
    }
    
    executionTime := time.Since(startTime)
    result.ExecutionTime = executionTime.Milliseconds()
    
    e.updateStats(rule.ID, passed, executionTime)
    
    return result
}
```

#### 并发处理与性能优化特点

并发处理与性能优化具有以下特点：

1. **高并发**：并发处理使用读写锁和goroutine实现高并发处理，提高了系统的并发处理能力。

2. **高性能**：连接池管理、缓存机制、批量操作等技术提高了系统的性能，减少了资源消耗。

3. **高可用**：重试机制、超时控制、错误处理等技术提高了系统的可用性，确保系统的稳定运行。

4. **可监控**：性能监控通过记录操作的执行时间，监控系统的性能指标，便于性能分析和优化。

5. **可扩展**：并发处理与性能优化采用模块化设计，各个组件独立封装，便于扩展和维护。

## 7.4 系统部署与运维

### 7.4.1 配置管理

配置管理是系统部署和运维的核心技术之一，负责管理系统的各种配置参数，包括服务器配置、数据库配置、Redis配置、大模型配置、OCR配置、存储配置、日志配置、安全配置、应用配置等。配置管理采用分层设计，支持多环境配置、环境变量覆盖、配置验证等功能，确保系统的灵活性和可维护性。

#### 配置结构设计

配置结构采用分层设计，将不同模块的配置分类管理，便于维护和扩展。配置结构包括服务器配置、数据库配置、Redis配置、大模型配置、OCR配置、存储配置、日志配置、安全配置、应用配置等。

配置结构的定义如下：

```go
type Config struct {
    Server   ServerConfig   `yaml:"server" json:"server"`
    Database DatabaseConfig `yaml:"database" json:"database"`
    Redis    RedisConfig    `yaml:"redis" json:"redis"`
    LLM      LLMConfig      `yaml:"llm" json:"llm"`
    OCR      OCRConfig      `yaml:"ocr" json:"ocr"`
    Storage  StorageConfig  `yaml:"storage" json:"storage"`
    Logger   LoggerConfig   `yaml:"logger" json:"logger"`
    Security SecurityConfig `yaml:"security" json:"security"`
    App      AppConfig      `yaml:"app" json:"app"`
}
```

服务器配置包括服务器地址、端口、模式、超时时间等参数。数据库配置包括数据库类型、主机、端口、用户名、密码、数据库名、字符集、连接池配置等参数。Redis配置包括主机、端口、密码、数据库编号、连接池配置等参数。大模型配置包括API密钥、模型名称、超时时间、最大重试次数等参数。OCR配置包括提供商、密钥、区域、超时时间、最大重试次数等参数。存储配置包括存储类型、根目录、最大文件大小、允许的文件类型等参数。日志配置包括日志级别、格式、输出、文件名、最大大小、最大备份数、最大天数、压缩等参数。安全配置包括JWT密钥、过期时间、加密密钥、限流配置等参数。应用配置包括应用名称、版本、环境、调试模式等参数。

#### 配置加载机制

配置加载机制支持从YAML文件和环境变量加载配置，环境变量可以覆盖YAML配置。配置加载器首先从YAML文件加载配置，然后从环境变量加载配置，最后验证配置的有效性。

配置加载器的实现如下：

```go
func (l *Loader) Load() (*Config, error) {
    config, err := l.LoadFromYAML()
    if err != nil {
        config = l.getDefaultConfig()
    }

    config = l.LoadFromEnv(config)

    if err := config.Validate(); err != nil {
        return nil, fmt.Errorf("配置验证失败: %w", err)
    }

    l.config = config
    return config, nil
}
```

从YAML文件加载配置的实现如下：

```go
func (l *Loader) LoadFromYAML() (*Config, error) {
    if _, err := os.Stat(l.path); os.IsNotExist(err) {
        return nil, fmt.Errorf("配置文件不存在: %s", l.path)
    }

    data, err := os.ReadFile(l.path)
    if err != nil {
        return nil, fmt.Errorf("读取配置文件失败: %w", err)
    }

    config := &Config{}
    if err := yaml.Unmarshal(data, config); err != nil {
        return nil, fmt.Errorf("解析YAML配置失败: %w", err)
    }

    return config, nil
}
```

从环境变量加载配置的实现如下：

```go
func (l *Loader) LoadFromEnv(config *Config) *Config {
    if config == nil {
        config = l.getDefaultConfig()
    }

    if host := os.Getenv("SERVER_HOST"); host != "" {
        config.Server.Host = host
    }
    if port := os.Getenv("SERVER_PORT"); port != "" {
        if p, err := strconv.Atoi(port); err == nil {
            config.Server.Port = p
        }
    }

    if secretID := os.Getenv("OCR_SECRET_ID"); secretID != "" {
        config.OCR.SecretID = secretID
    }
    if secretKey := os.Getenv("OCR_SECRET_KEY"); secretKey != "" {
        config.OCR.SecretKey = secretKey
    }

    return config
}
```

#### 配置验证机制

配置验证机制确保配置参数的有效性和完整性，防止配置错误导致系统异常。配置验证包括必填字段验证、数据类型验证、范围验证、格式验证等。

配置验证的实现如下：

```go
func (c *Config) Validate() error {
    if c.Server.Port <= 0 || c.Server.Port > 65535 {
        return errors.New("服务器端口必须在1-65535之间")
    }

    if c.Database.Host == "" {
        return errors.New("数据库主机不能为空")
    }
    if c.Database.Port <= 0 || c.Database.Port > 65535 {
        return errors.New("数据库端口必须在1-65535之间")
    }
    if c.Database.Username == "" {
        return errors.New("数据库用户名不能为空")
    }
    if c.Database.DBName == "" {
        return errors.New("数据库名不能为空")
    }

    if c.OCR.Provider == "" {
        return errors.New("OCR提供商不能为空")
    }
    if c.OCR.SecretID == "" {
        return errors.New("OCR密钥ID不能为空")
    }
    if c.OCR.SecretKey == "" {
        return errors.New("OCR密钥不能为空")
    }

    if c.Storage.MaxFileSize <= 0 {
        return errors.New("最大文件大小必须大于0")
    }
    if len(c.Storage.AllowedTypes) == 0 {
        return errors.New("允许的文件类型不能为空")
    }

    if c.Logger.Level < 0 || c.Logger.Level > 5 {
        return errors.New("日志级别必须在0-5之间")
    }

    return nil
}
```

#### 配置热重载

配置热重载支持在不重启系统的情况下重新加载配置，提高了系统的可维护性。配置热重载通过监听配置文件的变化，自动重新加载配置，更新系统配置。

配置热重载的实现如下：

```go
func (l *Loader) Reload() error {
    config, err := l.LoadFromYAML()
    if err != nil {
        return err
    }

    config = l.LoadFromEnv(config)

    if err := config.Validate(); err != nil {
        return err
    }

    l.config = config
    return nil
}
```

#### 配置管理特点

配置管理具有以下特点：

1. **分层设计**：配置管理采用分层设计，将不同模块的配置分类管理，便于维护和扩展。

2. **多环境支持**：配置管理支持多环境配置，包括开发环境、测试环境、生产环境等，便于不同环境的部署和管理。

3. **环境变量覆盖**：配置管理支持环境变量覆盖YAML配置，便于在容器化部署中动态配置。

4. **配置验证**：配置管理提供配置验证机制，确保配置参数的有效性和完整性，防止配置错误导致系统异常。

5. **热重载**：配置管理支持配置热重载，在不重启系统的情况下重新加载配置，提高了系统的可维护性。

### 7.4.2 监控与日志

监控与日志是系统运维的核心技术之一，负责监控系统的运行状态和记录系统的运行日志，便于问题排查和性能优化。监控与日志采用分层设计，包括日志记录、日志输出、日志格式、日志级别、日志轮转、性能监控等功能，确保系统的可观测性和可维护性。

#### 日志系统设计

日志系统采用分层设计，包括日志接口层、日志实现层、日志配置层、日志输出层。日志接口层定义了日志的标准接口，日志实现层提供了日志的具体实现，日志配置层管理日志的配置参数，日志输出层管理日志的输出方式。

日志接口的定义如下：

```go
type Logger interface {
    Debug(msg string, fields ...Field)
    Info(msg string, fields ...Field)
    Warn(msg string, fields ...Field)
    Error(msg string, fields ...Field)
    Fatal(msg string, fields ...Field)
    WithContext(ctx context.Context) Logger
    WithFields(fields ...Field) Logger
    WithField(key string, value interface{}) Logger
    SetLevel(level Level)
    GetLevel() Level
    SetOutput(w io.Writer)
    Close() error
}
```

#### 日志级别

日志级别包括Debug、Info、Warn、Error、Fatal五个级别，用于区分不同重要程度的日志信息。Debug级别用于记录调试信息，Info级别用于记录一般信息，Warn级别用于记录警告信息，Error级别用于记录错误信息，Fatal级别用于记录致命信息。

日志级别的定义如下：

```go
type Level int

const (
    DebugLevel Level = iota
    InfoLevel
    WarnLevel
    ErrorLevel
    FatalLevel
)
```

#### 日志格式

日志格式支持JSON格式和文本格式两种，用于不同的场景。JSON格式适合结构化日志，便于日志分析和检索，文本格式适合人类阅读，便于开发调试。

JSON格式的实现如下：

```go
func (l *loggerImpl) formatJSON(level Level, msg string, fields []Field) string {
    jsonStr := fmt.Sprintf(`{"level":"%s","time":"%s","message":"%s"`,
        level.String(), time.Now().Format(time.RFC3339), msg)

    if len(fields) > 0 {
        jsonStr += `,"fields":{`
        for i, field := range fields {
            if i > 0 {
                jsonStr += ","
            }
            jsonStr += fmt.Sprintf(`"%s":"%v"`, field.Key, field.Value)
        }
        jsonStr += "}"
    }

    jsonStr += "}"
    return jsonStr
}
```

文本格式的实现如下：

```go
func (l *loggerImpl) formatText(level Level, msg string, fields []Field) string {
    textStr := fmt.Sprintf("[%s] %s %s",
        level.String(), time.Now().Format(time.RFC3339), msg)

    if len(fields) > 0 {
        textStr += " |"
        for _, field := range fields {
            textStr += fmt.Sprintf(" %s=%v", field.Key, field.Value)
        }
    }

    return textStr
}
```

#### 日志输出

日志输出支持多种输出方式，包括标准输出、标准错误、文件输出等。标准输出适合开发调试，标准错误适合错误日志，文件输出适合生产环境。

日志输出的实现如下：

```go
func (l *loggerImpl) setOutput() error {
    switch l.config.Output {
    case "stdout":
        l.output = os.Stdout
    case "stderr":
        l.output = os.Stderr
    case "file":
        if l.config.Filename == "" {
            return fmt.Errorf("filename is required when output is file")
        }
        l.output = &lumberjack.Logger{
            Filename:   l.config.Filename,
            MaxSize:    l.config.MaxSize,
            MaxBackups: l.config.MaxBackups,
            MaxAge:     l.config.MaxAge,
            Compress:   l.config.Compress,
        }
    default:
        return fmt.Errorf("unsupported output: %s", l.config.Output)
    }
    return nil
}
```

#### 日志轮转

日志轮转使用lumberjack库实现，支持按文件大小、备份数量、天数进行日志轮转，防止日志文件过大占用磁盘空间。日志轮转包括最大文件大小、最大备份数量、最大天数、压缩等配置。

日志轮转的配置如下：

```go
type LoggerConfig struct {
    Level      Level  `yaml:"level" json:"level"`
    Format     string `yaml:"format" json:"format"`
    Output     string `yaml:"output" json:"output"`
    Filename   string `yaml:"filename" json:"filename"`
    MaxSize    int    `yaml:"max_size" json:"max_size"`
    MaxBackups int    `yaml:"max_backups" json:"max_backups"`
    MaxAge     int    `yaml:"max_age" json:"max_age"`
    Compress   bool   `yaml:"compress" json:"compress"`
}
```

#### 日志上下文

日志上下文支持从context中提取traceId，自动添加到日志中，便于日志追踪和问题排查。日志上下文通过WithContext方法添加context，在记录日志时自动提取traceId。

日志上下文的实现如下：

```go
func (l *loggerImpl) log(level Level, msg string, fields ...Field) {
    if level < l.config.Level {
        return
    }

    allFields := make([]Field, 0, len(l.fields)+len(fields))
    allFields = append(allFields, l.fields...)
    allFields = append(allFields, fields...)

    if l.context != nil {
        if traceId := l.context.Value("trace_id"); traceId != nil {
            if id, ok := traceId.(string); ok && id != "" {
                hasTraceId := false
                for _, field := range allFields {
                    if field.Key == "trace_id" {
                        hasTraceId = true
                        break
                    }
                }
                if !hasTraceId {
                    allFields = append(allFields, Field{Key: "trace_id", Value: id})
                }
            }
        }
    }

    var logLine string
    if l.config.Format == "json" {
        logLine = l.formatJSON(level, msg, allFields)
    } else {
        logLine = l.formatText(level, msg, allFields)
    }

    l.mu.Lock()
    defer l.mu.Unlock()

    fmt.Fprintln(l.output, logLine)
}
```

#### 性能监控

性能监控通过记录操作的执行时间，监控系统的性能指标，便于性能分析和优化。系统在规则执行、向量检索、RAG分析等关键操作中都记录了执行时间，便于性能监控。

性能监控的实现如下：

```go
func (e *Engine) evaluateRule(ctx context.Context, rule *Rule, data interface{}) *RuleValidationResult {
    startTime := time.Now()

    result := &RuleValidationResult{
        RuleID:    rule.ID,
        RuleName:  rule.Name,
        RuleType:  "rule_engine",
        Passed:    true,
        Message:   "规则执行成功",
        Timestamp: time.Now(),
    }

    passed := e.evaluateConditions(ctx, rule.Conditions, data)
    result.Passed = passed

    executionTime := time.Since(startTime)
    result.ExecutionTime = executionTime.Milliseconds()

    e.updateStats(rule.ID, passed, executionTime)

    return result
}
```

#### 监控与日志特点

监控与日志具有以下特点：

1. **分层设计**：监控与日志采用分层设计，包括日志接口层、日志实现层、日志配置层、日志输出层，便于维护和扩展。

2. **多级别日志**：监控与日志支持多级别日志，包括Debug、Info、Warn、Error、Fatal，用于区分不同重要程度的日志信息。

3. **多格式输出**：监控与日志支持JSON格式和文本格式，用于不同的场景，JSON格式适合结构化日志，文本格式适合人类阅读。

4. **多输出方式**：监控与日志支持多种输出方式，包括标准输出、标准错误、文件输出等，用于不同的场景。

5. **日志轮转**：监控与日志支持日志轮转，防止日志文件过大占用磁盘空间，确保日志的可持续性。

6. **上下文追踪**：监控与日志支持从context中提取traceId，自动添加到日志中，便于日志追踪和问题排查。

7. **性能监控**：监控与日志通过记录操作的执行时间，监控系统的性能指标，便于性能分析和优化。
