# 第三章 系统需求分析与总体设计

## 3.1 需求分析

### 3.1.1 功能性需求

智能报销审核系统基于"规则引擎+大模型RAG"双层审核模式，旨在实现报销单据的自动化审核，提高审核效率和准确性。系统主要包含以下核心功能模块：

#### （1）报销单管理模块

报销单管理模块是系统的基础功能模块，负责报销单据的全生命周期管理。该模块主要功能包括：

**报销单创建与编辑**
- 支持用户通过Web界面创建报销单，填写报销单基本信息，包括报销标题、报销类别、报销金额、报销事由、报销日期、详细描述等
- 支持报销单的编辑功能，用户可以在提交审核前修改报销单信息
- 支持报销单的删除功能，仅限于未提交审核状态的报销单
- 报销单状态管理：待提交、待审核、审核中、已完成、已驳回等状态流转

**报销单信息管理**
- 用户信息：包括用户ID、用户姓名、所属部门、申请人级别（高管/经理/员工）等
- 报销信息：包括报销类型（交通费、住宿费、餐饮费、办公用品、招待费等）、报销金额、币种、申请日期、费用发生日期等
- 出差信息：包括出差开始日期、结束日期、出差目的地、出差城市、出差省份、出差事由、交通工具等
- 项目信息：包括项目编码、预算科目等

**报销单查询与统计**
- 支持按用户ID、状态、时间范围、报销类别等多维度查询报销单
- 支持分页查询，提供灵活的排序功能
- 提供报销单统计功能，包括报销单数量、总金额、通过率等统计数据

#### （2）发票管理模块

发票管理模块负责发票信息的采集、存储和管理，是审核流程的重要数据来源。该模块主要功能包括：

**发票上传与识别**
- 支持单张发票上传和批量发票上传功能
- 支持多种发票格式：JPG、PNG、PDF等
- 支持发票文件大小限制，单张发票不超过10MB
- 集成OCR服务，自动识别发票信息，包括发票代码、发票号码、发票类型、开票日期、金额、税额、价税合计、购买方信息、销售方信息、商品明细等
- 支持发票识别失败的重试机制和人工补录功能

**发票信息管理**
- 发票基本信息：发票ID、报销单ID、发票类型、发票代码、发票号码、开票日期、发票金额、税额等
- 购销方信息：购买方名称、购买方税号、销售方名称、销售方税号等
- 商品明细：商品名称、规格型号、单位、数量、单价、金额、税率、税额等
- 扩展信息：发票类别（差旅费/办公费/招待费/培训费等）、发票子类别、费用类型、商户类型、消费地点、消费城市、消费省份等

**发票校验功能**
- 发票唯一性校验：检查发票号码是否已被使用，防止重复报销
- 发票日期有效性校验：检查发票日期是否在有效期内（如6个月内）
- 发票金额范围校验：检查发票金额是否在合理范围内
- 发票类型合规性校验：检查发票类型是否与报销类别匹配
- 发票商品合规性校验：检查商品名称是否符合报销政策（如禁止报销烟草、奢侈品等）

**发票状态管理**
- 发票状态：待识别、已识别、识别失败等
- 验证状态：未验证、已验证、验证失败等
- 支持发票的重新识别和重新验证功能

#### （3）规则引擎模块

规则引擎模块是系统的刚性审核层，基于规则引擎技术实现报销单据的自动化校验。该模块主要功能包括：

**规则管理功能**
- 规则的创建、编辑、删除、启用/禁用功能
- 规则分类管理：按报销类别（差旅费、交通费、住宿费、餐饮费等）分类
- 规则优先级管理：支持设置规则优先级，数字越大优先级越高
- 规则版本管理：记录规则的修改历史，支持规则回滚
- 规则测试功能：支持对规则进行测试，验证规则的正确性

**规则类型**
系统支持多种类型的审核规则：

1. **金额校验规则**
   - 住宿费标准校验：根据城市等级（一线/二线/三线）和员工级别（高管/经理/员工）设定住宿费标准
   - 餐饮费标准校验：根据出差天数和员工级别设定餐饮费标准
   - 交通费标准校验：根据交通工具类型和出差距离设定交通费标准
   - 招待费标准校验：根据招待对象类型（A类/B类/C类）设定招待费标准

2. **频次校验规则**
   - 月报销次数限制：限制同一类别报销单的月报销次数
   - 日报销金额限制：限制单日报销金额上限
   - 季度报销金额限制：限制季度报销金额上限

3. **发票信息校验规则**
   - 发票号码唯一性校验：检查发票号码是否已被使用
   - 发票代码格式校验：检查发票代码格式是否正确
   - 发票日期有效性校验：检查发票日期是否在有效期内（如6个月内）
   - 发票金额范围校验：检查发票金额是否在合理范围内
   - 发票类型合规性校验：检查发票类型是否与报销类别匹配

4. **基础合规校验规则**
   - 报销单必填字段完整性校验：检查报销单必填字段是否完整
   - 发票与报销事由关联性校验：检查发票内容是否与报销事由匹配
   - 商品名称合规性校验：检查商品名称是否符合报销政策（如禁止报销烟草、奢侈品等）
   - 商户类型合规性校验：检查商户类型是否在允许范围内

5. **智能校验规则**
   - 智能住宿费校验：根据城市等级、员工级别、活动情况、节假日情况动态调整住宿费标准
   - 智能交通费校验：根据出差距离、交通工具类型、市场价格动态调整交通费标准
   - 智能招待费校验：根据招待对象类型、招待人数、招待地点动态调整招待费标准

**规则执行引擎**
- 基于Grule规则引擎框架实现规则的动态加载和执行
- 支持规则的优先级排序，高优先级规则先执行
- 支持规则的逻辑运算（AND、OR），实现复杂的规则组合
- 支持规则的条件判断（等于、不等于、大于、小于、包含等）
- 提供规则执行结果记录，包括规则ID、规则名称、是否通过、执行时间、详细信息等

**规则特征函数**
系统提供丰富的特征函数，用于规则的条件判断：

- 发票相关特征：发票金额、发票日期、发票类型、发票代码、发票号码、商品名称、商户类型等
- 报销单相关特征：报销金额、报销类别、报销事由、申请日期、出差城市、员工级别等
- 时间相关特征：发票日期与申请日期的差值、是否在6个月内、是否为节假日等
- 频次相关特征：月报销次数、日报销金额、季度报销金额等
- 智能特征：城市等级、活动调整系数、节假日调整系数等

#### （4）智能审核模块

智能审核模块是系统的柔性审核层，基于RAG（检索增强生成）技术和大模型实现报销单据的智能审核。该模块主要功能包括：

**知识库管理**
- 支持报销制度文档的上传，包括PDF、Word、TXT等格式
- 支持文档的解析和分片处理，将长文档切分为多个文本片段
- 支持文档的向量化处理，将文本片段转换为向量表示
- 支持文档的分类管理，按报销类别（差旅费、交通费、住宿费等）分类
- 支持文档的版本管理，记录文档的修改历史
- 支持文档的搜索功能，支持关键词搜索和语义搜索

**向量检索**
- 基于向量数据库实现文档片段的相似度检索
- 支持多种相似度计算方法（余弦相似度、欧氏距离等）
- 支持Top-K检索，返回最相关的K个文档片段
- 支持检索结果的过滤和排序
- 提供检索结果的相关性评分

**大模型交互**
- 集成大模型API（如智谱AI、文心一言等），实现自然语言理解和生成
- 支持Prompt的动态构建，根据报销单信息和检索到的文档片段生成审核Prompt
- 支持大模型的参数配置，包括模型名称、最大Token数、温度参数等
- 支持大模型调用的超时和重试机制
- 提供大模型响应的结构化解析，将自然语言响应转换为JSON格式

**智能审核流程**
1. **规则校验阶段**：首先执行规则引擎的刚性校验，检查报销单是否符合基本的报销政策
2. **RAG检索阶段**：基于报销单信息和规则校验结果，从知识库中检索相关的报销制度条款
3. **大模型分析阶段**：将报销单信息、规则校验结果、检索到的制度条款输入大模型，进行智能分析
4. **结果生成阶段**：大模型生成审核结论、审核理由、改进建议等
5. **结果结构化阶段**：将大模型的自然语言响应转换为结构化的审核结果

**审核结果管理**
- 审核结果包括：审核状态（通过/不通过/需人工审核）、审核理由、风险等级、风险分数、改进建议等
- 支持审核结果的查询和统计
- 支持审核结果的导出功能，支持PDF、Excel等格式
- 提供审核结果的详细报告，包括规则校验结果、RAG分析结果、大模型响应等

#### （5）知识库管理模块

知识库管理模块负责报销制度文档的管理和维护，是RAG智能审核的基础。该模块主要功能包括：

**文档上传与管理**
- 支持多种文档格式的上传：PDF、Word、TXT等
- 支持文档的基本信息管理：文档名称、文档类型、文档大小、上传时间、上传人等
- 支持文档的分类管理：按报销类别（差旅费、交通费、住宿费等）分类
- 支持文档的版本管理：记录文档的修改历史，支持文档回滚
- 支持文档的下载和预览功能

**文档解析与处理**
- 支持文档的文本提取，将PDF、Word等格式转换为纯文本
- 支持文档的分片处理，将长文档切分为多个文本片段（如500字/片）
- 支持文档的向量化处理，将文本片段转换为向量表示
- 支持文档的元数据提取，包括作者、创建时间、生效时间、失效时间、关键词等

**知识库检索**
- 支持关键词搜索：基于关键词匹配检索相关文档片段
- 支持语义搜索：基于向量相似度检索相关文档片段
- 支持混合搜索：结合关键词搜索和语义搜索，提高检索准确率
- 支持检索结果的过滤和排序
- 提供检索结果的相关性评分

**知识库维护**
- 支持文档的编辑和更新功能
- 支持文档的删除功能
- 支持知识库的统计功能，包括文档数量、片段数量、向量数量等
- 提供知识库的健康检查功能，检查文档的有效性和完整性

#### （6）审核流程管理模块

审核流程管理模块负责审核流程的编排和管理，确保审核流程的顺利进行。该模块主要功能包括：

**审核流程编排**
- 支持审核流程的自动化编排，包括规则校验、RAG检索、大模型分析等步骤
- 支持审核流程的并行执行，提高审核效率
- 支持审核流程的异常处理，包括重试、降级等机制
- 提供审核流程的进度跟踪，实时显示审核进度

**审核状态管理**
- 审核状态：待审核、审核中、审核完成、审核失败、待人工审核、已通过、已驳回、已撤回等
- 工作流状态：已提交、规则审核中、规则审核通过、规则审核失败、AI审核中、AI审核通过、AI审核失败、待人工审核、人工审核通过、人工审核驳回、已通过、已驳回、已撤回等
- 支持审核状态的自动流转
- 支持审核状态的手动干预

**审核流程日志**
- 记录审核流程的每一个步骤，包括开始时间、结束时间、执行结果、执行人等
- 支持流程日志的查询和导出
- 提供流程日志的可视化展示，方便用户了解审核流程

**人工审核功能**
- 支持人工审核的介入，当智能审核无法确定时，可以转入人工审核
- 支持人工审核的通过和驳回操作
- 支持人工审核理由的填写
- 提供人工审核的统计功能

#### （7）用户管理模块

用户管理模块负责系统用户的管理和权限控制。该模块主要功能包括：

**用户管理**
- 支持用户的创建、编辑、删除功能
- 支持用户信息的管理，包括用户名、真实姓名、所属部门、邮箱、手机号等
- 支持用户角色的管理，包括普通用户、审核员、管理员等
- 支持用户状态的启用/禁用功能

**权限管理**
- 基于RBAC（基于角色的访问控制）模型实现权限管理
- 支持角色的创建、编辑、删除功能
- 支持权限的分配和回收
- 支持权限的细粒度控制，包括功能权限和数据权限

**认证与授权**
- 支持用户登录和登出功能
- 支持JWT令牌认证
- 支持API密钥认证
- 支持会话管理

#### （8）系统管理模块

系统管理模块负责系统的配置和维护。该模块主要功能包括：

**系统配置**
- 支持系统参数的配置，包括数据库配置、大模型API配置、OCR服务配置、存储配置等
- 支持多环境配置（开发环境、测试环境、生产环境）
- 支持配置的导入和导出功能

**日志管理**
- 支持操作日志的记录，包括用户操作、系统操作等
- 支持日志的查询和导出功能
- 支持日志的分级管理（DEBUG、INFO、WARN、ERROR）
- 支持日志的归档和清理功能

**监控与告警**
- 支持系统性能监控，包括CPU、内存、磁盘、网络等
- 支持业务指标监控，包括审核通过率、审核耗时、规则命中率等
- 支持异常告警，包括系统异常、业务异常等
- 支持告警通知，包括邮件、短信、企业微信等

### 3.1.2 非功能性需求

非功能性需求描述了系统在性能、安全性、可靠性、可扩展性等方面的质量属性，是系统设计和实现的重要依据。

#### （1）性能需求

**响应时间需求**
- 报销单创建响应时间：≤ 2秒
- 发票上传响应时间：≤ 3秒
- 发票OCR识别响应时间：≤ 5秒
- 规则校验响应时间：≤ 3秒
- RAG检索响应时间：≤ 2秒
- 大模型调用响应时间：≤ 10秒
- 审核流程总响应时间：≤ 20秒
- 报销单查询响应时间：≤ 1秒

**吞吐量需求**
- 系统支持并发用户数：≥ 100
- 系统支持每秒处理报销单数：≥ 10
- 系统支持每秒处理发票数：≥ 20
- 系统支持每秒审核单据数：≥ 5

**资源利用率需求**
- CPU利用率：≤ 80%
- 内存利用率：≤ 80%
- 磁盘利用率：≤ 70%
- 网络带宽利用率：≤ 70%

#### （2）安全性需求

**身份认证需求**
- 支持用户名密码认证
- 支持JWT令牌认证
- 支持API密钥认证
- 密码必须满足复杂度要求（至少8位，包含大小写字母、数字、特殊字符）
- 密码必须定期更换（建议90天）
- 支持密码错误次数限制（5次错误后锁定账户）

**授权控制需求**
- 基于RBAC模型实现权限控制
- 支持功能权限的细粒度控制
- 支持数据权限的细粒度控制（如只能查看自己的报销单）
- 支持权限的动态分配和回收
- 支持权限审计，记录权限变更历史

**数据安全需求**
- 敏感数据加密存储（如密码、身份证号等）
- 传输数据加密（使用HTTPS协议）
- 支持数据备份和恢复
- 支持数据脱敏，保护用户隐私
- 支持数据访问审计，记录数据访问日志

**网络安全需求**
- 支持跨域请求控制（CORS）
- 支持IP白名单功能
- 支持请求频率限制（Rate Limiting）
- 支持防SQL注入、防XSS攻击
- 支持防CSRF攻击

**审计需求**
- 记录所有用户操作日志
- 记录所有系统操作日志
- 记录所有审核流程日志
- 日志必须包含操作人、操作时间、操作内容、操作结果等
- 日志必须保存至少180天

#### （3）可靠性需求

**可用性需求**
- 系统可用性：≥ 99.5%（年停机时间≤ 43.8小时）
- 系统平均无故障时间（MTBF）：≥ 720小时
- 系统平均修复时间（MTTR）：≤ 4小时

**容错性需求**
- 支持服务降级，当某个服务不可用时，系统仍能提供基本功能
- 支持自动重试，当请求失败时，自动重试最多3次
- 支持熔断机制，当某个服务连续失败时，自动熔断，避免雪崩效应
- 支持限流机制，当系统负载过高时，自动限流，保护系统稳定性

**数据一致性需求**
- 支持事务管理，确保数据的一致性
- 支持乐观锁和悲观锁，避免并发冲突
- 支持数据校验，确保数据的完整性和准确性
- 支持数据同步，确保多个数据源的数据一致性

**备份与恢复需求**
- 支持数据库的定期备份（每天备份一次）
- 支持文件的定期备份（每天备份一次）
- 支持备份数据的异地存储
- 支持数据的快速恢复，恢复时间≤ 2小时

#### （4）可扩展性需求

**水平扩展需求**
- 支持应用服务器的水平扩展，通过增加服务器数量提高系统处理能力
- 支持数据库的水平扩展，通过分库分表提高数据库处理能力
- 支持存储的水平扩展，通过分布式存储提高存储能力

**垂直扩展需求**
- 支持应用服务器的垂直扩展，通过升级服务器配置提高系统处理能力
- 支持数据库的垂直扩展，通过升级数据库配置提高数据库处理能力

**模块化需求**
- 系统采用模块化设计，各模块之间低耦合、高内聚
- 支持模块的独立部署和升级
- 支持模块的动态加载和卸载

**接口开放需求**
- 提供RESTful API接口，支持第三方系统集成
- 提供SDK，支持多种编程语言的调用
- 提供API文档，方便开发者使用

#### （5）可维护性需求

**代码质量需求**
- 代码必须符合编码规范
- 代码必须有充分的注释
- 代码必须有单元测试，单元测试覆盖率≥ 80%
- 代码必须通过静态代码检查

**文档需求**
- 提供系统设计文档，包括架构设计、数据库设计、接口设计等
- 提供用户手册，指导用户使用系统
- 提供运维手册，指导运维人员维护系统
- 提供开发手册，指导开发人员进行二次开发

**监控需求**
- 提供系统监控功能，实时监控系统运行状态
- 提供业务监控功能，实时监控业务指标
- 提供告警功能，及时通知运维人员处理异常
- 提供日志分析功能，帮助运维人员定位问题

**故障诊断需求**
- 提供详细的错误日志，帮助开发人员定位问题
- 提供性能分析工具，帮助开发人员优化性能
- 提供调试工具，帮助开发人员进行调试

#### （6）易用性需求

**界面友好性需求**
- 界面设计简洁明了，符合用户习惯
- 界面布局合理，信息层次清晰
- 界面风格统一，保持一致性
- 支持主题切换，满足不同用户的审美需求

**操作便捷性需求**
- 操作流程简单，减少用户操作步骤
- 提供操作提示，引导用户完成操作
- 提供快捷键，提高用户操作效率
- 支持批量操作，提高用户操作效率

**响应及时性需求**
- 界面响应及时，避免用户长时间等待
- 提供加载提示，告知用户系统正在处理
- 提供进度提示，告知用户操作进度

**错误提示友好性需求**
- 错误提示清晰明了，告诉用户发生了什么
- 错误提示提供解决方案，告诉用户如何处理
- 错误提示避免技术术语，使用用户能理解的语言

#### （7）兼容性需求

**浏览器兼容性需求**
- 支持Chrome浏览器（最新版本）
- 支持Firefox浏览器（最新版本）
- 支持Safari浏览器（最新版本）
- 支持Edge浏览器（最新版本）

**操作系统兼容性需求**
- 支持Windows操作系统（Windows 10及以上）
- 支持MacOS操作系统（MacOS 10.14及以上）
- 支持Linux操作系统（Ubuntu 18.04及以上）

**移动设备兼容性需求**
- 支持iOS设备（iOS 13及以上）
- 支持Android设备（Android 8.0及以上）
- 提供响应式设计，适配不同屏幕尺寸

#### （8）国际化需求

**多语言支持需求**
- 支持中文简体
- 支持中文繁体
- 支持英文
- 支持语言切换功能

**时区支持需求**
- 支持多时区显示
- 支持时区切换功能
- 默认使用用户本地时区

**货币支持需求**
- 支持人民币（CNY）
- 支持美元（USD）
- 支持欧元（EUR）
- 支持货币转换功能

### 3.1.3 业务流程分析

智能报销审核系统的核心业务流程包括报销流程和审核流程两个主要部分。下面详细分析这两个业务流程。

#### （1）报销流程

报销流程是员工提交报销申请的完整流程，包括报销单创建、发票上传、报销单提交、审核、报销等环节。

**流程步骤**

1. **报销单创建**
   - 员工登录系统，进入报销单管理页面
   - 点击"新建报销单"按钮，进入报销单创建页面
   - 填写报销单基本信息：
     - 报销标题：简洁明了地描述报销内容
     - 报销类别：选择报销类别（差旅费、交通费、住宿费、餐饮费、办公用品、招待费、其他等）
     - 报销金额：填写报销总金额
     - 报销事由：详细说明报销原因
     - 报销日期：选择费用发生日期
     - 详细描述：补充说明报销详情
   - 填写出差信息（如果是差旅费报销）：
     - 出差开始日期
     - 出差结束日期
     - 出差目的地
     - 出差城市
     - 出差省份
     - 出差事由
     - 交通工具
   - 填写项目信息（如果有）：
     - 项目编码
     - 预算科目

2. **发票上传**
   - 在报销单创建页面，点击"上传发票"按钮
   - 选择发票文件（支持JPG、PNG、PDF等格式）
   - 系统自动调用OCR服务识别发票信息：
     - 发票代码
     - 发票号码
     - 发票类型
     - 开票日期
     - 金额
     - 税额
     - 价税合计
     - 购买方信息
     - 销售方信息
     - 商品明细
   - 用户确认发票信息是否正确，如有错误可以手动修改
   - 支持上传多张发票
   - 支持批量上传发票

3. **报销单保存**
   - 用户确认报销单信息和发票信息无误后，点击"保存"按钮
   - 系统保存报销单信息和发票信息
   - 报销单状态为"待提交"

4. **报销单提交**
   - 用户点击"提交审核"按钮
   - 系统校验报销单信息是否完整：
     - 检查必填字段是否填写
     - 检查发票是否上传
     - 检查发票金额总和是否与报销金额一致
   - 校验通过后，系统自动触发审核流程
   - 报销单状态更新为"审核中"

5. **审核流程**
   - 系统自动执行规则校验（刚性审核）
   - 系统自动执行RAG智能审核（柔性审核）
   - 如果审核通过，报销单状态更新为"待人工审核"
   - 如果审核不通过，报销单状态更新为"已驳回"，用户需要修改后重新提交

6. **人工审核**
   - 审核员登录系统，进入审核管理页面
   - 查看"待人工审核"的报销单
   - 查看报销单详细信息、发票信息、审核结果等
   - 根据审核结果，选择"通过"或"驳回"
   - 如果选择"驳回"，需要填写驳回理由
   - 如果选择"通过"，报销单状态更新为"已通过"
   - 如果选择"驳回"，报销单状态更新为"已驳回"，用户需要修改后重新提交

7. **报销处理**
   - 财务人员查看"已通过"的报销单
   - 进行打款处理
   - 报销单状态更新为"已完成"

**流程图**

```
员工登录系统
    ↓
创建报销单
    ↓
上传发票
    ↓
OCR识别发票
    ↓
保存报销单（状态：待提交）
    ↓
提交审核
    ↓
规则校验（刚性审核）
    ↓
RAG智能审核（柔性审核）
    ↓
审核通过？
    ├─ 是 → 状态：待人工审核
    │         ↓
    │      人工审核
    │         ↓
    │      审核通过？
    │         ├─ 是 → 状态：已通过 → 财务打款 → 状态：已完成
    │         └─ 否 → 状态：已驳回 → 员工修改 → 重新提交
    └─ 否 → 状态：已驳回 → 员工修改 → 重新提交
```

#### （2）审核流程

审核流程是系统对报销单进行自动化审核的完整流程，包括规则校验和RAG智能审核两个主要环节。

**流程步骤**

1. **审核开始**
   - 用户提交报销单后，系统自动触发审核流程
   - 系统创建审核记录，审核状态为"审核中"
   - 系统创建流程日志，记录审核开始时间

2. **规则校验（刚性审核）**
   - 系统加载所有启用的审核规则
   - 系统按规则优先级排序，高优先级规则先执行
   - 系统对报销单的每一张发票执行规则校验：
     - 金额校验：检查发票金额是否符合标准
     - 频次校验：检查报销频次是否符合限制
     - 发票信息校验：检查发票信息是否合规
     - 基础合规校验：检查报销单是否完整
   - 系统记录每条规则的校验结果：
     - 规则ID
     - 规则名称
     - 是否通过
     - 校验时间
     - 详细信息
   - 系统判断规则校验是否全部通过：
     - 如果全部通过，规则校验通过，进入RAG智能审核
     - 如果有规则未通过，规则校验未通过，继续执行RAG智能审核以获得完整的审核结果

3. **RAG智能审核（柔性审核）**
   - 系统构建报销单信息，包括：
     - 报销单基本信息
     - 发票详细信息
     - 规则校验结果
   - 系统从知识库中检索相关的报销制度条款：
     - 基于报销类别检索
     - 基于报销事由检索
     - 基于发票信息检索
     - 基于规则校验结果检索
   - 系统构建审核Prompt：
     - 拼接报销单信息
     - 拼接规则校验结果
     - 拼接检索到的制度条款
   - 系统调用大模型API进行智能分析：
     - 大模型分析报销单是否符合报销政策
     - 大模型生成审核结论
     - 大模型生成审核理由
     - 大模型生成改进建议
   - 系统解析大模型响应：
     - 提取审核结论（通过/不通过/需人工审核）
     - 提取审核理由
     - 提取改进建议
     - 计算置信度

4. **审核结果生成**
   - 系统综合规则校验结果和RAG智能审核结果，生成最终审核结果：
     - 如果规则校验通过且RAG智能审核通过，审核通过
     - 如果规则校验未通过或RAG智能审核未通过，审核不通过
   - 系统计算风险分数：
     - 规则校验未通过，风险分数增加0.5
     - RAG智能审核未通过，风险分数增加0.3
     - RAG智能审核置信度低，风险分数增加（1-置信度）×0.2
   - 系统确定风险等级：
     - 风险分数≥0.7，高风险
     - 风险分数≥0.4，中风险
     - 风险分数<0.4，低风险
   - 系统生成改进建议：
     - 如果规则校验未通过，列出未通过的规则
     - 如果RAG智能审核未通过，建议人工复核
     - 如果风险等级高，建议详细审核

5. **审核结束**
   - 系统更新审核记录：
     - 审核状态：审核完成
     - 审核结果：通过/不通过
     - 审核理由
     - 风险等级
     - 风险分数
     - 改进建议
     - 审核耗时
   - 系统更新报销单状态：
     - 如果审核通过，状态更新为"待人工审核"
     - 如果审核不通过，状态更新为"已驳回"
   - 系统创建流程日志，记录审核结束时间

6. **人工审核介入**
   - 如果RAG智能审核的置信度较低（<0.6），系统自动转入人工审核
   - 审核员查看报销单详细信息、发票信息、规则校验结果、RAG智能审核结果等
   - 审核员根据审核结果，选择"通过"或"驳回"
   - 如果选择"驳回"，需要填写驳回理由
   - 系统更新审核记录和报销单状态
   - 系统创建流程日志，记录人工审核操作

**流程图**

```
报销单提交
    ↓
创建审核记录（状态：审核中）
    ↓
记录流程日志（审核开始）
    ↓
规则校验（刚性审核）
    ↓
加载所有启用的规则
    ↓
按优先级排序规则
    ↓
对每张发票执行规则校验
    ↓
记录规则校验结果
    ↓
规则校验通过？
    ├─ 是 → 进入RAG智能审核
    └─ 否 → 继续RAG智能审核（获取完整结果）
    ↓
RAG智能审核（柔性审核）
    ↓
构建报销单信息
    ↓
检索相关制度条款
    ↓
构建审核Prompt
    ↓
调用大模型API
    ↓
解析大模型响应
    ↓
生成审核结果
    ↓
综合规则和RAG结果
    ↓
计算风险分数
    ↓
确定风险等级
    ↓
生成改进建议
    ↓
更新审核记录（状态：审核完成）
    ↓
更新报销单状态
    ├─ 审核通过 → 状态：待人工审核
    └─ 审核不通过 → 状态：已驳回
    ↓
记录流程日志（审核结束）
    ↓
置信度低？
    ├─ 是 → 转入人工审核
    └─ 否 → 审核流程结束
```

#### （3）双层审核协同机制

系统采用"规则引擎+大模型RAG"双层审核模式，两层审核相互协同，共同完成报销单的审核工作。

**规则引擎刚性审核层**
- 优点：执行速度快、结果确定、易于理解和维护
- 缺点：规则固定、缺乏灵活性、无法处理复杂场景
- 适用场景：金额校验、频次校验、发票信息校验、基础合规校验等

**RAG智能柔性审核层**
- 优点：灵活性强、能处理复杂场景、能提供详细的审核理由
- 缺点：执行速度慢、结果不确定、依赖大模型质量
- 适用场景：模糊发票、特殊场景、需要详细审核理由等

**协同机制**
1. **规则优先原则**：先执行规则引擎刚性审核，快速过滤明显不合规的报销单
2. **RAG补充原则**：规则审核通过后，执行RAG智能审核，处理规则无法覆盖的复杂场景
3. **结果融合原则**：综合规则审核结果和RAG审核结果，生成最终审核结论
4. **风险分层原则**：根据规则审核结果和RAG审核结果，计算风险分数，确定风险等级
5. **人工介入原则**：当RAG审核置信度较低时，自动转入人工审核，确保审核质量

**协同优势**
- 提高审核效率：规则引擎快速过滤明显不合规的报销单，减少RAG审核的工作量
- 提高审核准确性：RAG智能审核处理规则无法覆盖的复杂场景，提高审核准确性
- 提供审核依据：RAG智能审核提供详细的审核理由和改进建议，帮助用户理解审核结果
- 降低人工成本：自动化审核减少人工审核的工作量，降低人工成本
- 提升用户体验：快速反馈审核结果，提供详细的审核理由，提升用户体验

#### （4）异常处理流程

系统在运行过程中可能会遇到各种异常情况，需要设计完善的异常处理流程。

**OCR识别失败**
- 系统自动重试OCR识别，最多重试3次
- 如果重试后仍然失败，提示用户手动录入发票信息
- 记录OCR识别失败的日志，方便后续分析和优化

**规则校验失败**
- 系统记录规则校验失败的详细信息
- 系统继续执行RAG智能审核，获取完整的审核结果
- 系统提供规则校验失败的详细说明，帮助用户理解失败原因

**RAG检索失败**
- 系统记录RAG检索失败的详细信息
- 系统使用默认的审核结果（需人工审核）
- 系统提供RAG检索失败的详细说明，帮助用户理解失败原因

**大模型调用失败**
- 系统自动重试大模型调用，最多重试3次
- 如果重试后仍然失败，系统使用默认的审核结果（需人工审核）
- 系统记录大模型调用失败的日志，方便后续分析和优化

**审核超时**
- 系统设置审核超时时间（如30秒）
- 如果审核超时，系统自动转入人工审核
- 系统记录审核超时的日志，方便后续分析和优化

**系统异常**
- 系统捕获所有异常，避免系统崩溃
- 系统记录异常的详细信息，包括异常类型、异常消息、堆栈信息等
- 系统提供友好的错误提示，告诉用户发生了什么
- 系统提供解决方案，告诉用户如何处理

**数据异常**
- 系统对输入数据进行校验，确保数据的完整性和准确性
- 如果数据异常，系统提示用户修改数据
- 系统记录数据异常的日志，方便后续分析和优化

通过上述需求分析，明确了智能报销审核系统的功能性需求、非功能性需求和业务流程，为系统的设计和实现提供了清晰的指导。

## 3.2 系统总体架构

### 3.2.1 系统架构设计

智能报销审核系统采用分层架构设计，遵循领域驱动设计（DDD）思想，将系统划分为多个层次，每个层次负责不同的职责，层与层之间通过明确的接口进行交互。系统整体架构采用前后端分离模式，前端使用Vue.js框架，后端使用Go语言开发，通过RESTful API进行数据交互。

#### （1）整体架构设计

系统采用经典的四层架构设计，从上到下依次为：表现层、应用层、领域层和基础设施层。这种分层架构设计具有以下优势：

- **职责清晰**：每层都有明确的职责，层与层之间通过接口交互，降低了层与层之间的耦合度
- **易于维护**：由于层次分明，修改某一层的实现不会影响其他层，便于系统的维护和升级
- **可扩展性强**：可以在不修改现有代码的情况下，通过增加新的层次或扩展现有层次的功能来扩展系统
- **易于测试**：每层都可以独立进行单元测试和集成测试，提高了系统的可测试性

**架构层次划分**

```
┌─────────────────────────────────────────────────────────────┐
│                        表现层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Web前端     │  │  移动端     │  │  API网关    │  │
│  │  (Vue.js)    │  │  (可选)     │  │  (可选)     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                        应用层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ API路由      │  │  请求处理    │  │  响应封装    │  │
│  │  中间件      │  │  参数校验    │  │  异常处理    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                        领域层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 报销单领域   │  │ 规则引擎领域 │  │ 审核领域     │  │
│  │ 发票领域     │  │ RAG领域      │  │ 知识库领域   │  │
│  │ 用户领域     │  │ OCR领域      │  │              │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      基础设施层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 数据库访问   │  │ 外部服务集成 │  │ 文件存储     │  │
│  │ 缓存访问     │  │ 消息队列     │  │ 日志记录     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      外部服务层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ MySQL数据库  │  │ PostgreSQL   │  │ OCR服务      │  │
│  │ Redis缓存    │  │ 大模型API    │  │ 向量数据库   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### （2）表现层设计

表现层负责与用户进行交互，接收用户的请求，并将处理结果返回给用户。表现层包括Web前端、移动端（可选）和API网关（可选）。

**Web前端**

Web前端使用Vue.js 3.x框架开发，采用组件化开发模式，提供了良好的用户体验。主要技术栈包括：

- **前端框架**：Vue.js 3.x，采用Composition API开发模式
- **UI组件库**：Element Plus，提供丰富的UI组件
- **状态管理**：Pinia，用于管理应用的全局状态
- **路由管理**：Vue Router，用于管理前端路由
- **HTTP客户端**：Axios，用于与后端API进行数据交互
- **构建工具**：Vite，提供快速的开发构建体验

**前端模块划分**

前端按照功能模块进行划分，主要包括：

- **登录模块**：用户登录、登出、密码修改等功能
- **报销单管理模块**：报销单创建、编辑、删除、查询等功能
- **发票管理模块**：发票上传、识别、编辑、删除等功能
- **审核管理模块**：审核任务查看、审核操作、审核结果查询等功能
- **规则引擎管理模块**：规则创建、编辑、删除、启用/禁用等功能
- **知识库管理模块**：文档上传、编辑、删除、查询等功能
- **系统管理模块**：用户管理、角色管理、权限管理、系统配置等功能

**前端架构特点**

- **组件化开发**：将页面拆分为多个可复用的组件，提高代码复用率
- **响应式设计**：适配不同屏幕尺寸，提供良好的移动端体验
- **路由懒加载**：按需加载页面组件，提高首屏加载速度
- **状态管理**：使用Pinia管理全局状态，实现组件间的数据共享
- **错误处理**：统一的错误处理机制，提供友好的错误提示

#### （3）应用层设计

应用层负责协调领域层的业务逻辑，处理用户的请求，并将处理结果返回给表现层。应用层包括API路由、请求处理、参数校验、响应封装、异常处理等功能。

**API路由设计**

系统采用RESTful API设计风格，所有的API接口都遵循RESTful规范。API路由按照功能模块进行分组，主要包括：

- **认证相关接口**：/api/v1/auth/*
- **用户相关接口**：/api/v1/users/*
- **报销单相关接口**：/api/v1/reimbursements/*
- **发票相关接口**：/api/v1/invoices/*
- **审核相关接口**：/api/v1/audits/*
- **规则引擎相关接口**：/api/v1/rules/*
- **知识库相关接口**：/api/v1/knowledge/*
- **系统管理相关接口**：/api/v1/admin/*

**中间件设计**

系统使用Gin框架的中间件机制，实现了多个中间件，用于处理通用的逻辑。主要中间件包括：

- **日志中间件**：记录所有API请求和响应的日志
- **认证中间件**：验证用户的身份，确保只有合法用户才能访问受保护的资源
- **授权中间件**：验证用户的权限，确保用户只能访问其有权限访问的资源
- **限流中间件**：限制API请求的频率，防止恶意攻击
- **CORS中间件**：处理跨域请求，支持前端跨域访问API
- **异常处理中间件**：统一处理异常，返回友好的错误信息

**请求处理流程**

1. **请求接收**：API路由接收用户的HTTP请求
2. **参数校验**：校验请求参数的完整性和正确性
3. **身份认证**：验证用户的身份，提取用户信息
4. **权限校验**：验证用户的权限，确保用户有权限访问该资源
5. **业务处理**：调用领域层的业务逻辑，处理业务请求
6. **响应封装**：将处理结果封装为统一的响应格式
7. **响应返回**：将响应返回给用户

**响应格式设计**

系统采用统一的响应格式，所有的API响应都遵循以下格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1234567890
}
```

- **code**：响应状态码，200表示成功，其他表示失败
- **message**：响应消息，描述请求的处理结果
- **data**：响应数据，包含具体的业务数据
- **timestamp**：响应时间戳，表示响应生成的时间

#### （4）领域层设计

领域层是系统的核心，包含了所有的业务逻辑和领域模型。领域层按照业务领域进行划分，主要包括报销单领域、发票领域、规则引擎领域、审核领域、知识库领域、用户领域、OCR领域等。

**报销单领域**

报销单领域负责报销单的业务逻辑，包括报销单的创建、编辑、删除、查询等功能。主要组件包括：

- **Reimbursement模型**：报销单的领域模型，包含报销单的所有属性
- **ReimbursementService**：报销单服务，封装报销单的业务逻辑
- **ReimbursementRepository**：报销单仓储接口，定义报销单的数据访问操作

**发票领域**

发票领域负责发票的业务逻辑，包括发票的创建、编辑、删除、查询等功能。主要组件包括：

- **Invoice模型**：发票的领域模型，包含发票的所有属性
- **InvoiceService**：发票服务，封装发票的业务逻辑
- **InvoiceRepository**：发票仓储接口，定义发票的数据访问操作

**规则引擎领域**

规则引擎领域负责规则引擎的业务逻辑，包括规则的创建、编辑、删除、启用/禁用、执行等功能。主要组件包括：

- **Rule模型**：规则的领域模型，包含规则的所有属性
- **RuleEngineService**：规则引擎服务，封装规则引擎的业务逻辑
- **RuleRepository**：规则仓储接口，定义规则的数据访问操作
- **RuleEngine**：规则引擎，负责规则的加载、编译和执行

**审核领域**

审核领域负责审核的业务逻辑，包括审核的创建、执行、查询等功能。主要组件包括：

- **AuditResult模型**：审核结果的领域模型，包含审核结果的所有属性
- **AuditService**：审核服务，封装审核的业务逻辑
- **AuditRepository**：审核仓储接口，定义审核的数据访问操作

**知识库领域**

知识库领域负责知识库的业务逻辑，包括文档的上传、编辑、删除、查询等功能。主要组件包括：

- **KnowledgeFile模型**：知识库文件的领域模型，包含文件的所有属性
- **KnowledgeService**：知识库服务，封装知识库的业务逻辑
- **KnowledgeRepository**：知识库仓储接口，定义知识库的数据访问操作

**RAG领域**

RAG领域负责RAG的业务逻辑，包括文档的处理、向量化、检索、大模型交互等功能。主要组件包括：

- **Document模型**：文档的领域模型，包含文档的所有属性
- **Vector模型**：向量的领域模型，包含向量的所有属性
- **RAGService**：RAG服务，封装RAG的业务逻辑
- **LLMClient**：大模型客户端，负责与大模型API的交互
- **VectorStore**：向量存储，负责向量的存储和检索
- **DocumentProcessor**：文档处理器，负责文档的解析和分片
- **PromptBuilder**：提示词构造器，负责提示词的构造

**用户领域**

用户领域负责用户的业务逻辑，包括用户的创建、编辑、删除、查询等功能。主要组件包括：

- **User模型**：用户的领域模型，包含用户的所有属性
- **UserService**：用户服务，封装用户的业务逻辑
- **UserRepository**：用户仓储接口，定义用户的数据访问操作

**OCR领域**

OCR领域负责OCR的业务逻辑，包括发票的识别、信息提取等功能。主要组件包括：

- **InvoiceInfo模型**：发票信息的领域模型，包含发票信息的所有属性
- **OCRService**：OCR服务，封装OCR的业务逻辑
- **OCRClient**：OCR客户端，负责与OCR服务API的交互

#### （5）基础设施层设计

基础设施层负责提供技术支持，包括数据库访问、外部服务集成、文件存储、缓存访问、日志记录等功能。基础设施层实现了领域层定义的仓储接口，为领域层提供数据访问能力。

**数据库访问**

系统使用GORM作为ORM框架，支持MySQL和PostgreSQL数据库。主要组件包括：

- **MySQL客户端**：MySQL数据库的客户端，负责与MySQL数据库的交互
- **PostgreSQL客户端**：PostgreSQL数据库的客户端，负责与PostgreSQL数据库的交互
- **数据迁移工具**：负责数据库的迁移，支持数据库结构的版本管理

**外部服务集成**

系统集成了多个外部服务，包括OCR服务、大模型API、向量数据库等。主要组件包括：

- **OCR服务客户端**：负责与OCR服务API的交互，实现发票的识别
- **大模型API客户端**：负责与大模型API的交互，实现智能审核
- **向量数据库客户端**：负责与向量数据库的交互，实现向量的存储和检索

**文件存储**

系统支持多种文件存储方式，包括本地存储、MinIO对象存储等。主要组件包括：

- **本地存储**：将文件存储在本地文件系统中
- **MinIO存储**：将文件存储在MinIO对象存储中
- **文件存储接口**：定义文件存储的统一接口，支持多种存储方式的切换

**缓存访问**

系统使用Redis作为缓存，提高系统的性能。主要组件包括：

- **Redis客户端**：Redis数据库的客户端，负责与Redis数据库的交互
- **缓存管理器**：负责缓存的统一管理，包括缓存的设置、获取、删除等操作

**日志记录**

系统使用Zap作为日志框架，支持多种日志级别和输出方式。主要组件包括：

- **日志记录器**：负责日志的记录，支持多种日志级别（DEBUG、INFO、WARN、ERROR）
- **日志输出器**：负责日志的输出，支持多种输出方式（控制台、文件）

#### （6）模块间交互设计**

系统各模块之间通过明确的接口进行交互，遵循依赖倒置原则，高层模块不依赖低层模块，都依赖于抽象。模块间交互主要包括：

- **表现层与应用层交互**：通过RESTful API进行数据交互
- **应用层与领域层交互**：通过领域服务接口进行交互
- **领域层与基础设施层交互**：通过仓储接口进行交互
- **领域层与外部服务交互**：通过服务客户端接口进行交互

**交互流程示例**

以报销单创建流程为例，说明模块间的交互流程：

1. **表现层**：用户在前端页面填写报销单信息，点击"保存"按钮
2. **应用层**：API路由接收请求，校验参数，调用报销单服务
3. **领域层**：报销单服务处理业务逻辑，调用报销单仓储
4. **基础设施层**：报销单仓储将报销单信息保存到数据库
5. **基础设施层**：返回保存结果给领域层
6. **领域层**：报销单服务返回处理结果给应用层
7. **应用层**：API路由封装响应，返回给表现层
8. **表现层**：前端页面显示保存结果

### 3.2.2 技术架构选型

技术架构选型是系统设计的重要环节，直接影响系统的性能、可维护性、可扩展性等。本系统在技术选型时，充分考虑了技术的成熟度、社区活跃度、学习成本、性能等因素，选择了最适合本系统的技术栈。

#### （1）后端技术栈

**编程语言：Go 1.25**

选择Go语言作为后端开发语言，主要基于以下考虑：

- **高性能**：Go语言采用协程机制，支持高并发，性能优异
- **简洁易学**：Go语言语法简洁，学习成本低，开发效率高
- **强类型**：Go语言是强类型语言，编译时就能发现大部分错误，提高代码质量
- **丰富的标准库**：Go语言提供了丰富的标准库，减少了对第三方库的依赖
- **跨平台**：Go语言支持跨平台编译，可以在不同的操作系统上运行
- **优秀的并发支持**：Go语言的协程机制使得并发编程变得简单高效

**Web框架：Gin**

选择Gin作为Web框架，主要基于以下考虑：

- **高性能**：Gin框架基于HttpRouter路由器，性能优异
- **中间件机制**：Gin框架提供了强大的中间件机制，便于实现通用的逻辑
- **RESTful API支持**：Gin框架对RESTful API提供了良好的支持
- **丰富的文档**：Gin框架提供了丰富的文档和示例，便于学习和使用
- **活跃的社区**：Gin框架有活跃的社区，问题可以快速得到解决

**ORM框架：GORM**

选择GORM作为ORM框架，主要基于以下考虑：

- **功能强大**：GORM提供了丰富的功能，包括关联、事务、迁移等
- **链式调用**：GORM支持链式调用，代码简洁易读
- **多数据库支持**：GORM支持多种数据库，包括MySQL、PostgreSQL、SQLite等
- **自动迁移**：GORM支持自动迁移，可以根据模型自动创建表结构
- **钩子函数**：GORM提供了钩子函数，可以在操作前后执行自定义逻辑

**规则引擎：Grule**

选择Grule作为规则引擎，主要基于以下考虑：

- **轻量级**：Grule是一个轻量级的规则引擎，易于集成
- **高性能**：Grule规则引擎性能优异，能够快速执行规则
- **灵活的规则定义**：Grule支持灵活的规则定义，可以定义复杂的规则
- **动态加载**：Grule支持规则的动态加载，可以在运行时加载新规则
- **易于使用**：Grule提供了简洁的API，易于使用

#### （2）前端技术栈

**前端框架：Vue.js 3.x**

选择Vue.js作为前端框架，主要基于以下考虑：

- **渐进式框架**：Vue.js是一个渐进式框架，可以逐步集成到现有项目中
- **组件化开发**：Vue.js支持组件化开发，提高代码复用率
- **响应式数据绑定**：Vue.js提供了响应式数据绑定，简化了数据操作
- **虚拟DOM**：Vue.js采用虚拟DOM技术，提高了页面渲染性能
- **丰富的生态系统**：Vue.js有丰富的生态系统，包括路由、状态管理等
- **活跃的社区**：Vue.js有活跃的社区，问题可以快速得到解决

**UI组件库：Element Plus**

选择Element Plus作为UI组件库，主要基于以下考虑：

- **丰富的组件**：Element Plus提供了丰富的UI组件，满足大部分业务需求
- **美观的设计**：Element Plus的设计美观，用户体验良好
- **完善的文档**：Element Plus提供了完善的文档和示例，便于学习和使用
- **TypeScript支持**：Element Plus支持TypeScript，提供类型安全
- **主题定制**：Element Plus支持主题定制，可以根据需求定制主题

**状态管理：Pinia**

选择Pinia作为状态管理库，主要基于以下考虑：

- **轻量级**：Pinia是一个轻量级的状态管理库，体积小
- **TypeScript支持**：Pinia支持TypeScript，提供类型安全
- **简洁的API**：Pinia提供了简洁的API，易于使用
- **模块化**：Pinia支持模块化，可以将状态拆分为多个模块
- **DevTools支持**：Pinia支持Vue DevTools，便于调试

**路由管理：Vue Router**

选择Vue Router作为路由管理库，主要基于以下考虑：

- **官方路由**：Vue Router是Vue.js的官方路由管理库
- **功能强大**：Vue Router提供了丰富的功能，包括路由嵌套、路由守卫等
- **易于使用**：Vue Router提供了简洁的API，易于使用
- **TypeScript支持**：Vue Router支持TypeScript，提供类型安全

**HTTP客户端：Axios**

选择Axios作为HTTP客户端，主要基于以下考虑：

- **Promise支持**：Axios基于Promise，支持async/await语法
- **拦截器**：Axios提供了拦截器，可以在请求和响应时执行自定义逻辑
- **请求取消**：Axios支持请求取消，可以取消未完成的请求
- **自动转换JSON**：Axios自动将响应数据转换为JSON格式
- **丰富的配置**：Axios提供了丰富的配置选项，满足各种需求

**构建工具：Vite**

选择Vite作为构建工具，主要基于以下考虑：

- **快速的开发体验**：Vite提供了快速的开发体验，启动速度快
- **原生ESM支持**：Vite支持原生ESM，无需打包即可运行
- **丰富的插件**：Vite提供了丰富的插件，支持各种需求
- **生产优化**：Vite对生产环境进行了优化，打包体积小

#### （3）数据库技术栈

**关系型数据库：MySQL 5.6+**

选择MySQL作为关系型数据库，主要基于以下考虑：

- **成熟稳定**：MySQL是一个成熟稳定的关系型数据库，广泛应用于各种场景
- **高性能**：MySQL性能优异，能够处理大量的数据
- **丰富的功能**：MySQL提供了丰富的功能，包括事务、索引、存储过程等
- **开源免费**：MySQL是开源免费的，降低了使用成本
- **活跃的社区**：MySQL有活跃的社区，问题可以快速得到解决

**向量数据库：PostgreSQL + PGVector**

选择PostgreSQL + PGVector作为向量数据库，主要基于以下考虑：

- **PGVector扩展**：PGVector是PostgreSQL的向量扩展，支持向量的存储和检索
- **成熟稳定**：PostgreSQL是一个成熟稳定的关系型数据库
- **丰富的功能**：PostgreSQL提供了丰富的功能，包括事务、索引、存储过程等
- **开源免费**：PostgreSQL是开源免费的，降低了使用成本
- **活跃的社区**：PostgreSQL有活跃的社区，问题可以快速得到解决

**缓存数据库：Redis**

选择Redis作为缓存数据库，主要基于以下考虑：

- **高性能**：Redis是一个高性能的键值存储数据库，读写速度快
- **丰富的数据结构**：Redis支持多种数据结构，包括字符串、列表、集合、哈希等
- **持久化**：Redis支持持久化，可以将数据保存到磁盘
- **分布式**：Redis支持分布式，可以构建集群
- **活跃的社区**：Redis有活跃的社区，问题可以快速得到解决

#### （4）AI服务技术栈

**OCR服务：腾讯云OCR**

选择腾讯云OCR作为OCR服务，主要基于以下考虑：

- **高准确率**：腾讯云OCR识别准确率高，能够准确识别发票信息
- **丰富的功能**：腾讯云OCR支持多种发票类型的识别，包括增值税发票、定额发票等
- **稳定的API**：腾讯云OCR提供了稳定的API，易于集成
- **合理的价格**：腾讯云OCR价格合理，降低了使用成本
- **完善的文档**：腾讯云OCR提供了完善的文档和示例，便于集成

**大模型API：智谱AI / 文心一言**

选择智谱AI / 文心一言作为大模型API，主要基于以下考虑：

- **强大的理解能力**：智谱AI / 文心一言具有强大的自然语言理解能力
- **灵活的接口**：智谱AI / 文心一言提供了灵活的API，易于集成
- **合理的价格**：智谱AI / 文心一言价格合理，降低了使用成本
- **完善的文档**：智谱AI / 文心一言提供了完善的文档和示例，便于集成
- **活跃的社区**：智谱AI / 文心一言有活跃的社区，问题可以快速得到解决

**向量嵌入模型：智谱AI Embedding**

选择智谱AI Embedding作为向量嵌入模型，主要基于以下考虑：

- **高质量的向量**：智谱AI Embedding生成高质量的向量，提高检索准确率
- **灵活的接口**：智谱AI Embedding提供了灵活的API，易于集成
- **合理的价格**：智谱AI Embedding价格合理，降低了使用成本
- **完善的文档**：智谱AI Embedding提供了完善的文档和示例，便于集成

#### （5）存储技术栈

**文件存储：本地存储 / MinIO**

系统支持多种文件存储方式，包括本地存储和MinIO对象存储。

**本地存储**

- **简单易用**：本地存储简单易用，无需额外的配置
- **适合小规模**：本地存储适合小规模的文件存储
- **成本低**：本地存储成本低，无需额外的费用

**MinIO对象存储**

- **分布式存储**：MinIO支持分布式存储，可以构建高可用的存储集群
- **兼容S3 API**：MinIO兼容S3 API，可以无缝迁移到AWS S3
- **高性能**：MinIO性能优异，能够处理大量的文件
- **开源免费**：MinIO是开源免费的，降低了使用成本

#### （6）部署技术栈

**容器化：Docker**

选择Docker作为容器化技术，主要基于以下考虑：

- **环境一致性**：Docker保证了开发、测试、生产环境的一致性
- **快速部署**：Docker可以快速部署应用，提高部署效率
- **资源隔离**：Docker提供了资源隔离，提高了系统的安全性
- **易于扩展**：Docker易于扩展，可以快速增加实例数量
- **活跃的社区**：Docker有活跃的社区，问题可以快速得到解决

**容器编排：Kubernetes（可选）**

选择Kubernetes作为容器编排技术（可选），主要基于以下考虑：

- **自动化部署**：Kubernetes支持自动化部署，减少人工操作
- **自动扩缩容**：Kubernetes支持自动扩缩容，根据负载自动调整实例数量
- **服务发现**：Kubernetes提供服务发现，自动发现和注册服务
- **负载均衡**：Kubernetes提供负载均衡，自动分配流量
- **活跃的社区**：Kubernetes有活跃的社区，问题可以快速得到解决

#### （7）监控技术栈

**日志收集：ELK Stack（可选）**

选择ELK Stack作为日志收集技术（可选），主要基于以下考虑：

- **强大的日志收集**：ELK Stack提供了强大的日志收集能力
- **灵活的查询**：ELK Stack提供了灵活的查询功能，可以快速定位问题
- **可视化**：ELK Stack提供了可视化界面，便于查看日志
- **活跃的社区**：ELK Stack有活跃的社区，问题可以快速得到解决

**监控告警：Prometheus + Grafana（可选）**

选择Prometheus + Grafana作为监控告警技术（可选），主要基于以下考虑：

- **强大的监控能力**：Prometheus提供了强大的监控能力
- **灵活的告警**：Prometheus提供了灵活的告警功能，可以自定义告警规则
- **可视化**：Grafana提供了可视化界面，便于查看监控数据
- **活跃的社区**：Prometheus + Grafana有活跃的社区，问题可以快速得到解决

### 3.2.3 部署架构设计

部署架构设计是系统部署的重要环节，直接影响系统的可用性、可扩展性、可维护性等。本系统采用容器化部署方式，支持多种部署模式，包括单机部署、集群部署等。

#### （1）部署模式设计

**单机部署模式**

单机部署模式适合小规模的应用场景，所有的服务都部署在同一台服务器上。单机部署模式的优点是部署简单、成本低，缺点是可用性低、扩展性差。

**部署架构**

```
┌─────────────────────────────────────────────────────────────┐
│                        服务器                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Nginx       │  │  Go后端     │  │  MySQL       │  │
│  │  (反向代理)   │  │  (应用服务)  │  │  (数据库)    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Vue前端     │  │  Redis       │  │  PostgreSQL  │  │
│  │  (静态文件)   │  │  (缓存)      │  │  (向量数据库) │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**部署步骤**

1. **环境准备**：安装Docker、Docker Compose等环境
2. **数据库部署**：部署MySQL、PostgreSQL数据库
3. **缓存部署**：部署Redis缓存
4. **后端部署**：部署Go后端服务
5. **前端部署**：部署Vue前端静态文件
6. **反向代理部署**：部署Nginx反向代理
7. **配置域名**：配置域名和SSL证书

**集群部署模式**

集群部署模式适合大规模的应用场景，服务分布在多台服务器上，提高了系统的可用性和扩展性。集群部署模式的优点是可用性高、扩展性强，缺点是部署复杂、成本高。

**部署架构**

```
┌─────────────────────────────────────────────────────────────┐
│                        负载均衡层                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Nginx LB    │  │  Nginx LB    │  │  Nginx LB    │  │
│  │  (主节点)     │  │  (备节点)     │  │  (备节点)     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                        应用层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Go后端1    │  │  Go后端2    │  │  Go后端3    │  │
│  │  (应用服务)  │  │  (应用服务)  │  │  (应用服务)  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                        数据层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  MySQL主库  │  │  MySQL从库  │  │  Redis集群   │  │
│  │  (主数据库)  │  │  (从数据库)  │  │  (缓存集群)  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ PostgreSQL  │  │  MinIO集群   │  │  向量数据库  │  │
│  │  (向量数据库) │  │  (对象存储)  │  │  (向量存储)  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**部署步骤**

1. **环境准备**：在多台服务器上安装Docker、Docker Compose等环境
2. **负载均衡部署**：部署Nginx负载均衡器
3. **数据库部署**：部署MySQL主从复制、PostgreSQL数据库
4. **缓存部署**：部署Redis集群
5. **应用部署**：部署多个Go后端服务实例
6. **存储部署**：部署MinIO对象存储集群
7. **配置域名**：配置域名和SSL证书
8. **配置监控**：配置监控和告警

#### （2）服务架构设计

**微服务架构（可选）**

系统支持微服务架构（可选），将系统拆分为多个微服务，每个微服务负责一个独立的业务领域。微服务架构的优点是可扩展性强、技术选型灵活，缺点是部署复杂、运维成本高。

**微服务划分**

- **用户服务**：负责用户的创建、编辑、删除、查询等功能
- **报销单服务**：负责报销单的创建、编辑、删除、查询等功能
- **发票服务**：负责发票的创建、编辑、删除、查询等功能
- **审核服务**：负责审核的创建、执行、查询等功能
- **规则引擎服务**：负责规则的创建、编辑、删除、执行等功能
- **知识库服务**：负责知识库的创建、编辑、删除、查询等功能
- **RAG服务**：负责文档的处理、向量化、检索、大模型交互等功能

**微服务通信**

微服务之间通过RESTful API进行通信，支持同步通信和异步通信。

- **同步通信**：使用HTTP协议进行同步通信，适用于实时性要求高的场景
- **异步通信**：使用消息队列进行异步通信，适用于实时性要求不高的场景

**微服务治理**

微服务治理包括服务注册与发现、负载均衡、熔断降级、限流等功能。

- **服务注册与发现**：使用服务注册中心（如Consul、Eureka）实现服务的注册与发现
- **负载均衡**：使用负载均衡器（如Nginx、HAProxy）实现负载均衡
- **熔断降级**：使用熔断器（如Hystrix、Sentinel）实现熔断降级
- **限流**：使用限流器（如Guava RateLimiter）实现限流

#### （3）数据架构设计

**数据存储架构**

系统采用多种数据存储方式，包括关系型数据库、向量数据库、缓存数据库、对象存储等。

**关系型数据库架构**

系统使用MySQL作为关系型数据库，存储结构化数据。MySQL采用主从复制架构，提高了数据库的可用性和性能。

- **主库**：负责写操作，所有的写操作都发送到主库
- **从库**：负责读操作，所有的读操作都发送到从库
- **读写分离**：通过读写分离，提高了数据库的性能

**向量数据库架构**

系统使用PostgreSQL + PGVector作为向量数据库，存储向量数据。PostgreSQL采用主从复制架构，提高了数据库的可用性和性能。

- **主库**：负责写操作，所有的写操作都发送到主库
- **从库**：负责读操作，所有的读操作都发送到从库
- **读写分离**：通过读写分离，提高了数据库的性能

**缓存数据库架构**

系统使用Redis作为缓存数据库，缓存热点数据，提高系统的性能。Redis采用集群架构，提高了缓存的可用性和性能。

- **主节点**：负责写操作，所有的写操作都发送到主节点
- **从节点**：负责读操作，所有的读操作都发送到从节点
- **读写分离**：通过读写分离，提高了缓存的性能

**对象存储架构**

系统使用MinIO作为对象存储，存储文件数据。MinIO采用分布式架构，提高了存储的可用性和性能。

- **分布式存储**：数据分布在多个节点上，提高了存储的可用性
- **数据冗余**：通过数据冗余，提高了数据的安全性
- **负载均衡**：通过负载均衡，提高了存储的性能

**数据备份架构**

系统采用多种备份方式，包括全量备份、增量备份、异地备份等。

- **全量备份**：定期进行全量备份，备份所有数据
- **增量备份**：定期进行增量备份，只备份变化的数据
- **异地备份**：将备份数据存储在异地，提高了数据的安全性

#### （4）高可用架构设计

**负载均衡**

系统采用负载均衡技术，将流量分发到多个服务器上，提高了系统的可用性和性能。

- **Nginx负载均衡**：使用Nginx作为负载均衡器，支持多种负载均衡算法
- **健康检查**：定期检查后端服务的健康状态，自动剔除不健康的服务
- **会话保持**：支持会话保持，保证同一用户的请求发送到同一台服务器

**服务冗余**

系统采用服务冗余技术，部署多个服务实例，提高了系统的可用性。

- **多实例部署**：部署多个服务实例，避免单点故障
- **自动故障转移**：当某个服务实例故障时，自动将流量转移到其他实例
- **快速恢复**：当服务实例故障时，快速启动新的实例

**数据冗余**

系统采用数据冗余技术，将数据复制到多个节点上，提高了数据的可用性。

- **主从复制**：将数据从主库复制到从库，提高了数据的可用性
- **数据备份**：定期备份数据，防止数据丢失
- **异地备份**：将备份数据存储在异地，提高了数据的安全性

**监控告警**

系统采用监控告警技术，实时监控系统运行状态，及时发现和处理异常。

- **系统监控**：监控CPU、内存、磁盘、网络等系统指标
- **业务监控**：监控审核通过率、审核耗时、规则命中率等业务指标
- **告警通知**：当系统或业务出现异常时，及时通知运维人员

## 3.3 双层审核模型设计

智能报销审核系统的核心创新在于采用"规则引擎+大模型RAG"双层审核模型，将刚性规则审核与柔性智能审核相结合，实现报销单据的高效、准确审核。本节详细阐述双层审核模型的设计，包括规则引擎刚性审核层、RAG智能柔性审核层和双层协同机制。

### 3.3.1 规则引擎刚性审核层

规则引擎刚性审核层是双层审核模型的第一层，基于规则引擎技术实现报销单据的自动化校验。该层主要负责执行明确的、可量化的审核规则，对报销单据进行快速、准确的初步筛选，过滤掉明显不合规的报销单据。

#### （1）规则引擎架构设计

规则引擎采用模块化设计，主要包括规则管理、规则执行、特征提取、规则统计等核心模块。系统基于Grule规则引擎框架实现，支持规则的动态加载和执行。

**核心组件**

1. **规则引擎核心（Engine）**
   - 规则加载：从数据库加载所有启用的规则
   - 规则执行：按照优先级顺序执行规则
   - 规则统计：记录规则的执行次数、成功率、平均执行时间等统计信息
   - 规则重载：支持规则的动态重载，无需重启服务

2. **规则管理服务（RuleEngineService）**
   - 规则CRUD操作：创建、读取、更新、删除规则
   - 条件管理：管理规则的条件，包括特征ID、运算符、值、逻辑运算符等
   - 决策管理：管理规则的决策，包括决策类型、决策理由等
   - 特征管理：管理特征，包括特征名称、特征编码、特征类型、值类型等
   - 规则启用/禁用：支持规则的启用和禁用操作

3. **特征函数注册表（FeatureFunctionRegistry）**
   - 特征函数注册：注册特征函数，供规则调用
   - 特征函数执行：执行特征函数，计算特征值
   - 特征函数管理：管理特征函数的元数据

**规则数据模型**

规则数据模型采用关系型数据库存储，主要包括以下表：

1. **规则表（rules）**
   - 规则ID：唯一标识规则
   - 规则名称：规则的名称
   - 规则描述：规则的详细描述
   - 规则优先级：规则的优先级，数字越大优先级越高
   - 规则状态：规则是否启用
   - 创建时间、更新时间：记录规则的创建和更新时间

2. **条件表（conditions）**
   - 条件ID：唯一标识条件
   - 规则ID：关联的规则ID
   - 特征ID：关联的特征ID
   - 运算符：比较运算符（等于、不等于、大于、小于、包含等）
   - 值：比较的目标值
   - 逻辑运算符：逻辑运算符（AND、OR）
   - 排序顺序：条件的执行顺序

3. **决策表（decisions）**
   - 决策ID：唯一标识决策
   - 规则ID：关联的规则ID
   - 决策类型：决策类型（通过、不通过）
   - 决策理由：决策的详细理由

4. **特征表（features）**
   - 特征ID：唯一标识特征
   - 特征名称：特征的名称
   - 特征编码：特征的编码
   - 特征类型：特征的类型（金额、发票、报销、用户、时间等）
   - 值类型：值的类型（字符串、数字、布尔、日期等）
   - 特征类别：特征的类别
   - 特征状态：特征是否启用
   - 函数名称：关联的特征函数名称
   - 函数配置：特征函数的配置参数

5. **特征值表（feature_values）**
   - 特征值ID：唯一标识特征值
   - 特征ID：关联的特征ID
   - 值：特征值
   - 标签：特征值的标签
   - 排序顺序：特征值的排序顺序
   - 特征值状态：特征值是否启用

#### （2）规则执行流程

规则执行流程是规则引擎刚性审核层的核心，负责按照预定义的规则对报销单据进行校验。

**执行流程**

1. **规则加载**
   - 系统启动时，从数据库加载所有启用的规则
   - 规则按照优先级排序，高优先级规则先执行
   - 规则加载到内存中，提高执行效率

2. **特征提取**
   - 从报销单据中提取特征值
   - 如果特征值不存在，尝试调用特征函数计算特征值
   - 特征值存储在内存中，供规则使用

3. **规则评估**
   - 遍历所有规则，按照优先级顺序执行
   - 对每个规则，评估其所有条件
   - 如果所有条件都满足，规则命中，执行决策
   - 如果有条件不满足，规则未命中

4. **结果记录**
   - 记录每个规则的执行结果
   - 包括规则ID、规则名称、是否通过、执行时间、详细信息等
   - 更新规则统计信息

5. **结果汇总**
   - 汇总所有规则的执行结果
   - 判断规则校验是否通过
   - 生成规则校验报告

**规则评估算法**

规则评估采用逻辑运算符（AND、OR）组合多个条件，实现复杂的规则逻辑。

```go
// 评估条件
func evaluateConditions(conditions []Condition, data map[string]interface{}, defaultLogicOp string) bool {
    if len(conditions) == 0 {
        return true
    }

    result := true
    currentLogicOp := defaultLogicOp

    for i, condition := range conditions {
        conditionPassed := evaluateCondition(&condition, data)

        if i > 0 {
            currentLogicOp = condition.LogicOp
        }

        if currentLogicOp == LogicOpAnd {
            result = result && conditionPassed
            if !result {
                return false
            }
        } else {
            result = result || conditionPassed
            if result {
                return true
            }
        }
    }

    return result
}
```

**特征函数调用**

系统支持特征函数的调用，用于计算复杂的特征值。特征函数通过特征函数注册表管理，支持动态注册和调用。

```go
// 调用特征函数
if feature.FunctionName != "" {
    fn, exists := featureFunctionRegistry.Get(feature.FunctionName)
    if exists {
        functionConfig := make(map[string]interface{})
        if feature.FunctionConfig != nil {
            for k, v := range feature.FunctionConfig {
                functionConfig[k] = v
            }
        }

        fnInput := &featurefunction.FunctionInput{
            Config:      functionConfig,
            InvoiceData: data,
            InvoiceID:   fmt.Sprintf("%v", data["invoice_id"]),
        }

        fnOutput, err := fn.Execute(ctx, fnInput)
        if err != nil {
            return false, nil
        }

        if fnOutput.Error != "" {
            return false, nil
        }

        value = fnOutput.Value
    }
}
```

#### （3）规则类型与特征

系统支持多种类型的审核规则，每种规则对应不同的特征和审核逻辑。

**规则类型**

1. **金额校验规则**
   - 住宿费标准校验：根据城市等级和员工级别设定住宿费标准
   - 餐饮费标准校验：根据出差天数和员工级别设定餐饮费标准
   - 交通费标准校验：根据交通工具类型和出差距离设定交通费标准
   - 招待费标准校验：根据招待对象类型设定招待费标准

2. **频次校验规则**
   - 月报销次数限制：限制同一类别报销单的月报销次数
   - 日报销金额限制：限制单日报销金额上限
   - 季度报销金额限制：限制季度报销金额上限

3. **发票信息校验规则**
   - 发票号码唯一性校验：检查发票号码是否已被使用
   - 发票代码格式校验：检查发票代码格式是否正确
   - 发票日期有效性校验：检查发票日期是否在有效期内（如6个月内）
   - 发票金额范围校验：检查发票金额是否在合理范围内
   - 发票类型合规性校验：检查发票类型是否与报销类别匹配

4. **基础合规校验规则**
   - 报销单必填字段完整性校验：检查报销单必填字段是否完整
   - 发票与报销事由关联性校验：检查发票内容是否与报销事由匹配
   - 商品名称合规性校验：检查商品名称是否符合报销政策（如禁止报销烟草、奢侈品等）
   - 商户类型合规性校验：检查商户类型是否在允许范围内

5. **智能校验规则**
   - 智能住宿费校验：根据城市等级、员工级别、活动情况、节假日情况动态调整住宿费标准
   - 智能交通费校验：根据出差距离、交通工具类型、市场价格动态调整交通费标准
   - 智能招待费校验：根据招待对象类型、招待人数、招待地点动态调整招待费标准

**特征分类**

1. **发票相关特征**
   - 发票金额（amount）
   - 发票日期（invoice_date）
   - 发票类型（invoice_type）
   - 发票代码（invoice_code）
   - 发票号码（invoice_number）
   - 商品名称（commodity_name）
   - 商户类型（merchant_type）

2. **报销单相关特征**
   - 报销金额（amount）
   - 报销类别（category）
   - 报销事由（purpose）
   - 申请日期（apply_date）
   - 出差城市（city）
   - 员工级别（employee_level）

3. **时间相关特征**
   - 发票日期与申请日期的差值（is_invoice_date_older_than_6_months）
   - 是否在6个月内（feat-invoice-date-validity）
   - 是否为节假日

4. **频次相关特征**
   - 月报销次数（trip_count）
   - 日报销金额
   - 季度报销金额

5. **智能特征**
   - 城市等级
   - 活动调整系数
   - 节假日调整系数

#### （4）规则引擎优势

规则引擎刚性审核层具有以下优势：

1. **执行速度快**
   - 规则在内存中执行，无需外部调用
   - 规则评估采用高效的算法，执行时间短
   - 支持规则的批量执行，提高处理效率

2. **结果确定**
   - 规则的执行结果是确定的，不会出现不确定性
   - 规则的执行结果可重复，便于测试和调试
   - 规则的执行结果可解释，便于用户理解

3. **易于理解和维护**
   - 规则采用自然语言描述，易于理解
   - 规则采用结构化存储，易于维护
   - 规则支持版本管理，便于回滚和升级

4. **支持动态配置**
   - 规则支持动态加载和重载，无需重启服务
   - 规则支持启用和禁用，便于灵活控制
   - 规则支持优先级配置，便于调整执行顺序

5. **支持特征函数**
   - 规则支持特征函数的调用，实现复杂的特征计算
   - 特征函数支持动态注册，便于扩展
   - 特征函数支持配置参数，便于灵活调整

### 3.3.2 RAG智能柔性审核层

RAG智能柔性审核层是双层审核模型的第二层，基于RAG（检索增强生成）技术和大模型实现报销单据的智能审核。该层主要负责处理规则引擎无法覆盖的复杂场景，提供灵活、智能的审核能力。

#### （1）RAG架构设计

RAG智能柔性审核层采用模块化设计，主要包括文档处理、向量存储、大模型交互、提示词构造等核心模块。系统基于PostgreSQL + PGVector实现向量存储，基于智谱AI/文心一言实现大模型交互。

**核心组件**

1. **RAG服务（RAGService）**
   - 文档处理：处理报销制度文档，包括文档解析、分片、向量化等
   - 向量检索：从向量数据库中检索相关的文档片段
   - 大模型交互：调用大模型API进行智能分析
   - 结果生成：生成审核结论、审核理由、改进建议等

2. **文档处理器（DocumentProcessor）**
   - 文档解析：解析PDF、Word、TXT等格式的文档
   - 文档清洗：清洗文档内容，去除无用信息
   - 文档分片：将长文档切分为多个文本片段
   - 元数据提取：提取文档的元数据，包括作者、创建时间、生效时间等

3. **向量存储（VectorStore）**
   - 向量存储：存储文档片段的向量表示
   - 向量检索：基于向量相似度检索相关的文档片段
   - 向量索引：创建和管理向量索引，提高检索效率
   - 向量统计：统计向量存储的统计信息

4. **大模型客户端（LLMClient）**
   - 大模型调用：调用大模型API进行智能分析
   - 向量嵌入：生成文本的向量表示
   - 响应解析：解析大模型的响应，提取审核结论
   - 健康检查：检查大模型服务的健康状态

5. **提示词构造器（PromptBuilder）**
   - 提示词构造：根据报销单信息和检索到的文档片段构造提示词
   - 提示词优化：优化提示词，提高大模型的响应质量
   - 提示词模板：提供提示词模板，便于快速构造

#### （2）文档处理流程

文档处理流程是RAG智能柔性审核层的基础，负责处理报销制度文档，将其转换为可检索的向量表示。

**处理流程**

1. **文档上传**
   - 用户上传报销制度文档
   - 支持多种文档格式：PDF、Word、TXT等
   - 文档存储在文件系统中

2. **文档解析**
   - 根据文档类型选择合适的解析器
   - PDF文档：使用PDF解析器提取文本
   - Word文档：使用Word解析器提取文本
   - TXT文档：直接读取文本内容

3. **文档清洗**
   - 去除文档中的无用信息，如空白行、特殊字符等
   - 统一文档格式，如换行符、制表符等
   - 提取文档的元数据，如作者、创建时间等

4. **文档分片**
   - 将长文档切分为多个文本片段
   - 每个片段的长度可配置（如500字）
   - 片段之间有重叠（如50字），保证上下文的连续性

5. **文档向量化**
   - 调用大模型的Embedding API生成向量
   - 每个片段生成一个向量表示
   - 向量维度为768维（智谱AI Embedding）

6. **向量存储**
   - 将向量存储到PostgreSQL + PGVector数据库
   - 存储向量的同时存储片段内容、元数据等
   - 创建向量索引，提高检索效率

**文档分片算法**

文档分片采用滑动窗口算法，保证上下文的连续性。

```go
// 分割文档内容
func SplitContent(content string, chunkSize int, overlap int) []string {
    if chunkSize <= 0 {
        chunkSize = 500
    }
    if overlap < 0 {
        overlap = 50
    }

    var chunks []string
    words := strings.Fields(content)

    for i := 0; i < len(words); i += (chunkSize - overlap) {
        end := i + chunkSize
        if end > len(words) {
            end = len(words)
        }

        chunk := strings.Join(words[i:end], " ")
        chunks = append(chunks, chunk)

        if end >= len(words) {
            break
        }
    }

    return chunks
}
```

**向量嵌入生成**

向量嵌入使用智谱AI Embedding模型，生成高质量的向量表示。

```go
// 生成向量嵌入
func (c *LLMClient) GenerateEmbedding(ctx context.Context, text string) ([]float64, error) {
    baseURL := c.embeddingBaseURL
    if baseURL == "" {
        baseURL = c.baseURL
    }

    apiKey := c.embeddingAPIKey
    if apiKey == "" {
        apiKey = c.apiKey
    }

    model := c.embeddingModel
    if model == "" {
        model = c.model
    }

    var embeddingRequest map[string]interface{}
    var endpoint string

    if c.embeddingProvider == "zhipu" {
        embeddingRequest = map[string]interface{}{
            "model": model,
            "input": []string{text},
        }
        if c.embeddingDimension > 0 {
            embeddingRequest["dimensions"] = c.embeddingDimension
        }
        endpoint = "/embeddings"
    } else {
        embeddingRequest = map[string]interface{}{
            "model": model,
            "input": text,
        }
        endpoint = "/embeddings"
    }

    requestBody, err := json.Marshal(embeddingRequest)
    if err != nil {
        return nil, err
    }

    req, err := http.NewRequestWithContext(ctx, "POST", baseURL+endpoint, bytes.NewBuffer(requestBody))
    if err != nil {
        return nil, err
    }

    req.Header.Set("Content-Type", "application/json")
    req.Header.Set("Authorization", "Bearer "+apiKey)

    resp, err := c.httpClient.Do(req)
    if err != nil {
        return nil, err
    }
    defer resp.Body.Close()

    body, err := io.ReadAll(resp.Body)
    if err != nil {
        return nil, err
    }

    if resp.StatusCode != http.StatusOK {
        return nil, errors.New("请求失败")
    }

    var embeddingResponse struct {
        Data []struct {
            Embedding []float64 `json:"embedding"`
            Index     int       `json:"index"`
        } `json:"data"`
    }

    if err := json.Unmarshal(body, &embeddingResponse); err != nil {
        return nil, err
    }

    if len(embeddingResponse.Data) == 0 {
        return nil, errors.New("响应中没有嵌入向量")
    }

    return embeddingResponse.Data[0].Embedding, nil
}
```

#### （3）向量检索流程

向量检索流程是RAG智能柔性审核层的核心，负责从向量数据库中检索相关的文档片段。

**检索流程**

1. **查询向量化**
   - 将报销单信息转换为查询文本
   - 调用大模型的Embedding API生成查询向量
   - 查询向量的维度与文档向量相同（768维）

2. **向量相似度计算**
   - 计算查询向量与文档向量的相似度
   - 使用余弦相似度作为相似度度量
   - 相似度范围在0到1之间，1表示完全相似

3. **Top-K检索**
   - 检索最相似的K个文档片段
   - K值可配置（如5）
   - 返回文档片段的内容、相似度、元数据等

4. **结果过滤**
   - 根据类别过滤文档片段
   - 根据相似度过滤文档片段
   - 根据元数据过滤文档片段

5. **结果排序**
   - 按照相似度排序文档片段
   - 相似度高的文档片段排在前面
   - 返回排序后的文档片段列表

**向量相似度计算**

向量相似度使用余弦相似度计算。

```go
// 计算向量相似度
func CalculateSimilarity(vector1, vector2 []float64) float64 {
    if len(vector1) != len(vector2) {
        return 0
    }

    var dotProduct, norm1, norm2 float64
    for i := 0; i < len(vector1); i++ {
        dotProduct += vector1[i] * vector2[i]
        norm1 += vector1[i] * vector1[i]
        norm2 += vector2[i] * vector2[i]
    }

    if norm1 == 0 || norm2 == 0 {
        return 0
    }

    return dotProduct / (math.Sqrt(norm1) * math.Sqrt(norm2))
}
```

**向量检索SQL**

向量检索使用PostgreSQL的PGVector扩展，支持高效的向量相似度检索。

```sql
SELECT id, file_name, file_type, category, chunk_id, chunk_index, chunk_content,
       embedding <-> ?::vector AS distance
FROM reimbursement_documents
WHERE embedding IS NOT NULL
ORDER BY distance ASC
LIMIT ?
```

#### （4）大模型交互流程

大模型交互流程是RAG智能柔性审核层的核心，负责调用大模型API进行智能分析。

**交互流程**

1. **提示词构造**
   - 拼接报销单信息
   - 拼接规则校验结果
   - 拼接检索到的文档片段
   - 构造完整的提示词

2. **大模型调用**
   - 调用大模型的Chat API
   - 传递提示词给大模型
   - 设置大模型的参数（如温度、最大Token数等）

3. **响应解析**
   - 解析大模型的响应
   - 提取审核结论（通过/不通过/需人工审核）
   - 提取审核理由
   - 提取改进建议
   - 计算置信度

4. **结果生成**
   - 生成审核结果
   - 生成审核报告
   - 生成改进建议

**提示词构造**

提示词构造采用结构化的方式，确保大模型能够准确理解审核需求。

```go
// 构造提示词
func BuildPrompt(reimbursementInfo map[string]interface{}, ruleResults []*RuleValidationResult, chunks []*DocumentChunk) string {
    prompt := "你是一个专业的报销审核助手，能够根据报销制度文档对报销单据进行审核和分析。\n\n"

    prompt += "## 报销单信息\n"
    prompt += fmt.Sprintf("- 报销单ID: %s\n", reimbursementInfo["id"])
    prompt += fmt.Sprintf("- 报销类别: %s\n", reimbursementInfo["category"])
    prompt += fmt.Sprintf("- 报销金额: %.2f\n", reimbursementInfo["amount"])
    prompt += fmt.Sprintf("- 报销事由: %s\n", reimbursementInfo["purpose"])
    prompt += fmt.Sprintf("- 申请日期: %s\n", reimbursementInfo["apply_date"])

    prompt += "\n## 规则校验结果\n"
    for _, result := range ruleResults {
        prompt += fmt.Sprintf("- 规则: %s, 结果: %s, 理由: %s\n",
            result.RuleName,
            map[bool]string{true: "通过", false: "不通过"}[result.Passed],
            result.Message)
    }

    prompt += "\n## 相关制度条款\n"
    for i, chunk := range chunks {
        prompt += fmt.Sprintf("%d. %s\n", i+1, chunk.Content)
    }

    prompt += "\n## 审核要求\n"
    prompt += "请根据上述信息，对报销单据进行审核，并给出以下信息：\n"
    prompt += "1. 审核结论（通过/不通过/需人工审核）\n"
    prompt += "2. 审核理由（详细说明审核结论的原因）\n"
    prompt += "3. 改进建议（如果审核不通过，给出改进建议）\n"
    prompt += "4. 置信度（0到1之间的数值，表示审核结论的可信度）\n"

    return prompt
}
```

**大模型调用**

大模型调用使用智谱AI/文心一言的Chat API。

```go
// 调用大模型
func (c *LLMClient) Chat(ctx context.Context, messages []ChatMessage, temperature float64, maxTokens int) (*ChatResponse, error) {
    if len(messages) == 0 {
        return nil, errors.New("消息列表不能为空")
    }

    request := ChatRequest{
        Model:       c.model,
        Messages:    messages,
        Temperature: temperature,
        MaxTokens:   maxTokens,
        Stream:      false,
    }

    requestBody, err := json.Marshal(request)
    if err != nil {
        return nil, err
    }

    req, err := http.NewRequestWithContext(ctx, "POST", c.baseURL+"/chat/completions", bytes.NewBuffer(requestBody))
    if err != nil {
        return nil, err
    }

    req.Header.Set("Content-Type", "application/json")
    req.Header.Set("Authorization", "Bearer "+c.apiKey)

    resp, err := c.httpClient.Do(req)
    if err != nil {
        return nil, err
    }
    defer resp.Body.Close()

    body, err := io.ReadAll(resp.Body)
    if err != nil {
        return nil, err
    }

    if resp.StatusCode != http.StatusOK {
        return nil, errors.New("请求失败")
    }

    var chatResponse ChatResponse
    if err := json.Unmarshal(body, &chatResponse); err != nil {
        return nil, err
    }

    return &chatResponse, nil
}
```

#### （5）RAG智能审核优势

RAG智能柔性审核层具有以下优势：

1. **灵活性强**
   - 基于大模型的理解能力，能够处理复杂的审核场景
   - 基于向量检索，能够快速找到相关的制度条款
   - 基于提示词构造，能够灵活调整审核逻辑

2. **能处理复杂场景**
   - 能够理解模糊的发票信息
   - 能够处理特殊的报销场景
   - 能够提供详细的审核理由

3. **提供详细的审核依据**
   - 基于检索到的制度条款，提供审核依据
   - 基于大模型的推理能力，提供详细的审核理由
   - 基于规则校验结果，提供改进建议

4. **持续学习**
   - 基于大模型的预训练能力，能够理解各种报销场景
   - 基于知识库的更新，能够学习新的报销制度
   - 基于用户反馈，能够优化审核逻辑

5. **可解释性强**
   - 基于检索到的制度条款，提供审核依据
   - 基于大模型的推理过程，提供审核理由
   - 基于规则校验结果，提供改进建议

### 3.3.3 双层协同机制

双层协同机制是双层审核模型的核心，负责协调规则引擎刚性审核层和RAG智能柔性审核层的工作，实现两层审核的有机结合。

#### （1）协同流程设计

双层协同机制采用"规则优先、RAG补充、结果融合、风险分层、人工介入"的协同策略，实现两层审核的高效协同。

**协同流程**

1. **规则优先原则**
   - 先执行规则引擎刚性审核，快速过滤明显不合规的报销单
   - 规则审核通过后，执行RAG智能审核
   - 规则审核未通过，继续执行RAG智能审核，以获得完整的审核结果

2. **RAG补充原则**
   - RAG智能审核处理规则引擎无法覆盖的复杂场景
   - RAG智能审核提供详细的审核理由和改进建议
   - RAG智能审核基于检索到的制度条款，提供审核依据

3. **结果融合原则**
   - 综合规则审核结果和RAG审核结果，生成最终审核结论
   - 规则审核未通过，风险分数增加0.5
   - RAG审核未通过，风险分数增加0.3
   - RAG审核置信度低，风险分数增加（1-置信度）×0.2

4. **风险分层原则**
   - 根据风险分数确定风险等级
   - 风险分数≥0.7，高风险
   - 风险分数≥0.4，中风险
   - 风险分数<0.4，低风险

5. **人工介入原则**
   - 当RAG审核置信度较低（<0.6）时，自动转入人工审核
   - 当风险等级较高时，建议人工复核
   - 当审核结论不确定时，建议人工审核

**协同流程图**

```
报销单提交
    ↓
创建审核记录
    ↓
规则引擎刚性审核
    ↓
执行所有规则
    ↓
规则审核通过？
    ├─ 是 → 进入RAG智能审核
    └─ 否 → 继续RAG智能审核（获取完整结果）
    ↓
RAG智能柔性审核
    ↓
构建报销单信息
    ↓
检索相关制度条款
    ↓
构造审核提示词
    ↓
调用大模型API
    ↓
解析大模型响应
    ↓
结果融合
    ↓
综合规则和RAG结果
    ↓
计算风险分数
    ↓
确定风险等级
    ↓
生成改进建议
    ↓
审核完成
    ↓
置信度低？
    ├─ 是 → 转入人工审核
    └─ 否 → 审核流程结束
```

#### （2）结果融合算法

结果融合算法是双层协同机制的核心，负责综合规则审核结果和RAG审核结果，生成最终审核结论。

**融合算法**

```go
// 计算风险分数
func calculateRiskScore(audit *AuditResult) float64 {
    riskScore := 0.0

    if !audit.RulePass {
        riskScore += 0.5
    }

    if !audit.RAGPass {
        riskScore += 0.3
    }

    if audit.RAGResults != nil && audit.RAGResults.Confidence < 1.0 {
        riskScore += (1.0 - audit.RAGResults.Confidence) * 0.2
    }

    return riskScore
}

// 确定风险等级
func determineRiskLevel(riskScore float64) string {
    if riskScore >= 0.7 {
        return "高风险"
    } else if riskScore >= 0.4 {
        return "中风险"
    } else {
        return "低风险"
    }
}

// 生成审核结论
func generateAuditReason(audit *AuditResult) string {
    var reasons []string

    if !audit.RulePass {
        reasons = append(reasons, "规则校验未通过")
    }

    if !audit.RAGPass {
        reasons = append(reasons, "RAG智能审核未通过")
    }

    if audit.RAGResults != nil && audit.RAGResults.Confidence < 0.6 {
        reasons = append(reasons, "RAG审核置信度较低")
    }

    if len(reasons) == 0 {
        return "智能审核通过，等待人工审核"
    }

    return strings.Join(reasons, "；")
}
```

#### （3）协同优势

双层协同机制具有以下优势：

1. **提高审核效率**
   - 规则引擎快速过滤明显不合规的报销单，减少RAG审核的工作量
   - RAG智能审核处理规则无法覆盖的复杂场景，提高审核准确性
   - 两层审核并行执行，提高审核效率

2. **提高审核准确性**
   - 规则引擎处理明确的、可量化的审核规则，确保审核的准确性
   - RAG智能审核处理复杂的、模糊的审核场景，提高审核的灵活性
   - 两层审核相互补充，提高审核的全面性

3. **提供审核依据**
   - 规则引擎提供明确的审核依据，便于用户理解审核结果
   - RAG智能审核提供详细的审核理由，便于用户理解审核逻辑
   - 两层审核相互印证，提高审核的可信度

4. **降低人工成本**
   - 自动化审核减少人工审核的工作量，降低人工成本
   - 智能审核提供详细的审核理由，减少人工审核的时间
   - 风险分层审核，合理分配人工审核资源

5. **提升用户体验**
   - 快速反馈审核结果，提升用户体验
   - 提供详细的审核理由，帮助用户理解审核结果
   - 提供改进建议，帮助用户改进报销单据

## 3.4 系统功能模块设计

智能报销审核系统按照功能划分为多个模块，每个模块负责特定的业务功能。本节详细阐述系统的主要功能模块，包括报销单管理模块、发票管理模块、规则引擎模块、智能审核模块和知识库管理模块。

### 3.4.0 系统功能模块架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    智能报销审核系统                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              表现层（Presentation Layer）            │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 报销单管理  │  │  发票管理    │  │  审核管理    │ │   │
│  │  │   页面      │  │   页面      │  │   页面      │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 规则引擎    │  │  知识库管理  │  │  系统管理    │ │   │
│  │  │   页面      │  │   页面      │  │   页面      │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                          ↓                                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              应用层（Application Layer）              │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 报销单应用  │  │  发票应用    │  │  审核应用    │ │   │
│  │  │   服务      │  │   服务      │  │   服务      │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 规则引擎    │  │  知识库应用  │  │  系统管理    │ │   │
│  │  │   应用服务  │  │   服务      │  │   应用服务  │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                          ↓                                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              领域层（Domain Layer）                   │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 报销单      │  │  发票        │  │  审核        │ │   │
│  │  │   领域模型  │  │   领域模型  │  │   领域模型  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 规则引擎    │  │  知识库      │  │  OCR         │ │   │
│  │  │   领域模型  │  │   领域模型  │  │   领域模型  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ RAG         │  │  用户        │  │  特征函数    │ │   │
│  │  │   领域模型  │  │   领域模型  │  │   注册表    │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                          ↓                                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │          基础设施层（Infrastructure Layer）           │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 数据库访问  │  │  外部服务    │  │  文件存储    │ │   │
│  │  │   (GORM)    │  │   集成      │  │   (本地/MinIO)│   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 缓存访问    │  │  消息队列    │  │  日志记录    │   │
│  │  │   (Redis)   │  │   (可选)     │  │   (Zap)      │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                          ↓                                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              外部服务层（External Services）            │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ MySQL数据库  │  │  PostgreSQL  │  │  OCR服务     │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ Redis缓存    │  │  大模型API   │  │  向量数据库   │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 3.4.1 报销单管理模块

报销单管理模块是系统的基础功能模块，负责报销单据的全生命周期管理，包括报销单的创建、编辑、删除、查询、提交、撤回等功能。

#### （1）模块架构设计

报销单管理模块采用分层架构设计，分为表现层、应用层、领域层和基础设施层。

**表现层**
- 报销单创建页面：提供报销单创建的界面
- 报销单编辑页面：提供报销单编辑的界面
- 报销单列表页面：提供报销单查询和展示的界面
- 报销单详情页面：提供报销单详细信息展示的界面

**应用层**
- 报销单应用服务：负责处理报销单相关的业务逻辑
- 请求参数校验：校验请求参数的完整性和正确性
- 响应数据封装：封装响应数据，返回给表现层

**领域层**
- 报销单领域模型：定义报销单的领域模型
- 报销单领域服务：实现报销单的业务逻辑
- 报销单仓储接口：定义报销单的数据访问操作

**基础设施层**
- 报销单仓储实现：实现报销单的数据访问操作
- 数据库访问：使用GORM框架访问数据库

#### （2）核心功能设计

**报销单创建功能**

报销单创建功能允许用户创建新的报销单，填写报销单的基本信息。

**功能流程**

1. 用户进入报销单创建页面
2. 填写报销单基本信息：
   - 报销标题：简洁明了地描述报销内容
   - 报销类别：选择报销类别（差旅费、交通费、住宿费、餐饮费、办公用品、招待费、其他等）
   - 报销金额：填写报销总金额
   - 报销事由：详细说明报销原因
   - 报销日期：选择费用发生日期
   - 详细描述：补充说明报销详情
3. 填写出差信息（如果是差旅费报销）：
   - 出差开始日期
   - 出差结束日期
   - 出差目的地
   - 出差城市
   - 出差省份
   - 出差事由
   - 交通工具
4. 填写项目信息（如果有）：
   - 项目编码
   - 预算科目
5. 点击"保存"按钮，保存报销单
6. 报销单状态为"待提交"

**数据模型**

```go
type Reimbursement struct {
    ID             string    `json:"id" gorm:"primaryKey;type:varchar(36)"`
    UserID         string    `json:"user_id" gorm:"type:varchar(36);not null;index"`
    UserName       string    `json:"user_name" gorm:"type:varchar(100);not null"`
    Department     string    `json:"department" gorm:"type:varchar(100)"`
    Type           string    `json:"type" gorm:"type:varchar(50);not null;index"`
    Title          string    `json:"title" gorm:"type:varchar(200);not null"`
    Description    string    `json:"description" gorm:"type:text"`
    TotalAmount    float64   `json:"total_amount" gorm:"type:decimal(10,2);not null"`
    Currency       string    `json:"currency" gorm:"type:varchar(10);default:'CNY'"`
    ApplyDate      time.Time `json:"apply_date" gorm:"type:date;not null;index"`
    ExpenseDate    time.Time `json:"expense_date" gorm:"type:date;not null;index"`
    Status         string    `json:"status" gorm:"type:varchar(50);not null;index"`
    AuditID        string    `json:"audit_id" gorm:"type:varchar(36);index"`
    CreatedAt      time.Time `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt      time.Time `json:"updated_at" gorm:"autoUpdateTime"`
}
```

**业务规则**

1. 报销金额必须大于0
2. 申请日期不能是未来日期
3. 费用发生日期不能是未来日期
4. 费用发生日期不能晚于申请日期
5. 报销标题不能为空
6. 报销类别不能为空
7. 报销事由不能为空

**报销单编辑功能**

报销单编辑功能允许用户编辑未提交审核的报销单。

**功能流程**

1. 用户进入报销单列表页面
2. 选择要编辑的报销单
3. 进入报销单编辑页面
4. 修改报销单信息
5. 点击"保存"按钮，保存修改
6. 报销单状态仍为"待提交"

**业务规则**

1. 只有"待提交"状态的报销单可以编辑
2. 已提交审核的报销单不能编辑
3. 已完成审核的报销单不能编辑
4. 已驳回的报销单可以编辑后重新提交

**报销单删除功能**

报销单删除功能允许用户删除未提交审核的报销单。

**功能流程**

1. 用户进入报销单列表页面
2. 选择要删除的报销单
3. 点击"删除"按钮
4. 确认删除
5. 报销单被删除

**业务规则**

1. 只有"待提交"状态的报销单可以删除
2. 已提交审核的报销单不能删除
3. 已完成审核的报销单不能删除
4. 已驳回的报销单可以删除

**报销单查询功能**

报销单查询功能允许用户查询报销单，支持多维度查询和分页查询。

**功能流程**

1. 用户进入报销单列表页面
2. 设置查询条件：
   - 用户ID：查询指定用户的报销单
   - 状态：查询指定状态的报销单
   - 时间范围：查询指定时间范围内的报销单
   - 报销类别：查询指定类别的报销单
3. 点击"查询"按钮
4. 系统返回符合条件的报销单列表
5. 支持分页查询和排序

**查询参数**

```go
type ReimbursementFilter struct {
    UserID     string    `json:"user_id"`
    Status     string    `json:"status"`
    Type       string    `json:"type"`
    StartDate  time.Time `json:"start_date"`
    EndDate    time.Time `json:"end_date"`
    Page       int       `json:"page"`
    Size       int       `json:"size"`
}
```

**报销单提交功能**

报销单提交功能允许用户提交报销单进行审核。

**功能流程**

1. 用户进入报销单列表页面
2. 选择要提交的报销单
3. 点击"提交审核"按钮
4. 系统校验报销单信息是否完整
5. 校验通过后，系统自动触发审核流程
6. 报销单状态更新为"审核中"

**业务规则**

1. 只有"待提交"状态的报销单可以提交
2. 报销单必须关联至少一张发票
3. 报销单金额必须等于关联发票金额总和
4. 报销单必填字段必须完整

**报销单撤回功能**

报销单撤回功能允许用户撤回已提交审核的报销单。

**功能流程**

1. 用户进入报销单列表页面
2. 选择要撤回的报销单
3. 点击"撤回"按钮
4. 确认撤回
5. 系统撤回审核流程
6. 报销单状态更新为"待提交"

**业务规则**

1. 只有"审核中"状态的报销单可以撤回
2. 已完成审核的报销单不能撤回
3. 已驳回的报销单不能撤回
4. 撤回后可以修改报销单信息并重新提交

**报销单状态转换图**

```
┌─────────────┐
│ 待提交      │
│ (pending)  │
└──────┬──────┘
       │
       │ 提交审核
       ↓
┌─────────────┐
│ 审核中      │
│ (auditing)  │
└──────┬──────┘
       │
       ├──────────────┐
       │              │
       │ 审核通过      │ 审核不通过
       ↓              ↓
┌─────────────┐  ┌─────────────┐
│ 待人工审核    │  │ 已驳回      │
│ (pending_   │  │ (rejected)  │
│  manual)    │  └──────┬──────┘
└──────┬──────┘         │
       │                │ 修改后重新提交
       │ 人工审核通过       │
       ↓                │
┌─────────────┐         │
│ 已通过      │◄────────┘
│ (approved)  │
└──────┬──────┘
       │
       │ 财务打款
       ↓
┌─────────────┐
│ 已完成      │
│ (completed) │
└─────────────┘
```

**状态说明**

- **待提交**：报销单已创建，但未提交审核
- **审核中**：报销单已提交审核，正在进行规则校验和RAG智能审核
- **待人工审核**：智能审核通过，等待人工审核
- **已驳回**：审核不通过，需要用户修改后重新提交
- **已通过**：人工审核通过，等待财务打款
- **已完成**：财务打款完成，报销流程结束

**状态转换规则**

1. 待提交 → 审核中：用户提交报销单审核
2. 审核中 → 待人工审核：智能审核通过
3. 审核中 → 已驳回：智能审核不通过
4. 待人工审核 → 已通过：人工审核通过
5. 待人工审核 → 已驳回：人工审核不通过
6. 已驳回 → 待提交：用户修改报销单后重新提交
7. 已通过 → 已完成：财务打款完成
8. 审核中 → 待提交：用户撤回审核

#### （3）技术实现

**领域服务实现**

```go
type DomainService struct {
    repo   Repository
    logger logger.Logger
}

func (s *DomainService) CreateReimbursement(ctx context.Context, req *CreateReimbursementRequest) (*Reimbursement, error) {
    if req.UserID == "" {
        return nil, errors.New("用户ID不能为空")
    }
    if req.TotalAmount <= 0 {
        return nil, errors.New("报销金额必须大于0")
    }

    applyDate, expenseDate, err := s.parseDates(ctx, req.ApplyDate, req.ExpenseDate)
    if err != nil {
        return nil, err
    }

    now := time.Now()
    reimbursement := &Reimbursement{
        ID:          uuid.New().String(),
        UserID:      req.UserID,
        UserName:    req.UserName,
        Department:  req.Department,
        Type:        req.Category,
        Title:       req.Reason,
        Description: req.Description,
        TotalAmount: req.TotalAmount,
        Currency:    "CNY",
        ApplyDate:   applyDate,
        ExpenseDate: expenseDate,
        Status:      "pending_submission",
        CreatedAt:   now,
        UpdatedAt:   now,
    }

    if err := s.ValidateReimbursement(ctx, reimbursement); err != nil {
        return nil, err
    }

    if err := s.repo.CreateReimbursement(ctx, reimbursement); err != nil {
        return nil, err
    }

    return reimbursement, nil
}
```

**数据验证**

```go
func (s *DomainService) ValidateReimbursement(ctx context.Context, reimbursement *Reimbursement) error {
    if reimbursement.ID == "" {
        return errors.New("报销单ID不能为空")
    }
    if reimbursement.UserID == "" {
        return errors.New("用户ID不能为空")
    }
    if reimbursement.TotalAmount <= 0 {
        return errors.New("报销金额必须大于0")
    }

    if reimbursement.ApplyDate.After(time.Now()) {
        return errors.New("申请日期不能是未来日期")
    }

    if reimbursement.ExpenseDate.After(time.Now()) {
        return errors.New("费用发生日期不能是未来日期")
    }

    if reimbursement.ExpenseDate.After(reimbursement.ApplyDate) {
        return errors.New("费用发生日期不能晚于申请日期")
    }

    return nil
}
```

### 3.4.2 发票管理模块

发票管理模块负责发票信息的采集、存储和管理，是审核流程的重要数据来源。该模块主要包括发票上传、发票识别、发票校验、发票管理等功能。

#### （1）模块架构设计

发票管理模块采用分层架构设计，分为表现层、应用层、领域层和基础设施层。

**表现层**
- 发票上传页面：提供发票上传的界面
- 发票列表页面：提供发票查询和展示的界面
- 发票详情页面：提供发票详细信息展示的界面
- 发票编辑页面：提供发票编辑的界面

**应用层**
- 发票应用服务：负责处理发票相关的业务逻辑
- 请求参数校验：校验请求参数的完整性和正确性
- 响应数据封装：封装响应数据，返回给表现层

**领域层**
- 发票领域模型：定义发票的领域模型
- 发票领域服务：实现发票的业务逻辑
- 发票仓储接口：定义发票的数据访问操作
- OCR服务接口：定义OCR服务的接口

**基础设施层**
- 发票仓储实现：实现发票的数据访问操作
- OCR服务实现：实现OCR服务的功能
- 数据库访问：使用GORM框架访问数据库
- 文件存储：使用本地存储或MinIO存储发票图片

#### （2）核心功能设计

**发票上传功能**

发票上传功能允许用户上传发票图片，支持单张上传和批量上传。

**功能流程**

1. 用户进入发票上传页面
2. 选择发票图片文件（支持JPG、PNG、PDF等格式）
3. 点击"上传"按钮
4. 系统校验文件格式和大小
5. 校验通过后，系统保存发票图片
6. 系统自动调用OCR服务识别发票信息
7. 发票状态为"待识别"或"已识别"

**业务规则**

1. 发票文件格式必须是JPG、PNG、PDF等格式
2. 发票文件大小不能超过10MB
3. 发票图片必须清晰可辨
4. 发票信息必须完整

**发票识别功能**

发票识别功能使用OCR技术自动识别发票信息，包括发票代码、发票号码、发票类型、开票日期、金额、税额、价税合计、购买方信息、销售方信息、商品明细等。

**功能流程**

1. 系统接收发票识别请求
2. 获取发票图片路径
3. 调用OCR服务识别发票信息
4. OCR服务返回识别结果
5. 系统验证识别结果
6. 系统更新发票信息
7. 发票状态更新为"已识别"或"识别失败"

**识别信息**

```go
type InvoiceInfo struct {
    InvoiceNumber    string         `json:"invoice_number"`
    InvoiceCode     string         `json:"invoice_code"`
    InvoiceType     string         `json:"invoice_type"`
    InvoiceName     string         `json:"invoice_name"`
    InvoiceDate     string         `json:"invoice_date"`
    TotalAmount     float64        `json:"total_amount"`
    TaxAmount       float64        `json:"tax_amount"`
    Buye rName      string         `json:"buyer_name"`
    Buye rTaxNumber string         `json:"buyer_tax_number"`
    SellerName      string         `json:"seller_name"`
    SellerTaxNumber string         `json:"seller_tax_number"`
    Items           []InvoiceItem  `json:"items"`
    Remarks         string         `json:"remarks"`
    RawText         string         `json:"raw_text"`
    IsValid         bool           `json:"is_valid"`
    ErrorMessage    string         `json:"error_message"`
    ParseTime       time.Time      `json:"parse_time"`
}

type InvoiceItem struct {
    Name               string  `json:"name"`
    Specification      string  `json:"specification"`
    Unit               string  `json:"unit"`
    Quantity           float64 `json:"quantity"`
    UnitPrice          float64 `json:"unit_price"`
    AmountWithoutTax    float64 `json:"amount_without_tax"`
    TaxRate            float64 `json:"tax_rate"`
    TaxAmount          float64 `json:"tax_amount"`
}
```

**智能推断功能**

系统支持智能推断发票的扩展字段，包括发票类别、商户类型、是否为电子发票等。

**推断规则**

1. **发票类别推断**
   - 住宿费：发票类型包含"住宿"或销售方包含"酒店"、"宾馆"或商品名称包含"住宿"
   - 交通费：发票类型包含"交通"或销售方包含"航空"、"铁路"、"客运"或商品名称包含"机票"、"火车票"
   - 餐饮费：发票类型包含"餐饮"或销售方包含"餐厅"、"饭店"、"美食"或商品名称包含"餐饮"
   - 办公费：发票类型包含"办公用品"或销售方包含"文具"、"办公"或商品名称包含"办公用品"
   - 培训费：发票类型包含"培训"或销售方包含"培训"、"教育"或商品名称包含"培训"
   - 会议费：发票类型包含"会议"或销售方包含"会议"或商品名称包含"会议"
   - 通信费：发票类型包含"通信"或销售方包含"通信"、"电信"或商品名称包含"话费"
   - 租赁费：发票类型包含"租赁"或销售方包含"租赁"或商品名称包含"租赁"
   - 服务费：发票类型包含"服务"或销售方包含"服务"或商品名称包含"服务"
   - 咨询费：发票类型包含"咨询"或销售方包含"咨询"或商品名称包含"咨询"
   - 其他费用：其他情况

2. **商户类型推断**
   - 酒店：销售方包含"酒店"、"宾馆"、"旅馆"
   - 餐厅：销售方包含"餐厅"、"饭店"、"美食"
   - 航空公司：销售方包含"航空"、"机场"
   - 铁路公司：销售方包含"铁路"、"高铁"
   - 超市：销售方包含"超市"、"商场"
   - 加油站：销售方包含"加油站"、"石油"
   - 医疗机构：销售方包含"医院"、"诊所"
   - 银行：销售方包含"银行"

3. **电子发票推断**
   - 发票类型包含"电子"或"数电"

**发票校验功能**

发票校验功能对发票信息进行校验，确保发票的合规性。

**校验规则**

1. **发票唯一性校验**：检查发票号码是否已被使用，防止重复报销
2. **发票日期有效性校验**：检查发票日期是否在有效期内（如6个月内）
3. **发票金额范围校验**：检查发票金额是否在合理范围内
4. **发票类型合规性校验**：检查发票类型是否与报销类别匹配
5. **发票商品合规性校验**：检查商品名称是否符合报销政策（如禁止报销烟草、奢侈品等）
6. **商户类型合规性校验**：检查商户类型是否在允许范围内

**发票管理功能**

发票管理功能允许用户查看、编辑、删除发票。

**功能流程**

1. 用户进入发票列表页面
2. 查看发票信息
3. 可以编辑发票信息（仅限未提交审核的发票）
4. 可以删除发票（仅限未提交审核的发票）
5. 支持按报销单ID查询发票
6. 支持分页查询和排序

#### （3）技术实现

**OCR服务实现**

```go
type ParserService struct {
    parser   InvoiceParser
    repo     Repository
    logger   logger.Logger
    basePath string
}

func (s *ParserService) ParseInvoiceImage(ctx context.Context, invoiceID string) error {
    invoice, err := s.repo.GetInvoiceByID(ctx, invoiceID)
    if err != nil {
        return err
    }

    fullPath := invoice.ImagePath
    if s.basePath != "" {
        fullPath = s.basePath + "/" + invoice.ImagePath
    }

    ocrResult, err := s.parser.ParseInvoice(ctx, fullPath)
    if err != nil {
        invoice.Status = "解析失败"
        invoice.UpdatedAt = time.Now()
        s.repo.UpdateInvoice(ctx, invoice)
        return err
    }

    isValid, errMsg := ocrResult.Validate()
    if !isValid {
        invoice.Status = "无效"
        invoice.UpdatedAt = time.Now()
        s.repo.UpdateInvoice(ctx, invoice)
        return fmt.Errorf("OCR解析结果验证失败: %s", errMsg)
    }

    s.updateInvoiceFromOCR(ctx, invoice, ocrResult)
    invoice.Status = "已识别"
    invoice.UpdatedAt = time.Now()
    s.repo.UpdateInvoice(ctx, invoice)

    return nil
}
```

**智能推断实现**

```go
func (s *ParserService) inferInvoiceCategory(invoice *Invoice, ocrResult *InvoiceInfo) {
    invoiceType := invoice.Category
    if invoiceType == "" {
        invoiceType = ocrResult.InvoiceType
    }

    sellerName := ocrResult.SellerName
    mainCommodity := invoice.MainCommodity

    switch {
    case strings.Contains(invoiceType, "住宿") || strings.Contains(sellerName, "酒店") || strings.Contains(sellerName, "宾馆") || strings.Contains(mainCommodity, "住宿"):
        invoice.Category = "差旅费"
        invoice.SubCategory = "住宿费"
    case strings.Contains(invoiceType, "交通") || strings.Contains(sellerName, "航空") || strings.Contains(sellerName, "铁路") || strings.Contains(sellerName, "客运") || strings.Contains(mainCommodity, "机票") || strings.Contains(mainCommodity, "火车票"):
        invoice.Category = "差旅费"
        invoice.SubCategory = "交通费"
    case strings.Contains(invoiceType, "餐饮") || strings.Contains(sellerName, "餐厅") || strings.Contains(sellerName, "饭店") || strings.Contains(sellerName, "美食") || strings.Contains(mainCommodity, "餐饮"):
        invoice.Category = "招待费"
        invoice.SubCategory = "餐饮费"
    default:
        invoice.Category = "其他费用"
        invoice.SubCategory = "其他"
    }
}
```

**发票处理流程图**

```
┌─────────────┐
│ 用户上传    │
│  发票图片    │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 文件格式    │
│  校验      │
└──────┬──────┘
       │
       ├──────────────┐
       │              │
       │ 格式正确      │ 格式错误
       ↓              │
┌─────────────┐         │
│ 保存发票    │         │
│  图片      │         │
└──────┬──────┘         │
       │                │
       ↓                │
┌─────────────┐         │
│ 调用OCR    │         │
│  服务      │         │
└──────┬──────┘         │
       │                │
       ├──────────────┐   │
       │              │   │
       │ 识别成功      │ 识别失败
       ↓              │   │
┌─────────────┐         │   │
│ 验证OCR    │         │   │
│  结果      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ├──────────────┐   │   │
       │              │   │   │
       │ 验证通过      │ 验证失败  │
       ↓              │   │   │
┌─────────────┐         │   │   │
│ 智能推断    │         │   │   │
│  发票信息    │         │   │   │
└──────┬──────┘         │   │   │
       │                │   │   │
       ↓                │   │   │
┌─────────────┐         │   │   │
│ 更新发票    │         │   │   │
│  信息      │         │   │   │
└──────┬──────┘         │   │   │
       │                │   │   │
       ↓                │   │   │
┌─────────────┐         │   │   │
│ 发票状态    │         │   │   │
│  更新为    │         │   │   │
│  "已识别"    │         │   │   │
└──────┬──────┘         │   │   │
       │                │   │   │
       ↓                │   │   │
┌─────────────┐         │   │   │
│ 完成处理    │         │   │   │
└─────────────┘         │   │   │
                        │   │   │
                        └───┴───┴───┘
                            │
                            ↓
                    ┌─────────────┐
                    │ 返回错误    │
                    │  提示      │
                    └─────────────┘
```

**流程说明**

1. **文件格式校验**：检查发票文件格式是否为JPG、PNG、PDF等格式
2. **保存发票图片**：将发票图片保存到文件系统
3. **调用OCR服务**：调用OCR服务识别发票信息
4. **验证OCR结果**：验证OCR识别结果是否完整和正确
5. **智能推断发票信息**：根据OCR识别结果智能推断发票类别、商户类型等
6. **更新发票信息**：将OCR识别结果和智能推断结果更新到发票信息中
7. **更新发票状态**：将发票状态更新为"已识别"
8. **错误处理**：如果任何步骤失败，返回错误提示

### 3.4.3 规则引擎模块

规则引擎模块负责规则的创建、编辑、删除、启用/禁用、执行等功能，是系统刚性审核层的重要组成部分。

#### （1）模块架构设计

规则引擎模块采用分层架构设计，分为表现层、应用层、领域层和基础设施层。

**表现层**
- 规则列表页面：提供规则查询和展示的界面
- 规则创建页面：提供规则创建的界面
- 规则编辑页面：提供规则编辑的界面
- 规则测试页面：提供规则测试的界面

**应用层**
- 规则引擎应用服务：负责处理规则相关的业务逻辑
- 请求参数校验：校验请求参数的完整性和正确性
- 响应数据封装：封装响应数据，返回给表现层

**领域层**
- 规则领域模型：定义规则的领域模型
- 规则引擎服务：实现规则引擎的业务逻辑
- 规则仓储接口：定义规则的数据访问操作
- 特征函数注册表：管理特征函数的注册和调用

**基础设施层**
- 规则仓储实现：实现规则的数据访问操作
- 规则引擎实现：实现规则的加载、编译和执行
- 数据库访问：使用GORM框架访问数据库

#### （2）核心功能设计

**规则管理功能**

规则管理功能允许用户创建、编辑、删除、启用/禁用规则。

**功能流程**

1. 用户进入规则列表页面
2. 查看规则列表
3. 点击"创建规则"按钮，进入规则创建页面
4. 填写规则基本信息：
   - 规则名称：规则的名称
   - 规则描述：规则的详细描述
   - 规则优先级：规则的优先级，数字越大优先级越高
   - 规则状态：规则是否启用
5. 添加规则条件：
   - 选择特征：从特征列表中选择特征
   - 选择运算符：选择比较运算符（等于、不等于、大于、小于、包含等）
   - 输入值：输入比较的目标值
   - 选择逻辑运算符：选择逻辑运算符（AND、OR）
6. 设置规则决策：
   - 决策类型：选择决策类型（通过、不通过）
   - 决策理由：输入决策的详细理由
7. 点击"保存"按钮，保存规则
8. 系统自动重新加载规则

**规则数据模型**

```go
type Rule struct {
    ID          string      `json:"id" gorm:"primaryKey;type:varchar(36)"`
    Name        string      `json:"name" gorm:"type:varchar(100);not null;uniqueIndex"`
    Description string      `json:"description" gorm:"type:text"`
    Priority    int         `json:"priority" gorm:"type:int;default:0;index"`
    Enabled     bool        `json:"enabled" gorm:"type:boolean;default:true;index"`
    Conditions  []Condition `json:"conditions" gorm:"foreignKey:RuleID"`
    Decision    *Decision   `json:"decision" gorm:"foreignKey:RuleID"`
    CreatedAt   time.Time   `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt   time.Time   `json:"updated_at" gorm:"autoUpdateTime"`
}

type Condition struct {
    ID        string `json:"id" gorm:"primaryKey;type:varchar(36)"`
    RuleID    string `json:"rule_id" gorm:"type:varchar(36);not null;index"`
    FeatureID string `json:"feature_id" gorm:"type:varchar(36);not null;index"`
    Operator  string `json:"operator" gorm:"type:varchar(20);not null"`
    Value     string `json:"value" gorm:"type:varchar(255);not null"`
    LogicOp   string `json:"logic_op" gorm:"type:varchar(10);default:'and'"`
    SortOrder int    `json:"sort_order" gorm:"type:int;default:0"`
    CreatedAt time.Time `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt time.Time `json:"updated_at" gorm:"autoUpdateTime"`
}

type Decision struct {
    ID     string `json:"id" gorm:"primaryKey;type:varchar(36)"`
    RuleID string `json:"rule_id" gorm:"type:varchar(36);not null;uniqueIndex"`
    Type   string `json:"type" gorm:"type:varchar(20);not null"`
    Reason string `json:"reason" gorm:"type:text"`
    CreatedAt time.Time `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt time.Time `json:"updated_at" gorm:"autoUpdateTime"`
}
```

**规则执行功能**

规则执行功能允许用户执行规则，对报销单据进行校验。

**功能流程**

1. 用户进入规则测试页面
2. 选择要测试的规则
3. 输入测试数据
4. 点击"执行"按钮
5. 系统执行规则
6. 系统返回执行结果

**执行结果**

```go
type RuleValidationResult struct {
    RuleID        string                 `json:"rule_id"`
    RuleName      string                 `json:"rule_name"`
    RuleType      string                 `json:"rule_type"`
    Passed        bool                   `json:"passed"`
    Message       string                 `json:"message"`
    Details       map[string]interface{} `json:"details"`
    ExecutionTime int64                  `json:"execution_time"`
    Timestamp     time.Time              `json:"timestamp"`
}
```

**特征管理功能**

特征管理功能允许用户创建、编辑、删除、启用/禁用特征。

**功能流程**

1. 用户进入特征列表页面
2. 查看特征列表
3. 点击"创建特征"按钮，进入特征创建页面
4. 填写特征基本信息：
   - 特征名称：特征的名称
   - 特征编码：特征的编码
   - 特征类型：特征的类型（金额、发票、报销、用户、时间等）
   - 值类型：值的类型（字符串、数字、布尔、日期等）
   - 特征类别：特征的类别
   - 特征状态：特征是否启用
   - 函数名称：关联的特征函数名称
   - 函数配置：特征函数的配置参数
5. 添加特征值：
   - 值：特征值
   - 标签：特征值的标签
   - 排序顺序：特征值的排序顺序
   - 特征值状态：特征值是否启用
6. 点击"保存"按钮，保存特征

**特征数据模型**

```go
type Feature struct {
    ID             string         `json:"id" gorm:"primaryKey;type:varchar(36)"`
    Name           string         `json:"name" gorm:"type:varchar(100);not null;uniqueIndex"`
    Code           string         `json:"code" gorm:"type:varchar(50);not null;uniqueIndex"`
    Description    string         `json:"description" gorm:"type:text"`
    Type           string         `json:"type" gorm:"type:varchar(20);not null"`
    ValueType      string         `json:"value_type" gorm:"type:varchar(20);not null"`
    Category       string         `json:"category" gorm:"type:varchar(50);index"`
    Enabled        bool           `json:"enabled" gorm:"default:true"`
    FunctionName   string         `json:"function_name" gorm:"type:varchar(100)"`
    FunctionConfig FeatureConfig  `json:"function_config" gorm:"type:json"`
    Values         []FeatureValue `json:"values" gorm:"foreignKey:FeatureID"`
    CreatedAt      time.Time      `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt      time.Time      `json:"updated_at" gorm:"autoUpdateTime"`
}

type FeatureValue struct {
    ID        string    `json:"id" gorm:"primaryKey;type:varchar(36)"`
    FeatureID string    `json:"feature_id" gorm:"type:varchar(36);not null;index"`
    Value     string    `json:"value" gorm:"type:varchar(255);not null"`
    Label     string    `json:"label" gorm:"type:varchar(255);not null"`
    SortOrder int       `json:"sort_order" gorm:"default:0"`
    Enabled   bool      `json:"enabled" gorm:"default:true"`
    CreatedAt time.Time `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt time.Time `json:"updated_at" gorm:"autoUpdateTime"`
}
```

#### （3）技术实现

**规则引擎服务实现**

```go
type RuleEngineService struct {
    ruleRepo                RuleRepository
    conditionRepo           ConditionRepository
    decisionRepo            DecisionRepository
    featureRepo             FeatureRepository
    featureValueRepo        FeatureValueRepository
    featureFunctionRegistry *featurefunction.FunctionRegistry
    logger                  logger.Logger
    engine                  *Engine
}

func (s *RuleEngineService) CreateRule(ctx context.Context, req *CreateRuleRequest) (*Rule, error) {
    if req.Name == "" {
        return nil, errors.New("规则名称不能为空")
    }
    if len(req.Conditions) == 0 {
        return nil, errors.New("规则至少需要一个条件")
    }
    if req.Decision.Type == "" {
        return nil, errors.New("决策类型不能为空")
    }

    rule := &Rule{
        ID:          uuid.New().String(),
        Name:        req.Name,
        Description: req.Description,
        Priority:    req.Priority,
        Enabled:     req.Enabled,
    }

    if err := s.ruleRepo.CreateRule(ctx, rule); err != nil {
        return nil, err
    }

    for _, cond := range req.Conditions {
        condition := &Condition{
            ID:        uuid.New().String(),
            RuleID:    rule.ID,
            FeatureID: cond.FeatureID,
            Operator:  cond.Operator,
            Value:     cond.Value,
            LogicOp:   cond.LogicOp,
            SortOrder: cond.SortOrder,
        }
        if err := s.conditionRepo.CreateCondition(ctx, condition); err != nil {
            return nil, err
        }
    }

    decision := &Decision{
        ID:     uuid.New().String(),
        RuleID: rule.ID,
        Type:   req.Decision.Type,
        Reason: req.Decision.Reason,
    }
    if err := s.decisionRepo.CreateDecision(ctx, decision); err != nil {
        return nil, err
    }

    s.triggerReload(ctx)
    return rule, nil
}
```

**规则执行实现**

```go
func (e *Engine) ExecuteAllRules(ctx context.Context, data interface{}) ([]*RuleValidationResult, error) {
    e.mu.RLock()
    totalRules := len(e.loadedRules)
    e.mu.RUnlock()

    e.mu.RLock()
    rules := make([]*Rule, 0, len(e.loadedRules))
    for _, rule := range e.loadedRules {
        rules = append(rules, rule)
    }
    e.mu.RUnlock()

    results := make([]*RuleValidationResult, 0, len(rules))

    for _, rule := range rules {
        result := e.evaluateRule(ctx, rule, data)
        results = append(results, result)
    }

    return results, nil
}
```

**规则执行流程图**

```
┌─────────────┐
│ 开始执行    │
│  所有规则    │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 加载所有    │
│  启用的规则  │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 按优先级    │
│  排序规则    │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 遍历规则    │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 提取特征    │
│  值        │
└──────┬──────┘
       │
       ├──────────────┐
       │              │
       │ 特征值存在    │ 特征值不存在
       ↓              │
┌─────────────┐         │
│ 评估规则    │         │
│  条件      │         │
└──────┬──────┘         │
       │                │
       ├──────────────┐   │
       │              │   │
       │ 条件满足      │ 条件不满足
       ↓              │   │
┌─────────────┐         │   │
│ 执行规则    │         │   │
│  决策      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 记录执行    │         │   │
│  结果      │         │   │
└──────┬──────┘         │   │
       │                │   │
       │                │   │
       └──────────────┘   │
                          │
                          ↓
                  ┌─────────────┐
                  │ 规则未命中  │
                  └─────────────┘
                          │
                          ↓
                  ┌─────────────┐
                  │ 是否还有     │
                  │  未执行规则  │
                  └──────┬──────┘
                         │
                  ┌────────┼────────┐
                  │        │        │
                  │ 是      │ 否      │
                  ↓        ↓        │
            ┌─────────────┐  ┌─────────────┐
            │ 继续下一个   │  │ 汇总结果    │
            │  规则       │  └──────┬──────┘
            └──────┬──────┘         │
                   │                │
                   └────────────────┘
                            │
                            ↓
                    ┌─────────────┐
                    │ 返回执行    │
                    │  结果      │
                    └─────────────┘
```

**流程说明**

1. **加载规则**：从数据库加载所有启用的规则
2. **排序规则**：按优先级排序规则，高优先级规则先执行
3. **遍历规则**：遍历所有规则，逐个执行
4. **提取特征值**：从报销单据中提取特征值
5. **调用特征函数**：如果特征值不存在，调用特征函数计算特征值
6. **评估条件**：评估规则的所有条件是否满足
7. **执行决策**：如果条件满足，执行规则决策
8. **记录结果**：记录规则的执行结果
9. **继续执行**：继续执行下一个规则
10. **汇总结果**：汇总所有规则的执行结果
11. **返回结果**：返回规则执行结果

### 3.4.4 智能审核模块

智能审核模块负责审核的创建、执行、查询等功能，是系统柔性审核层的重要组成部分。

#### （1）模块架构设计

智能审核模块采用分层架构设计，分为表现层、应用层、领域层和基础设施层。

**表现层**
- 审核列表页面：提供审核查询和展示的界面
- 审核详情页面：提供审核详细信息展示的界面
- 审核报告页面：提供审核报告展示的界面

**应用层**
- 审核应用服务：负责处理审核相关的业务逻辑
- 请求参数校验：校验请求参数的完整性和正确性
- 响应数据封装：封装响应数据，返回给表现层

**领域层**
- 审核领域模型：定义审核的领域模型
- 审核领域服务：实现审核的业务逻辑
- 审核仓储接口：定义审核的数据访问操作
- RAG服务接口：定义RAG服务的接口

**基础设施层**
- 审核仓储实现：实现审核的数据访问操作
- RAG服务实现：实现RAG服务的功能
- 数据库访问：使用GORM框架访问数据库

#### （2）核心功能设计

**审核触发功能**

审核触发功能允许用户触发报销单的审核流程。

**功能流程**

1. 用户进入报销单列表页面
2. 选择要审核的报销单
3. 点击"提交审核"按钮
4. 系统校验报销单信息是否完整
5. 校验通过后，系统自动触发审核流程
6. 系统执行规则引擎刚性审核
7. 系统执行RAG智能柔性审核
8. 系统综合审核结果，生成最终审核结论
9. 报销单状态更新为"待人工审核"或"已驳回"

**审核数据模型**

```go
type AuditResult struct {
    ID              string              `json:"id" gorm:"primaryKey;type:varchar(36)"`
    ReimbursementID string              `json:"reimbursement_id" gorm:"type:varchar(36);not null;uniqueIndex"`
    Status          string              `json:"status" gorm:"type:varchar(50);not null;index"`
    WorkflowStatus  string              `json:"workflow_status" gorm:"type:varchar(50);not null;index"`
    RulePass        bool                `json:"rule_pass" gorm:"type:boolean;default:false"`
    RAGPass         bool                `json:"rag_pass" gorm:"type:boolean;default:false"`
    FinalPass       bool                `json:"final_pass" gorm:"type:boolean;default:false"`
    RuleResults     []*RuleValidationResult `json:"rule_results" gorm:"serializer:json"`
    RAGResults      *RAGAnalysisResult   `json:"rag_results" gorm:"serializer:json"`
    RiskScore       float64             `json:"risk_score" gorm:"type:decimal(3,2);default:0.00"`
    RiskLevel       string              `json:"risk_level" gorm:"type:varchar(20);default:'低风险'"`
    Reason          string              `json:"reason" gorm:"type:text"`
    Suggestions     []string            `json:"suggestions" gorm:"serializer:json"`
    StartedAt       time.Time           `json:"started_at" gorm:"type:datetime;not null"`
    CompletedAt     *time.Time          `json:"completed_at" gorm:"type:datetime"`
    Duration        int64               `json:"duration" gorm:"type:bigint;default:0"`
    CreatedAt       time.Time           `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt       time.Time           `json:"updated_at" gorm:"autoUpdateTime"`
}
```

**审核查询功能**

审核查询功能允许用户查询审核结果，支持按报销单ID、审核状态等条件查询。

**功能流程**

1. 用户进入审核列表页面
2. 设置查询条件：
   - 报销单ID：查询指定报销单的审核结果
   - 审核状态：查询指定状态的审核结果
   - 时间范围：查询指定时间范围内的审核结果
3. 点击"查询"按钮
4. 系统返回符合条件的审核结果列表
5. 支持分页查询和排序

**审核详情功能**

审核详情功能允许用户查看审核的详细信息，包括规则校验结果、RAG分析结果、审核理由、改进建议等。

**功能流程**

1. 用户进入审核列表页面
2. 选择要查看的审核
3. 点击"查看详情"按钮
4. 系统显示审核详细信息：
   - 报销单信息
   - 规则校验结果
   - RAG分析结果
   - 审核结论
   - 审核理由
   - 风险等级
   - 改进建议

**审核撤回功能**

审核撤回功能允许用户撤回已提交审核的报销单。

**功能流程**

1. 用户进入报销单列表页面
2. 选择要撤回的报销单
3. 点击"撤回"按钮
4. 确认撤回
5. 系统撤回审核流程
6. 报销单状态更新为"待提交"

**审核重试功能**

审核重试功能允许用户重新执行审核流程。

**功能流程**

1. 用户进入审核列表页面
2. 选择要重试的审核
3. 点击"重试"按钮
4. 系统重新执行审核流程
5. 系统更新审核结果

**人工审核功能**

人工审核功能允许审核员对智能审核不确定的报销单进行人工审核。

**功能流程**

1. 审核员进入待人工审核列表页面
2. 选择要审核的报销单
3. 查看报销单详细信息、规则校验结果、RAG分析结果等
4. 根据审核结果，选择"通过"或"驳回"
5. 如果选择"驳回"，需要填写驳回理由
6. 点击"提交"按钮
7. 系统更新审核结果
8. 报销单状态更新为"已通过"或"已驳回"

#### （3）技术实现

**审核服务实现**

```go
type Service struct {
    repo              Repository
    reimbursementRepo reimbursement.Repository
    ruleEngineService *ruleenginedomain.RuleEngineService
    ragService        *rag.RAGService
    logger            logger.Logger
}

func (s *Service) StartAudit(ctx context.Context, reimbursementID string) (*AuditResult, error) {
    startTime := time.Now()

    reimbursement, err := s.reimbursementRepo.GetReimbursementByID(ctx, reimbursementID)
    reimbursement.Invoices, err = s.reimbursementRepo.GetInvoicesByReimbursementID(ctx, reimbursementID)
    if err != nil {
        return nil, err
    }

    audit := &AuditResult{
        ID:              uuid.New().String(),
        ReimbursementID: reimbursementID,
        Status:          AuditStatusRunning,
        WorkflowStatus:  WorkflowStatusSubmitted,
        StartedAt:       startTime,
        CreatedAt:       startTime,
        UpdatedAt:       startTime,
    }

    if err := s.repo.CreateAudit(ctx, audit); err != nil {
        return nil, err
    }

    reimbursement.Status = "auditing"
    reimbursement.AuditID = audit.ID
    if err := s.reimbursementRepo.UpdateReimbursement(ctx, reimbursement); err != nil {
        return nil, err
    }

    ruleResults, err := s.executeRuleValidation(ctx, reimbursement)
    if err != nil {
        audit.Status = AuditStatusCompleted
        audit.WorkflowStatus = WorkflowStatusRuleFailed
        audit.Reason = fmt.Sprintf("规则校验失败: %s", err.Error())
        audit.FinalPass = false
        audit.RulePass = false
        audit.RAGPass = false
        audit.RiskScore = 1.0
        audit.RiskLevel = "高风险"
        completedTime := time.Now()
        audit.CompletedAt = &completedTime
        audit.Duration = completedTime.Sub(startTime).Milliseconds()
        audit.UpdatedAt = completedTime
        s.repo.UpdateAudit(ctx, audit)
        return audit, nil
    }

    audit.RuleResults = ruleResults
    rulePass := s.checkRulePass(ruleResults)
    audit.RulePass = rulePass

    reimbursementInfo := s.buildReimbursementInfo(reimbursement)

    ragResult, err := s.executeRAGAnalysis(ctx, reimbursementInfo)
    if err != nil {
        audit.WorkflowStatus = WorkflowStatusRAGFailed
        audit.RAGPass = false
    } else {
        audit.RAGResults = ragResult
        audit.RAGPass = ragResult != nil && ragResult.Confidence > 0.6
    }

    audit.FinalPass = audit.RulePass && audit.RAGPass
    audit.RiskScore = s.calculateRiskScore(audit)
    audit.RiskLevel = s.determineRiskLevel(audit.RiskScore)
    audit.Suggestions = s.generateSuggestions(audit)
    audit.Reason = s.generateAuditReason(audit)

    completedTime := time.Now()
    audit.CompletedAt = &completedTime
    audit.Duration = completedTime.Sub(startTime).Milliseconds()
    audit.UpdatedAt = completedTime
    audit.Status = AuditStatusCompleted

    if audit.FinalPass {
        audit.WorkflowStatus = WorkflowStatusRAGPassed
        audit.Reason = "智能审核通过，等待人工审核"
        reimbursement.Status = "auditing"
    } else {
        audit.WorkflowStatus = WorkflowStatusRAGFailed
        reimbursement.Status = "rejected"
    }

    if err := s.reimbursementRepo.UpdateReimbursement(ctx, reimbursement); err != nil {
        return nil, err
    }

    if err := s.repo.UpdateAudit(ctx, audit); err != nil {
        return nil, err
    }

    return audit, nil
}
```

**RAG智能审核流程图**

```
┌─────────────┐
│ 开始审核    │
│  报销单    │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 创建审核    │
│  记录      │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 更新报销单  │
│  状态为    │
│  "审核中"    │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 执行规则    │
│  校验      │
└──────┬──────┘
       │
       ├──────────────┐
       │              │
       │ 校验成功      │ 校验失败
       ↓              │
┌─────────────┐         │
│ 记录规则    │         │
│  校验结果    │         │
└──────┬──────┘         │
       │                │
       ↓                │
┌─────────────┐         │
│ 构建报销单  │         │
│  信息      │         │
└──────┬──────┘         │
       │                │
       ↓                │
┌─────────────┐         │
│ 执行RAG    │         │
│  智能审核    │         │
└──────┬──────┘         │
       │                │
       ├──────────────┐   │
       │              │   │
       │ 审核成功      │ 审核失败
       ↓              │   │
┌─────────────┐         │   │
│ 记录RAG    │         │   │
│  审核结果    │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 综合审核    │         │   │
│  结果      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 计算风险    │         │   │
│  分数      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 确定风险    │         │   │
│  等级      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 生成改进    │         │   │
│  建议      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 生成审核    │         │   │
│  理由      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ├──────────────┐   │   │
       │              │   │   │
       │ 审核通过      │ 审核不通过
       ↓              │   │   │
┌─────────────┐         │   │   │
│ 更新报销单  │         │   │   │
│  状态为    │         │   │   │
│  "审核中"    │         │   │   │
└──────┬──────┘         │   │   │
       │                │   │   │
       ↓                │   │   │
┌─────────────┐         │   │   │
│ 更新审核    │         │   │   │
│  记录      │         │   │   │
└──────┬──────┘         │   │   │
       │                │   │   │
       │                │   │   │
       └────────────────┘   │   │
                          │   │
                          │   │
                          └───┴───┘
                              │
                              ↓
                      ┌─────────────┐
                      │ 更新报销单  │
                      │  状态为    │
                      │  "已驳回"    │
                      └──────┬──────┘
                             │
                             ↓
                     ┌─────────────┐
                     │ 完成审核    │
                     └─────────────┘
```

**流程说明**

1. **创建审核记录**：创建审核记录，记录审核开始时间
2. **更新报销单状态**：将报销单状态更新为"审核中"
3. **执行规则校验**：执行规则引擎刚性审核
4. **记录规则校验结果**：记录规则校验的结果
5. **构建报销单信息**：构建报销单信息，包括报销单基本信息、发票信息等
6. **执行RAG智能审核**：执行RAG智能柔性审核
7. **记录RAG审核结果**：记录RAG审核的结果
8. **综合审核结果**：综合规则校验结果和RAG审核结果
9. **计算风险分数**：根据规则校验结果和RAG审核结果计算风险分数
10. **确定风险等级**：根据风险分数确定风险等级
11. **生成改进建议**：根据审核结果生成改进建议
12. **生成审核理由**：根据审核结果生成审核理由
13. **更新报销单状态**：根据审核结果更新报销单状态
14. **更新审核记录**：更新审核记录，记录审核结束时间
15. **完成审核**：审核流程结束

### 3.4.5 知识库管理模块

知识库管理模块负责报销制度文档的管理和维护，是RAG智能审核的基础。

#### （1）模块架构设计

知识库管理模块采用分层架构设计，分为表现层、应用层、领域层和基础设施层。

**表现层**
- 文档列表页面：提供文档查询和展示的界面
- 文档上传页面：提供文档上传的界面
- 文档详情页面：提供文档详细信息展示的界面
- 文档编辑页面：提供文档编辑的界面

**应用层**
- 知识库应用服务：负责处理知识库相关的业务逻辑
- 请求参数校验：校验请求参数的完整性和正确性
- 响应数据封装：封装响应数据，返回给表现层

**领域层**
- 知识库领域模型：定义知识库的领域模型
- 知识库领域服务：实现知识库的业务逻辑
- 知识库仓储接口：定义知识库的数据访问操作
- RAG服务接口：定义RAG服务的接口

**基础设施层**
- 知识库仓储实现：实现知识库的数据访问操作
- RAG服务实现：实现RAG服务的功能
- 向量存储实现：实现向量的存储和检索
- 数据库访问：使用GORM框架访问数据库

#### （2）核心功能设计

**文档上传功能**

文档上传功能允许用户上传报销制度文档，支持PDF、Word、TXT等格式。

**功能流程**

1. 用户进入文档上传页面
2. 选择文档文件（支持PDF、Word、TXT等格式）
3. 填写文档基本信息：
   - 文档名称：文档的名称
   - 文档类型：文档的类型（差旅费、交通费、住宿费等）
   - 文档描述：文档的详细描述
4. 点击"上传"按钮
5. 系统校验文件格式和大小
6. 校验通过后，系统保存文档
7. 系统自动处理文档：
   - 解析文档内容
   - 清洗文档内容
   - 分片文档内容
   - 向量化文档内容
   - 存储向量到向量数据库
8. 文档状态为"已处理"

**文档数据模型**

```go
type KnowledgeFile struct {
    ID          string    `json:"id" gorm:"primaryKey;type:varchar(36)"`
    FileName    string    `json:"file_name" gorm:"type:varchar(255);not null;index"`
    FileType    string    `json:"file_type" gorm:"type:varchar(50);not null"`
    Category    string    `json:"category" gorm:"type:varchar(50);not null;index"`
    Description string    `json:"description" gorm:"type:text"`
    FilePath    string    `json:"file_path" gorm:"type:varchar(500);not null"`
    FileSize    int64     `json:"file_size" gorm:"type:bigint;default:0"`
    Status      string    `json:"status" gorm:"type:varchar(50);not null;index"`
    UploaderID  string    `json:"uploader_id" gorm:"type:varchar(36);not null;index"`
    CreatedAt   time.Time `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt   time.Time `json:"updated_at" gorm:"autoUpdateTime"`
}
```

**文档处理功能**

文档处理功能自动处理上传的文档，包括文档解析、内容清洗、文档分片、向量化等。

**处理流程**

1. 系统接收文档处理请求
2. 获取文档文件路径
3. 解析文档内容：
   - PDF文档：使用PDF解析器提取文本
   - Word文档：使用Word解析器提取文本
   - TXT文档：直接读取文本内容
4. 清洗文档内容：
   - 去除文档中的无用信息，如空白行、特殊字符等
   - 统一文档格式，如换行符、制表符等
5. 分片文档内容：
   - 将长文档切分为多个文本片段
   - 每个片段的长度可配置（如500字）
   - 片段之间有重叠（如50字），保证上下文的连续性
6. 向量化文档内容：
   - 调用大模型的Embedding API生成向量
   - 每个片段生成一个向量表示
   - 向量维度为768维（智谱AI Embedding）
7. 存储向量：
   - 将向量存储到PostgreSQL + PGVector数据库
   - 存储向量的同时存储片段内容、元数据等
   - 创建向量索引，提高检索效率

**文档查询功能**

文档查询功能允许用户查询文档，支持按文档名称、文档类型、上传者等条件查询。

**功能流程**

1. 用户进入文档列表页面
2. 设置查询条件：
   - 文档名称：查询指定名称的文档
   - 文档类型：查询指定类型的文档
   - 上传者：查询指定上传者的文档
   - 时间范围：查询指定时间范围内的文档
3. 点击"查询"按钮
4. 系统返回符合条件的文档列表
5. 支持分页查询和排序

**文档编辑功能**

文档编辑功能允许用户编辑文档的基本信息。

**功能流程**

1. 用户进入文档列表页面
2. 选择要编辑的文档
3. 点击"编辑"按钮
4. 修改文档基本信息：
   - 文档名称
   - 文档类型
   - 文档描述
5. 点击"保存"按钮
6. 系统更新文档信息

**文档删除功能**

文档删除功能允许用户删除文档。

**功能流程**

1. 用户进入文档列表页面
2. 选择要删除的文档
3. 点击"删除"按钮
4. 确认删除
5. 系统删除文档：
   - 删除文档文件
   - 删除文档向量
   - 删除文档记录

#### （3）技术实现

**知识库服务实现**

```go
type KnowledgeService struct {
    repo          Repository
    ragService     *rag.RAGService
    documentProcessor *rag.DocumentProcessor
    logger        logger.Logger
}

func (s *KnowledgeService) UploadFile(ctx context.Context, file *KnowledgeFile) error {
    if file.FileName == "" {
        return errors.New("文件名不能为空")
    }
    if file.FileType == "" {
        return errors.New("文件类型不能为空")
    }
    if file.FilePath == "" {
        return errors.New("文件路径不能为空")
    }

    file.ID = uuid.New().String()
    file.Status = "processing"
    file.CreatedAt = time.Now()
    file.UpdatedAt = time.Now()

    if err := s.repo.CreateFile(ctx, file); err != nil {
        return err
    }

    go s.processFile(ctx, file)

    return nil
}

func (s *KnowledgeService) processFile(ctx context.Context, file *KnowledgeFile) {
    document, err := s.documentProcessor.ProcessDocument(ctx, file.FilePath)
    if err != nil {
        file.Status = "failed"
        file.UpdatedAt = time.Now()
        s.repo.UpdateFile(ctx, file)
        return
    }

    for _, chunk := range document.Chunks {
        vector := &rag.Vector{
            ID:           uuid.New().String(),
            DocumentID:   file.ID,
            ChunkID:      chunk.ID,
            ChunkContent: chunk.Content,
            Category:     file.Category,
            Values:       make([]float64, 768),
            Dimension:    768,
            Metadata:     make(map[string]interface{}),
            CreatedAt:    time.Now(),
            UpdatedAt:    time.Now(),
        }

        embedding, err := s.ragService.GenerateEmbedding(ctx, chunk.Content)
        if err != nil {
            continue
        }

        vector.Values = embedding
        s.ragService.StoreVector(ctx, vector)
    }

    file.Status = "processed"
    file.UpdatedAt = time.Now()
    s.repo.UpdateFile(ctx, file)return
}
```

**知识库处理流程图**

```
┌─────────────┐
│ 用户上传    │
│  文档      │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 文件格式    │
│  校验      │
└──────┬──────┘
       │
       ├──────────────┐
       │              │
       │ 格式正确      │ 格式错误
       ↓              │
┌─────────────┐         │
│ 保存文档    │         │
│  记录      │         │
└──────┬──────┘         │
       │                │
       ↓                │
┌─────────────┐         │
│ 异步处理    │         │
│  文档      │         │
└──────┬──────┘         │
       │                │
       ↓                │
┌─────────────┐         │
│ 解析文档    │         │
│  内容      │         │
└──────┬──────┘         │
       │                │
       ├──────────────┐   │
       │              │   │
       │ 解析成功      │ 解析失败
       ↓              │   │
┌─────────────┐         │   │
│ 清洗文档    │         │   │
│  内容      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 分片文档    │         │   │
│  内容      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 遍历文档    │         │   │
│  片段      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ↓                │   │
┌─────────────┐         │   │
│ 生成向量    │         │   │
│  嵌入      │         │   │
└──────┬──────┘         │   │
       │                │   │
       ├──────────────┐   │   │
       │              │   │   │
       │ 生成成功      │ 生成失败
       ↓              │   │   │
┌─────────────┐         │   │   │
│ 存储向量    │         │   │   │
│  到向量库    │         │   │   │
└──────┬──────┘         │   │   │
       │                │   │   │
       ↓                │   │   │
┌─────────────┐         │   │   │
│ 更新文档    │         │   │   │
│  状态为    │         │   │   │
│  "已处理"    │         │   │   │
└──────┬──────┘         │   │   │
       │                │   │   │
       ↓                │   │   │
┌─────────────┐         │   │   │
│ 完成处理    │         │   │   │
└─────────────┘         │   │   │
                        │   │   │
                        └───┴───┴───┘
                            │
                            ↓
                    ┌─────────────┐
                    │ 更新文档    │
                    │  状态为    │
                    │  "处理失败"  │
                    └─────────────┘
```

**流程说明**

1. **文件格式校验**：检查文档文件格式是否为PDF、Word、TXT等格式
2. **保存文档记录**：将文档信息保存到数据库
3. **异步处理文档**：异步处理文档，避免阻塞用户操作
4. **解析文档内容**：根据文档类型解析文档内容
5. **清洗文档内容**：清洗文档内容，去除无用信息
6. **分片文档内容**：将长文档切分为多个文本片段
7. **遍历文档片段**：遍历所有文档片段
8. **生成向量嵌入**：调用大模型的Embedding API生成向量
9. **存储向量到向量库**：将向量存储到PostgreSQL + PGVector数据库
10. **更新文档状态**：将文档状态更新为"已处理"
11. **错误处理**：如果任何步骤失败，将文档状态更新为"处理失败"

通过上述系统功能模块设计，明确了智能报销审核系统的各个功能模块的架构设计、核心功能设计和技术实现，为系统的开发和实现提供了清晰的指导。

通过上述架构设计，明确了智能报销审核系统的系统架构设计、技术架构选型和部署架构设计，为系统的实现和部署提供了清晰的指导。